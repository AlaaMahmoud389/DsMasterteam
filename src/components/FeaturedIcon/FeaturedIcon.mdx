import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as FeaturedIconStories from './FeaturedIcon.stories';

<Meta of={FeaturedIconStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif", padding: '32px 0', maxWidth: 860 }}>

{/* ── Header ── */}
<div style={{ marginBottom: 32 }}>
  <p style={{ margin: '0 0 4px', fontSize: 13, color: '#6b7280' }}>Components</p>
  <h1 style={{ margin: '0 0 10px', fontSize: 28, fontWeight: 700, color: '#000b36', lineHeight: 1.25 }}>
    Featured Icon
  </h1>
  <p style={{ margin: '0 0 16px', fontSize: 15, color: '#374151', lineHeight: 1.6, maxWidth: 700 }}>
    A compact icon container that pairs a design-system icon with a semantic color token, size, shape, and surface mode. Use it to anchor alerts, empty states, feature cards, and onboarding flows with a single, accessible visual signal.
  </p>
  <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6, background: '#d1fae5', color: '#065f46', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#10b981', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>4 Sizes</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>7 Colours</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>2 Shapes</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>2 Surface Modes</span>
  </div>
</div>

{/* ── Table of Contents ── */}
<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 10, padding: '20px 24px', marginBottom: 40 }}>
  <p style={{ margin: '0 0 10px', fontSize: 13, fontWeight: 700, color: '#000b36' }}>On this page</p>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
    {[
      ['#overview',      'Overview'],
      ['#anatomy',       'Anatomy'],
      ['#size',          'Property: Size'],
      ['#color',         'Property: Color'],
      ['#circle',        'Property: Circle'],
      ['#on-color',      'Property: OnColor'],
      ['#properties',    'Properties'],
      ['#accessibility', 'Accessibility'],
      ['#usage',         'Usage Guidelines'],
    ].map(([href, label]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none' }}>{label}</a>
    ))}
  </div>
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* OVERVIEW                                                       */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="overview" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Overview</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Toggle <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>size</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>color</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>circle</code>, and <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>onColor</code> in the controls panel to explore all configurations. Pass an icon name via the <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>icon</code> prop to render any icon from the design-system registry, or pass a custom element via <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>children</code>.
</p>

<Canvas of={FeaturedIconStories.Playground} />
<Controls of={FeaturedIconStories.Playground} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* ANATOMY                                                        */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="anatomy" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Anatomy</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  The Featured Icon is a single composed element — a container div with visual tokens applied directly, wrapping a single icon child.
</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 32 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '40px 1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>#</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Part</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Description</div>
  </div>
  {[
    [1, 'Container',      'The outer div that sets size, background, border-radius, and color. Receives role="img" and aria-label when rendered as a standalone visual.'],
    [2, 'Icon',           'The inner SVG icon element, always rendered with aria-hidden="true". Sized automatically relative to the container.'],
    [3, 'Color token',    'Resolves both the background fill (or border, for OnColor=No) and the icon foreground from a single semantic color prop.'],
    [4, 'Shape modifier', 'circle prop toggles border-radius between a 4 px rounded rectangle and a 50% full circle.'],
    [5, 'Surface mode',   'onColor prop switches the rendering between a filled tinted background and a white background with a tinted border.'],
  ].map(([n, part, desc]) => (
    <div key={n} style={{ display: 'grid', gridTemplateColumns: '40px 1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', fontWeight: 700 }}>{n}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#111827', fontWeight: 600 }}>{part}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* PROPERTY: SIZE                                                 */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="size" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Property: Size</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Four sizes control both the container dimensions and the icon scale. Select the size that matches the information hierarchy — larger sizes for primary calls-to-action, smaller for inline or secondary contexts.
</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Value</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Container</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Usage</div>
  </div>
  {[
    ['sm', '32 × 32 px', 'Compact contexts — inline alerts, dense lists, table rows'],
    ['md', '40 × 40 px', 'Default — card headers, notification banners'],
    ['lg', '48 × 48 px', 'Feature highlights, empty-state headers'],
    ['xl', '56 × 56 px', 'Hero sections, onboarding illustrations, primary calls-to-action'],
  ].map(([val, size, usage]) => (
    <div key={val} style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{val}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{size}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{usage}</div>
    </div>
  ))}
</div>

<Canvas of={FeaturedIconStories.Sizes} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* PROPERTY: COLOR                                               */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="color" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Property: Color</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Seven semantic color tokens communicate the meaning of the icon — choose the token that matches the message conveyed, not the visual preference. Each token resolves a distinct background tint and icon foreground for both OnColor modes.
</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Token</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Label</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>When to use</div>
  </div>
  {[
    ['default',        'Default',        'Neutral actions and generic informational content'],
    ['info',           'Info',           'Informational notices that do not require action'],
    ['success',        'Success',        'Completed actions, confirmations, and positive states'],
    ['warning',        'Warning',        'Cautionary states that need attention but are non-critical'],
    ['error',          'Error',          'Failure states, validation errors, and destructive outcomes'],
    ['brand',          'Brand',          'Premium features, highlights, and primary brand moments'],
    ['gray-attention', 'Gray-attention', 'Secondary or system-level notices without semantic urgency'],
  ].map(([token, label, usage]) => (
    <div key={token} style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{token}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{label}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{usage}</div>
    </div>
  ))}
</div>

<Canvas of={FeaturedIconStories.ColorsOnBackground} />
<Canvas of={FeaturedIconStories.ColorsWhiteBackground} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* PROPERTY: CIRCLE                                              */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="circle" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Property: Circle</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  The <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>circle</code> prop switches the container between two shapes. Use circles for person-like or entity-representing icons; use rectangles for action or status icons.
</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Value</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Shape</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Guidance</div>
  </div>
  {[
    ['false (default)', '4 px rounded rectangle', 'Preferred for action, status, and feature icons'],
    ['true',            'Full circle (50%)',        'Use for avatar-like, entity, or brand moments where a circular frame feels intentional'],
  ].map(([val, shape, guidance]) => (
    <div key={val} style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{val}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{shape}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{guidance}</div>
    </div>
  ))}
</div>

<Canvas of={FeaturedIconStories.Shape} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* PROPERTY: ONCOLOR                                             */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="on-color" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Property: OnColor</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  The <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>onColor</code> prop controls the surface rendering mode. Match the mode to the page surface — use <strong>OnColor=Yes</strong> on dark or strongly tinted backgrounds, and <strong>OnColor=No</strong> on light/white backgrounds.
</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Value</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Rendering</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Use on</div>
  </div>
  {[
    ['true (OnColor=Yes)', 'Filled tinted background + matching foreground icon',        'Dark (#00001C) or colored brand surfaces'],
    ['false (OnColor=No)', 'White background with a tinted semantic border + icon color', 'Light, white, or neutral page backgrounds'],
  ].map(([val, rendering, useOn]) => (
    <div key={val} style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{val}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{rendering}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{useOn}</div>
    </div>
  ))}
</div>

<Canvas of={FeaturedIconStories.OnColorModes} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* PROPERTIES                                                     */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="properties" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Properties</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 32 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1.2fr 1fr 0.8fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Prop', 'Type', 'Default', 'Description'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['size',     "'sm' | 'md' | 'lg' | 'xl'",                                                           "'md'",      'Container and icon size'],
    ['color',    "'default' | 'info' | 'success' | 'warning' | 'error' | 'brand' | 'gray-attention'",  "'default'", 'Semantic color token'],
    ['circle',   'boolean',                                                                               'false',     'true → full circle  ·  false → 4 px rounded rect'],
    ['onColor',  'boolean',                                                                               'true',      'true → filled tinted bg  ·  false → white bg with border'],
    ['icon',     'string',                                                                                '—',         'Icon name from the design-system registry. Ignored when children is provided.'],
    ['children', 'ReactNode',                                                                             '—',         'Custom icon element rendered inside the container. Takes priority over icon prop.'],
    ['className','string',                                                                                '—',         'Additional CSS class applied to the container'],
    ['style',    'CSSProperties',                                                                         '—',         'Inline styles applied to the container element'],
  ].map(([prop, type, def, desc]) => (
    <div key={prop} style={{ display: 'grid', gridTemplateColumns: '1.2fr 1fr 0.8fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{prop}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{def}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* ACCESSIBILITY                                                  */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="accessibility" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Accessibility</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Featured Icon ships with built-in ARIA semantics and colour contrast guarantees for all token/mode combinations.
</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 32 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Concern</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Implementation</div>
  </div>
  {[
    ['Role and label',              'The container receives role="img" and aria-label when an accessible label is provided, exposing the icon\'s meaning to screen readers without relying on the visual icon alone.'],
    ['aria-hidden on icon',         'The inner icon SVG is always rendered with aria-hidden="true", preventing duplicate or redundant announcements when the container already carries role="img".'],
    ['Default hidden mode',         'When no aria-label is supplied the entire element is aria-hidden="true", treating it as purely decorative so screen readers skip it entirely.'],
    ['Contrast — OnColor=Yes',      'Default and Gray-attention icon colours meet WCAG AA (≥ 4.5 : 1) on the #00001C surface. Info, Success, Warning, Error, and Brand icon colours meet WCAG AA on their respective tinted backgrounds.'],
    ['Contrast — OnColor=No',       'All seven icon colour tokens pass WCAG AA on the white background used in OnColor=No mode.'],
    ['Not the sole indicator',      'Always pair the Featured Icon with a text label, heading, or tooltip so colour-blind users and screen-reader users receive the same information as sighted users.'],
  ].map(([concern, impl]) => (
    <div key={concern} style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{concern}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.6 }}>{impl}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* USAGE GUIDELINES                                              */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="usage" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Usage Guidelines</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Follow these guidelines to keep Featured Icon meaningful and accessible across all surfaces.
</p>

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 20, marginBottom: 40 }}>
  <div style={{ border: '1.5px solid #d1fae5', background: '#f0fdf4', borderRadius: 8, padding: '16px 20px' }}>
    <p style={{ margin: '0 0 10px', fontSize: 13, fontWeight: 700, color: '#065f46' }}>Do</p>
    <ul style={{ margin: 0, padding: '0 0 0 18px', fontSize: 13, color: '#065f46', lineHeight: 1.8 }}>
      <li>Use the semantic <code style={{ fontSize: 12, background: '#dcfce7', padding: '1px 4px', borderRadius: 3 }}>color</code> token that matches the message — success for confirmations, error for failures.</li>
      <li>Pair every Featured Icon with an adjacent text label, heading, or tooltip so the meaning is conveyed beyond colour alone.</li>
      <li>Match <code style={{ fontSize: 12, background: '#dcfce7', padding: '1px 4px', borderRadius: 3 }}>onColor</code> to the actual page surface — Yes on dark surfaces, No on light backgrounds.</li>
      <li>Choose the <code style={{ fontSize: 12, background: '#dcfce7', padding: '1px 4px', borderRadius: 3 }}>size</code> proportional to the surrounding content hierarchy.</li>
      <li>Use <code style={{ fontSize: 12, background: '#dcfce7', padding: '1px 4px', borderRadius: 3 }}>circle</code> intentionally — reserve it for entity or avatar-like contexts.</li>
      <li>Provide an <code style={{ fontSize: 12, background: '#dcfce7', padding: '1px 4px', borderRadius: 3 }}>aria-label</code> whenever the icon conveys unique meaning not present in surrounding text.</li>
    </ul>
  </div>
  <div style={{ border: '1.5px solid #fee2e2', background: '#fff5f5', borderRadius: 8, padding: '16px 20px' }}>
    <p style={{ margin: '0 0 10px', fontSize: 13, fontWeight: 700, color: '#991b1b' }}>Don't</p>
    <ul style={{ margin: 0, padding: '0 0 0 18px', fontSize: 13, color: '#991b1b', lineHeight: 1.8 }}>
      <li>Don't choose a color token for aesthetic preference — each token has a defined semantic meaning that users rely on.</li>
      <li>Don't use Featured Icon as the sole indicator of status — always support it with text.</li>
      <li>Don't mix <code style={{ fontSize: 12, background: '#fee2e2', padding: '1px 4px', borderRadius: 3 }}>onColor=true</code> on a white background — the tinted fill will not have sufficient contrast.</li>
      <li>Don't use <code style={{ fontSize: 12, background: '#fee2e2', padding: '1px 4px', borderRadius: 3 }}>xl</code> size in compact or dense layouts where it overwhelms surrounding content.</li>
      <li>Don't render multiple Featured Icons of different semantic colors in close proximity without clear labelling separation.</li>
      <li>Don't override the icon color via CSS — always let the <code style={{ fontSize: 12, background: '#fee2e2', padding: '1px 4px', borderRadius: 3 }}>color</code> prop drive both background and foreground.</li>
    </ul>
  </div>
</div>

</div>
