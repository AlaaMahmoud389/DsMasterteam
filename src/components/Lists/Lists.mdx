import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ListsStories from './Lists.stories';

<Meta of={ListsStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif", padding: '32px 0', maxWidth: 860 }}>

{/* ── Header ── */}
<div style={{ marginBottom: 32 }}>
  <p style={{ margin: '0 0 4px', fontSize: 13, color: '#6b7280' }}>Components</p>
  <h1 style={{ margin: '0 0 10px', fontSize: 28, fontWeight: 700, color: '#000b36', lineHeight: 1.25 }}>
    Lists
  </h1>
  <p style={{ margin: '0 0 16px', fontSize: 15, color: '#374151', lineHeight: 1.6, maxWidth: 700 }}>
    Two composable components — <strong>ListItem</strong> and <strong>List</strong> — supporting
    Ordered, Unordered, and With-Icon types across three colour styles, two indentation levels,
    and full RTL support. Covers all 36 List Item variants and 18 List variants from Figma.
  </p>
  <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6, background: '#d1fae5', color: '#065f46', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#10b981', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>3 Styles</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>3 Types</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>2 Levels</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>RTL ✓</span>
  </div>
</div>

{/* ── Table of Contents ── */}
<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 10, padding: '20px 24px', marginBottom: 40 }}>
  <p style={{ margin: '0 0 10px', fontSize: 13, fontWeight: 700, color: '#000b36' }}>On this page</p>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
    {[
      ['#overview',         'Overview'],
      ['#list-item-types',  'List Item — Types & Levels'],
      ['#list-item-styles', 'List Item — Styles'],
      ['#list-item-rtl',    'List Item — RTL'],
      ['#ordered',          'List — Ordered'],
      ['#unordered',        'List — Unordered'],
      ['#with-icon',        'List — With Icon'],
      ['#list-rtl',         'List — RTL'],
      ['#matrix',           'Full Matrix'],
      ['#props',            'Props'],
      ['#tokens',           'Design Tokens'],
      ['#accessibility',    'Accessibility'],
    ].map(([href, label]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none' }}>{label}</a>
    ))}
  </div>
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* OVERVIEW                                                       */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="overview" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Overview</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Toggle <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>type</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>style</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>level</code>, and <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>rtl</code> in the controls panel to explore all ListItem configurations. Use the List playground to see auto-numbered ordered and nested items.
</p>

<Canvas of={ListsStories.PlaygroundListItem} />
<Controls of={ListsStories.PlaygroundListItem} />

<Canvas of={ListsStories.PlaygroundList} />
<Controls of={ListsStories.PlaygroundList} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* LIST ITEM — TYPES & LEVELS                                     */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="list-item-types" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>List Item — Types &amp; Levels</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Each type has two levels. <strong>Level One</strong> is the main item with no indent.
  <strong> Level Two</strong> is a sub-item indented by 24 px.
</p>

<Canvas of={ListsStories.ListItemTypes} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* LIST ITEM — STYLES                                             */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="list-item-styles" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>List Item — Styles</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Three colour variants. Place <strong>On-Color</strong> on dark backgrounds.
</p>

<Canvas of={ListsStories.ListItemStyles} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* LIST ITEM — RTL                                                */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="list-item-rtl" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>List Item — RTL</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Set <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>rtl</code> to flip direction. Indent on Level Two moves to the right side. Use Arabic text for RTL content.
</p>

<Canvas of={ListsStories.ListItemRTL} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* LIST — ORDERED                                                 */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="ordered" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>List — Ordered</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Level One items are numbered <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>1-</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>2-</code>… Level Two sub-items use letters <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>a-</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>b-</code>… The counter resets when a new Level One item is encountered.
</p>

<Canvas of={ListsStories.OrderedLists} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* LIST — UNORDERED                                              */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="unordered" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>List — Unordered</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Level One items use <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>-</code> as marker. Level Two sub-items use <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>•</code> with 24 px indent.
</p>

<Canvas of={ListsStories.UnorderedLists} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* LIST — WITH ICON                                              */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="with-icon" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>List — With Icon</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Replaces the text marker with a 16 px icon. The default icon is <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>chevron-right</code>; pass a different <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>icon</code> prop to use any icon from the design system.
</p>

<Canvas of={ListsStories.WithIconLists} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* LIST — RTL                                                     */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="list-rtl" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>List — RTL</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Set <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>rtl</code> on the <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>List</code> component to flip direction for all items.
</p>

<Canvas of={ListsStories.RTLLists} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* FULL MATRIX                                                    */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="matrix" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Full Matrix — Style × Type</h2>

<Canvas of={ListsStories.FullMatrix} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* PROPS                                                          */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="props" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Props</h2>

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 24 }}>ListItem</h3>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 32 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.6fr 0.8fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Prop', 'Type', 'Default', 'Description'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['children', 'string',                                   '—',               'The label text of the list item.'],
    ['type',     "'ordered' | 'unordered' | 'with-icon'",   "'unordered'",     'Determines the marker style.'],
    ['level',    '1 | 2',                                    '1',               'Level One = main item (no indent). Level Two = sub-item (24 px indent).'],
    ['style',    "'primary' | 'neutral' | 'on-color'",      "'primary'",       'Colour variant.'],
    ['rtl',      'boolean',                                  'false',           'Right-to-left layout — mirrors the marker position.'],
    ['marker',   'string',                                   'auto',            'Override the auto-generated marker text (e.g. "1-", "a-", "-").'],
    ['icon',     'string',                                   "'chevron-right'", "Icon name used when type='with-icon'."],
  ].map(([prop, type, def, desc]) => (
    <div key={prop} style={{ display: 'grid', gridTemplateColumns: '1fr 1.6fr 0.8fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{prop}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{def}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>List</h3>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.8fr 0.8fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Prop', 'Type', 'Default', 'Description'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['items', "Array<{ children: string, level?: 1|2 }>", '[]',               'List content. level defaults to 1 when omitted.'],
    ['type',  "'ordered' | 'unordered' | 'with-icon'",   "'unordered'",     'List type — controls markers for all items.'],
    ['style', "'primary' | 'neutral' | 'on-color'",      "'primary'",       'Colour variant applied to every item.'],
    ['rtl',   'boolean',                                   'false',           'Right-to-left layout — applied to every item.'],
    ['icon',  'string',                                    "'chevron-right'", "Icon name used when type='with-icon'."],
  ].map(([prop, type, def, desc]) => (
    <div key={prop} style={{ display: 'grid', gridTemplateColumns: '1fr 1.8fr 0.8fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{prop}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{def}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* DESIGN TOKENS                                                  */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="tokens" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Design Tokens</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1.2fr 1.2fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Token', 'Value', 'Usage'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['Primary color',    '#1849A9',            'Marker + label in Primary style'],
    ['Neutral color',    '#000B36',            'Marker + label in Neutral style'],
    ['On-Color',         '#FFFFFF',            'Marker + label in On-Color style — use on dark surfaces'],
    ['Font',             'IBM Plex Sans Arabic','All text, LTR and RTL'],
    ['Font size',        '16 px',              'Body text'],
    ['Font weight',      '400',                'Regular'],
    ['Line height',      '24 px',              'Single row height'],
    ['Level Two indent', '24 px',              'padding-left (LTR) / padding-right (RTL)'],
    ['Marker–text gap',  '8 px',               'gap inside each list row'],
    ['Icon size',        '16 px',              'with-icon marker'],
  ].map(([token, value, usage]) => (
    <div key={token} style={{ display: 'grid', gridTemplateColumns: '1.2fr 1.2fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{token}</div>
      <div style={{ padding: '12px 16px', display: 'flex', alignItems: 'center', gap: 8 }}>
        {value.startsWith('#') && (
          <span style={{ display: 'inline-block', width: 14, height: 14, borderRadius: 3, background: value, border: '1px solid #e5e7eb', flexShrink: 0 }} />
        )}
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{value}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{usage}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* ACCESSIBILITY                                                  */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="accessibility" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Accessibility</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Concern</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Implementation</div>
  </div>
  {[
    ['Marker and icon labelling', 'Markers and icons are wrapped in aria-hidden="true" — screen readers announce the label only, not the visual marker character.'],
    ['RTL reading order',         'The rtl prop sets dir="rtl" on each item, ensuring correct bidirectional text rendering and reading order for screen readers.'],
    ['List semantics',            'The List component renders as a plain div container; wrap it in a semantic <ul> / <ol> if the document structure requires native list semantics.'],
  ].map(([concern, impl]) => (
    <div key={concern} style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{concern}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.6 }}>{impl}</div>
    </div>
  ))}
</div>

</div>
