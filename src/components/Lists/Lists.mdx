{/* Lists.mdx — Masterteam Design System */}
import { Meta } from '@storybook/addon-docs/blocks';
import { ListItem, List } from './Lists';
import * as ListsStories from './Lists.stories';

<Meta of={ListsStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif" }}>

{/* ── Header ───────────────────────────────────────────────── */}
<div style={{ marginBottom: 40, paddingBottom: 28, borderBottom: '1px solid #e5e7eb' }}>
  <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 10 }}>Components / Lists</div>
  <h1 style={{ fontSize: 34, fontWeight: 600, marginBottom: 10, color: '#111827' }}>Lists</h1>
  <div style={{ fontSize: 15, color: '#6b7280', maxWidth: 680, lineHeight: 1.7 }}>
    Two composable components — <strong>ListItem</strong> and <strong>List</strong> — supporting
    Ordered, Unordered, and With-Icon types across three colour styles, two indentation levels,
    and full RTL support. Covers all 36 List Item variants and 18 List variants from Figma.
  </div>
  <div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', marginTop: 16 }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#059669', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>3 styles</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>3 types</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>2 levels</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>RTL ✓</span>
    <a href="https://www.figma.com/design/WTmRAkJVvw0IvZMA7wBdTC/Masterteam-Ds-For-Experiment-ONLY?node-id=4140-89810" target="_blank" rel="noopener" style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#eff3fb', color: '#1849a9', padding: '3px 12px', borderRadius: 100, fontSize: 12, textDecoration: 'none', fontWeight: 500 }}>Figma ↗</a>
  </div>
</div>

---

{/* ── List Item — All Types × Levels ───────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>List Item — Types & Levels</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Each type has two levels. <strong>Level One</strong> is the main item with no indent.
    <strong> Level Two</strong> is a sub-item indented by 24 px.
  </p>

  {/* Ordered */}
  <div style={{ marginBottom: 32 }}>
    <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 16 }}>Ordered — Level One (marker: 1-)</div>
    <ListItem type="ordered" level={1} style="primary" marker="1-">List item</ListItem>
  </div>

  <div style={{ marginBottom: 32 }}>
    <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 16 }}>Ordered — Level Two (marker: a-, indent: 24 px)</div>
    <ListItem type="ordered" level={2} style="primary" marker="a-">List item</ListItem>
  </div>

  {/* Unordered */}
  <div style={{ marginBottom: 32 }}>
    <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 16 }}>Unordered — Level One (marker: -)</div>
    <ListItem type="unordered" level={1} style="primary">List item</ListItem>
  </div>

  <div style={{ marginBottom: 32 }}>
    <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 16 }}>Unordered — Level Two (marker: •, indent: 24 px)</div>
    <ListItem type="unordered" level={2} style="primary">List item</ListItem>
  </div>

  {/* With Icon */}
  <div style={{ marginBottom: 32 }}>
    <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 16 }}>With Icon — Level One</div>
    <ListItem type="with-icon" level={1} style="primary">List item</ListItem>
  </div>

  <div style={{ marginBottom: 32 }}>
    <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 16 }}>With Icon — Level Two (indent: 24 px)</div>
    <ListItem type="with-icon" level={2} style="primary">List item</ListItem>
  </div>
</div>

---

{/* ── List Item — All Styles ────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>List Item — Styles</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Three colour variants. Place <strong>On-Color</strong> on dark backgrounds.
  </p>

  <div style={{ display: 'flex', flexDirection: 'column', gap: 24 }}>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Primary — #1849A9</div>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
        <ListItem type="ordered"   level={1} style="primary" marker="1-">List item</ListItem>
        <ListItem type="unordered" level={1} style="primary">List item</ListItem>
        <ListItem type="with-icon" level={1} style="primary">List item</ListItem>
      </div>
    </div>

    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Neutral — #000B36</div>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
        <ListItem type="ordered"   level={1} style="neutral" marker="1-">List item</ListItem>
        <ListItem type="unordered" level={1} style="neutral">List item</ListItem>
        <ListItem type="with-icon" level={1} style="neutral">List item</ListItem>
      </div>
    </div>

    <div style={{ background: '#000b36', padding: 16, borderRadius: 8 }}>
      <div style={{ fontSize: 12, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>On-Color — #FFFFFF (on dark surface)</div>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
        <ListItem type="ordered"   level={1} style="on-color" marker="1-">List item</ListItem>
        <ListItem type="unordered" level={1} style="on-color">List item</ListItem>
        <ListItem type="with-icon" level={1} style="on-color">List item</ListItem>
      </div>
    </div>
  </div>
</div>

---

{/* ── List Item — RTL ───────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>List Item — RTL</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Set <code>rtl</code> to flip direction. Indent on Level Two moves to the right side. Use Arabic text for RTL content.
  </p>

  <div style={{ display: 'flex', gap: 64, flexWrap: 'wrap' }}>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Ordered RTL — Level One</div>
      <ListItem type="ordered" level={1} style="primary" rtl marker="1-">عنصر قائمة</ListItem>
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Ordered RTL — Level Two</div>
      <ListItem type="ordered" level={2} style="primary" rtl marker="أ-">عنصر قائمة</ListItem>
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Unordered RTL — Level One</div>
      <ListItem type="unordered" level={1} style="primary" rtl>عنصر قائمة</ListItem>
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Unordered RTL — Level Two</div>
      <ListItem type="unordered" level={2} style="primary" rtl>عنصر قائمة</ListItem>
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>With Icon RTL — Neutral</div>
      <ListItem type="with-icon" level={1} style="neutral" rtl>عنصر قائمة</ListItem>
    </div>
  </div>
</div>

---

{/* ── List — Ordered ────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>List — Ordered</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Level One items are numbered <code>1-</code>, <code>2-</code>… Level Two sub-items use letters <code>a-</code>, <code>b-</code>…
    The counter resets when a new Level One item is encountered.
  </p>

  <div style={{ display: 'flex', gap: 64, flexWrap: 'wrap' }}>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Primary</div>
      <List type="ordered" style="primary" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Neutral</div>
      <List type="ordered" style="neutral" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
    <div style={{ background: '#000b36', padding: 16, borderRadius: 8 }}>
      <div style={{ fontSize: 12, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>On-Color</div>
      <List type="ordered" style="on-color" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
  </div>
</div>

---

{/* ── List — Unordered ─────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>List — Unordered</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Level One items use <code>-</code> as marker. Level Two sub-items use <code>•</code> with 24 px indent.
  </p>

  <div style={{ display: 'flex', gap: 64, flexWrap: 'wrap' }}>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Primary</div>
      <List type="unordered" style="primary" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Neutral</div>
      <List type="unordered" style="neutral" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
    <div style={{ background: '#000b36', padding: 16, borderRadius: 8 }}>
      <div style={{ fontSize: 12, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>On-Color</div>
      <List type="unordered" style="on-color" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
  </div>
</div>

---

{/* ── List — With Icon ─────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>List — With Icon</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Replaces the text marker with a 16 px icon. The default icon is <code>chevron-right</code>; pass a different
    <code> icon</code> prop to use any icon from the design system.
  </p>

  <div style={{ display: 'flex', gap: 64, flexWrap: 'wrap' }}>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Primary</div>
      <List type="with-icon" style="primary" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Neutral</div>
      <List type="with-icon" style="neutral" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
    <div style={{ background: '#000b36', padding: 16, borderRadius: 8 }}>
      <div style={{ fontSize: 12, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>On-Color</div>
      <List type="with-icon" style="on-color" items={[
        { children: 'List item', level: 1 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
        { children: 'List item', level: 2 },
      ]} />
    </div>
  </div>
</div>

---

{/* ── List — RTL ───────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>List — RTL</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Set <code>rtl</code> on the <code>List</code> component to flip direction for all items.
  </p>

  <div style={{ display: 'flex', gap: 64, flexWrap: 'wrap' }}>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Ordered — RTL — Primary</div>
      <List type="ordered" style="primary" rtl items={[
        { children: 'عنصر قائمة', level: 1 },
        { children: 'عنصر قائمة', level: 2 },
        { children: 'عنصر قائمة', level: 2 },
        { children: 'عنصر قائمة', level: 2 },
      ]} />
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Unordered — RTL — Neutral</div>
      <List type="unordered" style="neutral" rtl items={[
        { children: 'عنصر قائمة', level: 1 },
        { children: 'عنصر قائمة', level: 2 },
        { children: 'عنصر قائمة', level: 2 },
        { children: 'عنصر قائمة', level: 2 },
      ]} />
    </div>
    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>With Icon — RTL — Primary</div>
      <List type="with-icon" style="primary" rtl items={[
        { children: 'عنصر قائمة', level: 1 },
        { children: 'عنصر قائمة', level: 2 },
        { children: 'عنصر قائمة', level: 2 },
        { children: 'عنصر قائمة', level: 2 },
      ]} />
    </div>
  </div>
</div>

---

{/* ── Full Matrix ───────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 24 }}>Full Matrix — Style × Type</h2>

  <table style={{ borderCollapse: 'collapse', fontSize: 13, width: '100%' }}>
    <thead>
      <tr>
        <th style={{ padding: '8px 16px', textAlign: 'left', color: '#9ca3af', fontWeight: 400, borderBottom: '1px solid #f3f4f6' }}>Style</th>
        <th style={{ padding: '8px 16px', textAlign: 'left', color: '#9ca3af', fontWeight: 400, borderBottom: '1px solid #f3f4f6' }}>Ordered</th>
        <th style={{ padding: '8px 16px', textAlign: 'left', color: '#9ca3af', fontWeight: 400, borderBottom: '1px solid #f3f4f6' }}>Unordered</th>
        <th style={{ padding: '8px 16px', textAlign: 'left', color: '#9ca3af', fontWeight: 400, borderBottom: '1px solid #f3f4f6' }}>With Icon</th>
      </tr>
    </thead>
    <tbody>
      <tr style={{ borderTop: '1px solid #f3f4f6' }}>
        <td style={{ padding: '16px', color: '#6b7280', fontSize: 12, textTransform: 'uppercase', verticalAlign: 'top' }}>Primary</td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="ordered" style="primary" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="unordered" style="primary" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="with-icon" style="primary" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
      </tr>
      <tr style={{ borderTop: '1px solid #f3f4f6' }}>
        <td style={{ padding: '16px', color: '#6b7280', fontSize: 12, textTransform: 'uppercase', verticalAlign: 'top' }}>Neutral</td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="ordered" style="neutral" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="unordered" style="neutral" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="with-icon" style="neutral" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
      </tr>
      <tr style={{ borderTop: '1px solid #f3f4f6', background: '#000b36' }}>
        <td style={{ padding: '16px', color: '#6b7280', fontSize: 12, textTransform: 'uppercase', verticalAlign: 'top' }}>On-Color</td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="ordered" style="on-color" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="unordered" style="on-color" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
        <td style={{ padding: '16px', verticalAlign: 'top' }}>
          <List type="with-icon" style="on-color" items={[{ children: 'List item', level: 1 }, { children: 'List item', level: 2 }, { children: 'List item', level: 2 }]} />
        </td>
      </tr>
    </tbody>
  </table>
</div>

---

{/* ── Props ─────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 24 }}>Props</h2>

  <h3 style={{ fontSize: 16, fontWeight: 600, color: '#111827', marginBottom: 16 }}>ListItem</h3>
  <table style={{ borderCollapse: 'collapse', fontSize: 13, width: '100%', marginBottom: 32 }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Prop</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Type</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Default</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Description</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['children', 'string', '—', 'The label text of the list item.'],
        ['type', "'ordered' | 'unordered' | 'with-icon'", "'unordered'", 'Determines the marker style.'],
        ['level', '1 | 2', '1', 'Level One = main item (no indent). Level Two = sub-item (24 px indent).'],
        ['style', "'primary' | 'neutral' | 'on-color'", "'primary'", 'Colour variant.'],
        ['rtl', 'boolean', 'false', 'Right-to-left layout — mirrors the marker position.'],
        ['marker', 'string', 'auto', 'Override the auto-generated marker text (e.g. "1-", "a-", "-").'],
        ['icon', 'string', "'chevron-right'", "Icon name used when type='with-icon'."],
      ].map(([prop, type, def, desc]) => (
        <tr key={prop} style={{ borderTop: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#1849a9' }}>{prop}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#6b7280', fontSize: 12 }}>{type}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#374151' }}>{def}</td>
          <td style={{ padding: '10px 12px', color: '#374151' }}>{desc}</td>
        </tr>
      ))}
    </tbody>
  </table>

  <h3 style={{ fontSize: 16, fontWeight: 600, color: '#111827', marginBottom: 16 }}>List</h3>
  <table style={{ borderCollapse: 'collapse', fontSize: 13, width: '100%' }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Prop</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Type</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Default</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Description</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['items', 'Array<{ children: string, level?: 1|2 }>', '[]', 'List content. level defaults to 1 when omitted.'],
        ['type', "'ordered' | 'unordered' | 'with-icon'", "'unordered'", 'List type — controls markers for all items.'],
        ['style', "'primary' | 'neutral' | 'on-color'", "'primary'", 'Colour variant applied to every item.'],
        ['rtl', 'boolean', 'false', 'Right-to-left layout — applied to every item.'],
        ['icon', 'string', "'chevron-right'", "Icon name used when type='with-icon'."],
      ].map(([prop, type, def, desc]) => (
        <tr key={prop} style={{ borderTop: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#1849a9' }}>{prop}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#6b7280', fontSize: 12 }}>{type}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#374151' }}>{def}</td>
          <td style={{ padding: '10px 12px', color: '#374151' }}>{desc}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

{/* ── Design Tokens ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 24 }}>Design Tokens</h2>
  <table style={{ borderCollapse: 'collapse', fontSize: 13, width: '100%' }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Token</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Value</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Usage</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['Primary color', '#1849A9', 'Marker + label in Primary style'],
        ['Neutral color', '#000B36', 'Marker + label in Neutral style'],
        ['On-Color', '#FFFFFF', 'Marker + label in On-Color style — use on dark surfaces'],
        ['Font', 'IBM Plex Sans Arabic', 'All text, LTR and RTL'],
        ['Font size', '16 px', 'Body text'],
        ['Font weight', '400', 'Regular'],
        ['Line height', '24 px', 'Single row height'],
        ['Level Two indent', '24 px', 'padding-left (LTR) / padding-right (RTL)'],
        ['Marker–text gap', '8 px', 'gap inside each list row'],
        ['Icon size', '16 px', 'with-icon marker'],
      ].map(([token, value, usage]) => (
        <tr key={token} style={{ borderTop: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 12px', color: '#374151', fontWeight: 500 }}>{token}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#1849a9' }}>{value}</td>
          <td style={{ padding: '10px 12px', color: '#6b7280' }}>{usage}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

{/* ── Usage Examples ────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 24 }}>Usage Examples</h2>

  <h3 style={{ fontSize: 16, fontWeight: 600, color: '#111827', marginBottom: 12 }}>Standalone ListItem</h3>
  <pre style={{ background: '#f9fafb', border: '1px solid #e5e7eb', borderRadius: 6, padding: 20, fontSize: 13, lineHeight: 1.7, overflow: 'auto' }}>{`import { ListItem } from './Lists';

// Ordered, level 1, primary
<ListItem type="ordered" level={1} style="primary" marker="1-">
  First list item
</ListItem>

// Unordered, level 2 sub-item, neutral
<ListItem type="unordered" level={2} style="neutral">
  Sub-item
</ListItem>

// With icon, on dark background
<ListItem type="with-icon" style="on-color">
  List item
</ListItem>

// RTL
<ListItem type="ordered" level={1} style="primary" rtl marker="1-">
  عنصر قائمة
</ListItem>`}</pre>

  <h3 style={{ fontSize: 16, fontWeight: 600, color: '#111827', marginBottom: 12, marginTop: 32 }}>Full List (auto-numbering)</h3>
  <pre style={{ background: '#f9fafb', border: '1px solid #e5e7eb', borderRadius: 6, padding: 20, fontSize: 13, lineHeight: 1.7, overflow: 'auto' }}>{`import { List } from './Lists';

// Ordered list with level 1 + sub-items
<List
  type="ordered"
  style="primary"
  items={[
    { children: 'First item',  level: 1 },
    { children: 'Sub-item A', level: 2 },
    { children: 'Sub-item B', level: 2 },
    { children: 'Second item', level: 1 },
  ]}
/>
// Renders:  1-  First item
//               a-  Sub-item A
//               b-  Sub-item B
//           2-  Second item

// On dark background
<div style={{ background: '#000B36', padding: 16 }}>
  <List type="unordered" style="on-color" items={[
    { children: 'List item', level: 1 },
    { children: 'Sub-item',  level: 2 },
  ]} />
</div>

// RTL Arabic list
<List
  type="ordered"
  style="primary"
  rtl
  items={[
    { children: 'عنصر قائمة', level: 1 },
    { children: 'عنصر قائمة', level: 2 },
  ]}
/>`}</pre>
</div>

---

{/* ── Accessibility ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 16 }}>Accessibility</h2>
  <ul style={{ fontSize: 14, color: '#374151', lineHeight: 1.8, paddingLeft: 20 }}>
    <li>Markers and icons are wrapped in <code>aria-hidden="true"</code> — screen readers announce the label only, not the visual marker character.</li>
    <li>The <code>rtl</code> prop sets <code>dir="rtl"</code> on each item, ensuring correct bidirectional text rendering and reading order for screen readers.</li>
    <li>The <code>List</code> component renders as a plain <code>div</code> container; wrap it in a semantic <code>&lt;ul&gt;</code> / <code>&lt;ol&gt;</code> if the document structure requires native list semantics.</li>
  </ul>
</div>

</div>
