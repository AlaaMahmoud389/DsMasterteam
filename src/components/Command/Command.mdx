import { Meta } from '@storybook/addon-docs/blocks';
import * as CommandStories from './Command.stories';
import {
  Command,
  CommandInput,
  CommandMenuItem,
  CommandMenuSection,
  CommandFooter,
} from './Command';

<Meta of={CommandStories} />

{/* ─── Header ─────────────────────────────────────────────────── */}

<div style={{ marginBottom: 8, fontSize: 12, color: '#6b7280' }}>Components / Command</div>

# Command

A full-featured command palette for quick navigation, search, and action execution. Supports keyboard navigation, real-time filtering, shortcut badges, section grouping, RTL layout, and a footer with key-hint hints.

<div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginTop: 12, marginBottom: 32 }}>
  <span style={{ fontSize: 12, fontWeight: 700, padding: '3px 10px', borderRadius: 20, background: '#d1fae5', color: '#059669' }}>Available</span>
  <span style={{ fontSize: 12, fontWeight: 700, padding: '3px 10px', borderRadius: 20, background: '#ede9fe', color: '#6d28d9' }}>Node 4747:83322</span>
  <span style={{ fontSize: 12, fontWeight: 700, padding: '3px 10px', borderRadius: 20, background: '#dbeafe', color: '#1849a9' }}>DGA Guidelines</span>
  <span style={{ fontSize: 12, fontWeight: 700, padding: '3px 10px', borderRadius: 20, background: '#f3f4f6', color: '#374151' }}>Figma</span>
</div>

{/* ─── On this page ───────────────────────────────────────────── */}

<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '14px 20px', marginBottom: 24 }}>
  <div style={{ fontSize: 12, fontWeight: 700, color: '#1849a9', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.07em' }}>On this page</div>
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '4px 16px' }}>
    {[['Overview','#overview'],['Anatomy','#anatomy'],['Input States','#input-states'],['Menu Item Types','#menu-item-types'],['Menu Item States','#menu-item-states'],['Stacked Text','#stacked-text'],['Shortcut Keys','#shortcut-keys'],['RTL Support','#rtl-support'],['Props Reference','#props-reference'],['Token Table','#token-table'],['Do / Don\'t','#do--dont'],['Accessibility','#accessibility']].map(([label, href]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none', lineHeight: 2 }}>{label}</a>
    ))}
  </div>
</div>

---

## Overview

<div id="overview" style={{ border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden', padding: 24, background: '#f8fafc', marginBottom: 32 }}>
  <div style={{ width: 640, margin: '0 auto' }}>
    <Command
      sections={[
        { heading: 'Recent', items: [
          { type: 'icon', label: 'Marketing site redesign', supportingText: null, shortcut: null },
          { type: 'icon', label: 'Create new document', supportingText: null, shortcut: '⌘N' },
          { type: 'icon', label: 'Invite colleagues', supportingText: null, shortcut: '⌘I' },
        ]},
        { items: [
          { type: 'icon', label: 'My profile',         supportingText: null, shortcut: ['⌘K', 'P'] },
          { type: 'icon', label: 'Team profile',       supportingText: null, shortcut: ['⌘K', 'T'] },
          { type: 'icon', label: 'Keyboard shortcuts', supportingText: null, shortcut: '⌘?' },
        ]},
      ]}
      footer
      autoFocus={false}
    />
  </div>
</div>

## Anatomy

<div id="anatomy" style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 12, marginBottom: 32 }}>
  {[
    { n: '1', title: 'Search Input',      desc: 'Text field for filtering items in real time' },
    { n: '2', title: 'Input Shortcut',    desc: '⌘/ badge to open/focus the command palette' },
    { n: '3', title: 'Help Icon',         desc: 'Contextual help affordance in the input row' },
    { n: '4', title: 'Section Header',    desc: 'Label grouping items (e.g. "Recent")' },
    { n: '5', title: 'Menu Item',         desc: 'Selectable row with icon/dot, label, and optional shortcut' },
    { n: '6', title: 'Leading Indicator', desc: 'Avatar icon (Icon type) or status dot (Dot type)' },
    { n: '7', title: 'Shortcut Badge',    desc: 'Single or dual keyboard shortcut hint on each row' },
    { n: '8', title: 'Footer',            desc: 'Keyboard hint bar: navigate · select · close · return' },
  ].map(({ n, title, desc }) => (
    <div key={n} style={{ border: '1px solid #e5e7eb', borderRadius: 10, padding: 16, background: '#fff' }}>
      <div style={{ width: 28, height: 28, borderRadius: '50%', background: '#1849a9', color: '#fff', fontSize: 13, fontWeight: 700, display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: 10 }}>{n}</div>
      <div style={{ fontSize: 13, fontWeight: 700, color: '#111827', marginBottom: 4 }}>{title}</div>
      <div style={{ fontSize: 12, color: '#6b7280', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

## Input States

<div id="input-states" style={{ display: 'flex', flexDirection: 'column', gap: 20, marginBottom: 32, maxWidth: 576 }}>
  {[
    { state: 'placeholder', value: '',            label: 'Placeholder', note: 'Default empty state' },
    { state: 'filled',      value: 'Olivia Rhye', label: 'Filled',      note: 'Value present, text darkens' },
    { state: 'focus',       value: 'Olivia Rhye', label: 'Focus',       note: 'Focus ring: 0 0 0 4px #f2f4f7' },
  ].map(({ state, value, label, note }) => (
    <div key={state}>
      <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', marginBottom: 4, textTransform: 'uppercase', letterSpacing: '.06em' }}>{label} — <span style={{ fontWeight: 400, textTransform: 'none' }}>{note}</span></div>
      <div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', background: '#fff' }}>
        <CommandInput state={state} value={value} />
      </div>
    </div>
  ))}
  <div>
    <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', marginBottom: 4, textTransform: 'uppercase', letterSpacing: '.06em' }}>RTL — Placeholder</div>
    <div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', background: '#fff' }}>
      <CommandInput state="placeholder" value="" rtl />
    </div>
  </div>
</div>

## Menu Item Types

<div id="menu-item-types" style={{ display: 'flex', gap: 24, flexWrap: 'wrap', marginBottom: 32 }}>
  {[
    { type: 'icon', heading: 'Icon Leading', desc: 'Avatar icon placeholder — 24 × 24 px rounded square' },
    { type: 'dot',  heading: 'Dot Leading',  desc: 'Status dot indicator — 10 × 10 px colored dot' },
  ].map(({ type, heading, desc }) => (
    <div key={type} style={{ flex: '0 0 280px' }}>
      <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', marginBottom: 4, textTransform: 'uppercase', letterSpacing: '.06em' }}>{heading}</div>
      <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 8 }}>{desc}</div>
      <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
        <CommandMenuItem type={type} state="default" label="Olivia Rhye" supportingText="@olivia" />
        <CommandMenuItem type={type} state="hover"   label="Olivia Rhye" supportingText="@olivia" shortcut="⌘C" />
      </div>
    </div>
  ))}
</div>

## Menu Item States

<div id="menu-item-states" style={{ display: 'flex', flexDirection: 'column', gap: 12, maxWidth: 400, marginBottom: 32 }}>
  {[
    { label: 'Default',     state: 'default', type: 'icon', note: 'No background' },
    { label: 'Hover',       state: 'hover',   type: 'icon', note: 'Background: #eff8ff' },
    { label: 'Focus',       state: 'focus',   type: 'icon', note: 'White bg + 4px ring #f2f4f7' },
    { label: 'Dot Default', state: 'default', type: 'dot',  note: '' },
    { label: 'Dot Hover',   state: 'hover',   type: 'dot',  note: '' },
    { label: 'Dot Focus',   state: 'focus',   type: 'dot',  note: '' },
  ].map(({ label, state, type, note }) => (
    <div key={label}>
      <div style={{ fontSize: 10, color: '#9ca3af', marginBottom: 4, letterSpacing: '.04em', textAlign: 'center' }}>{label}{note && ` — ${note}`}</div>
      <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
        <CommandMenuItem type={type} state={state} label="Olivia Rhye" supportingText="@olivia" shortcut="⌘C" />
      </div>
    </div>
  ))}
</div>

## Stacked Text

<div id="stacked-text" style={{ display: 'flex', gap: 24, flexWrap: 'wrap', marginBottom: 32 }}>
  {['icon', 'dot'].map(type => (
    <div key={type} style={{ flex: '0 0 300px', background: '#fff', border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden' }}>
      <div style={{ padding: '8px 12px', borderBottom: '1px solid #f3f4f6', fontSize: 11, fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em' }}>{type} leading — stacked</div>
      {['default', 'hover', 'focus'].map(state => (
        <CommandMenuItem
          key={state}
          type={type}
          state={state}
          text="stacked"
          label="Olivia Rhye"
          supportingText="@olivia"
          shortcut={state !== 'default' ? '⌘C' : undefined}
        />
      ))}
    </div>
  ))}
</div>

## Shortcut Keys

<div id="shortcut-keys" style={{ marginBottom: 32 }}>
  <div style={{ fontSize: 13, color: '#6b7280', marginBottom: 16, lineHeight: 1.6 }}>
    Shortcut badges appear on the trailing edge (LTR) or leading edge (RTL) of menu items. Single shortcuts use one key badge; dual shortcuts show two keys separated by an arrow, indicating a key sequence.
  </div>
  <div style={{ display: 'flex', gap: 24, flexWrap: 'wrap' }}>
    <div style={{ flex: '0 0 340px' }}>
      <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Single Shortcut</div>
      <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden' }}>
        <CommandMenuItem type="icon" state="hover"   label="Create new document"  shortcut="⌘N" />
        <CommandMenuItem type="icon" state="hover"   label="Invite colleagues"     shortcut="⌘I" />
        <CommandMenuItem type="icon" state="hover"   label="Keyboard shortcuts"    shortcut="⌘?" />
        <CommandMenuItem type="icon" state="default" label="Changelog"             shortcut="⌘C" />
        <CommandMenuItem type="icon" state="default" label="Support"               shortcut="⌘H" />
      </div>
    </div>
    <div style={{ flex: '0 0 340px' }}>
      <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Dual Shortcut (Key Sequence)</div>
      <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden' }}>
        <CommandMenuItem type="icon" state="default" label="My profile"    shortcut={['⌘K', 'P']} />
        <CommandMenuItem type="icon" state="default" label="Team profile"  shortcut={['⌘K', 'T']} />
        <CommandMenuItem type="icon" state="hover"   label="My profile"    shortcut={['⌘K', 'P']} />
        <CommandMenuItem type="icon" state="focus"   label="Team profile"  shortcut={['⌘K', 'T']} />
      </div>
    </div>
  </div>
</div>

## RTL Support

<div id="rtl-support" style={{ display: 'flex', gap: 32, flexWrap: 'wrap', marginBottom: 32 }}>
  <div>
    <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>LTR</div>
    <div style={{ width: 520 }}>
      <Command
        sections={[{ heading: 'Recent', items: [
          { type: 'icon', label: 'Marketing site redesign', shortcut: null },
          { type: 'icon', label: 'Create new document',     shortcut: '⌘N' },
        ]}, { items: [
          { type: 'icon', label: 'My profile',  shortcut: ['⌘K', 'P'] },
          { type: 'icon', label: 'Team profile',shortcut: ['⌘K', 'T'] },
        ]}]}
        footer
        autoFocus={false}
      />
    </div>
  </div>
  <div>
    <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>RTL</div>
    <div style={{ width: 520 }}>
      <Command
        sections={[{ heading: 'حديث', items: [
          { type: 'icon', label: 'إعادة تصميم موقع التسويق', shortcut: null },
          { type: 'icon', label: 'إنشاء مستند جديد',          shortcut: '⌘N' },
        ]}, { items: [
          { type: 'icon', label: 'ملفي الشخصي',  shortcut: ['⌘K', 'P'] },
          { type: 'icon', label: 'ملف الفريق',   shortcut: ['⌘K', 'T'] },
        ]}]}
        footer
        rtl
        autoFocus={false}
      />
    </div>
  </div>
</div>

## Props Reference

<div id="props-reference" style={{ marginBottom: 32 }}>

### Command

<div style={{ display: 'grid', gridTemplateColumns: '180px 130px 110px 100px 1fr', gap: 0, marginBottom: 24, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
<div style={{ display: 'contents' }}>
  {['Prop','Type','Default','Required','Description'].map((h, i) => (
    <div key={h} style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
  ))}
</div>
{[
  ['sections',    'array',    '[]',      'No',  'Array of { heading?, items[] } section objects','#fdf4ff','#7c3aed'],
  ['placeholder', 'string',   '"Search"','No',  'Input placeholder text','#f0f4ff','#1849a9'],
  ['footer',      'boolean',  'true',    'No',  'Show keyboard hint footer','#f0fdf4','#059669'],
  ['rtl',         'boolean',  'false',   'No',  'Enable right-to-left layout','#f0fdf4','#059669'],
  ['autoFocus',   'boolean',  'true',    'No',  'Auto-focus the input on mount','#f0fdf4','#059669'],
  ['onSelect',    'function', '—',       'No',  'Callback fired with selected item on Enter or click','#f8fafc','#374151'],
  ['onClose',     'function', '—',       'No',  'Callback fired when Escape is pressed','#f8fafc','#374151'],
  ['className',   'string',   '—',       'No',  'Extra class on the outer wrapper','#f8fafc','#374151'],
  ['style',       'object',   '—',       'No',  'Inline styles on the outer wrapper','#f8fafc','#374151'],
].map(([prop, type, def, req, desc, bg, color]) => (
  <div key={prop} style={{ display: 'contents' }}>
    <div style={{ padding: '9px 14px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#7c3aed' }}>{type}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#6b7280' }}>{def}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
      <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: req === 'Yes' ? '#fee2e2' : '#f3f4f6', color: req === 'Yes' ? '#dc2626' : '#6b7280' }}>{req}</span>
    </div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4 }}>{desc}</div>
  </div>
))}
</div>

### CommandMenuItem

<div style={{ display: 'grid', gridTemplateColumns: '180px 130px 110px 100px 1fr', gap: 0, marginBottom: 24, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
<div style={{ display: 'contents' }}>
  {['Prop','Type','Default','Required','Description'].map((h, i) => (
    <div key={h} style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
  ))}
</div>
{[
  ['type',          '"icon"|"dot"',              '"icon"',     'No',  'Leading indicator type','#fff7ed','#c2410c'],
  ['state',         '"default"|"hover"|"focus"', '"default"',  'No',  'Visual state of the row','#fff7ed','#c2410c'],
  ['text',          '"default"|"stacked"',       '"default"',  'No',  'Layout: inline label+caption vs stacked','#f0f4ff','#1849a9'],
  ['size',          '"sm"|"md"',                 '"sm"',       'No',  'Item height (sm = compact, md = spacious)','#f8fafc','#374151'],
  ['label',         'string',                    '"Olivia Rhye"','No','Primary text label','#f0f4ff','#1849a9'],
  ['supportingText','string',                    '"@olivia"',  'No',  'Secondary caption text','#f0f4ff','#1849a9'],
  ['shortcut',      'string | [k1, k2]',         '—',          'No',  'Keyboard shortcut badge; array = dual sequence','#f8fafc','#374151'],
  ['rtl',           'boolean',                   'false',      'No',  'Right-to-left layout (shortcut on start side)','#f0fdf4','#059669'],
  ['active',        'boolean',                   'false',      'No',  'Keyboard-active highlight (focus ring)','#f0fdf4','#059669'],
  ['onClick',       'function',                  '—',          'No',  'Click handler','#f8fafc','#374151'],
].map(([prop, type, def, req, desc, bg, color]) => (
  <div key={prop} style={{ display: 'contents' }}>
    <div style={{ padding: '9px 14px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#7c3aed' }}>{type}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#6b7280' }}>{def}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
      <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: req === 'Yes' ? '#fee2e2' : '#f3f4f6', color: req === 'Yes' ? '#dc2626' : '#6b7280' }}>{req}</span>
    </div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4 }}>{desc}</div>
  </div>
))}
</div>

### CommandInput

<div style={{ display: 'grid', gridTemplateColumns: '180px 130px 110px 100px 1fr', gap: 0, marginBottom: 24, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
<div style={{ display: 'contents' }}>
  {['Prop','Type','Default','Required','Description'].map((h, i) => (
    <div key={h} style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
  ))}
</div>
{[
  ['state',       '"placeholder"|"filled"|"focus"', '"placeholder"', 'No', 'Visual state of the input row','#fff7ed','#c2410c'],
  ['value',       'string',  '""',       'No', 'Controlled input value','#f0f4ff','#1849a9'],
  ['placeholder', 'string',  '"Search"', 'No', 'Input placeholder text','#f0f4ff','#1849a9'],
  ['rtl',         'boolean', 'false',    'No', 'Right-to-left layout (search icon on right)','#f0fdf4','#059669'],
  ['autoFocus',   'boolean', 'false',    'No', 'Auto-focus input on mount','#f0fdf4','#059669'],
  ['onSearch',    'function','—',        'No', 'onChange callback, receives string value','#f8fafc','#374151'],
  ['onKeyDown',   'function','—',        'No', 'keyDown handler (forwarded to native input)','#f8fafc','#374151'],
].map(([prop, type, def, req, desc, bg, color]) => (
  <div key={prop} style={{ display: 'contents' }}>
    <div style={{ padding: '9px 14px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#7c3aed' }}>{type}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#6b7280' }}>{def}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
      <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: req === 'Yes' ? '#fee2e2' : '#f3f4f6', color: req === 'Yes' ? '#dc2626' : '#6b7280' }}>{req}</span>
    </div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4 }}>{desc}</div>
  </div>
))}
</div>

</div>

## Token Table

<div id="token-table" style={{ display: 'flex', flexDirection: 'column', gap: 0, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 32 }}>

{/* Group header */}
<div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '2px solid #e5e7eb', fontSize: 12, fontWeight: 700, color: '#374151', display: 'grid', gridTemplateColumns: '240px 140px 1fr', gap: 0 }}>
  <div>Token</div>
  <div style={{ borderLeft: '1px solid #e5e7eb', paddingLeft: 14 }}>Value</div>
  <div style={{ borderLeft: '1px solid #e5e7eb', paddingLeft: 14 }}>Usage</div>
</div>

{/* Container & Layout */}
<div style={{ padding: '8px 16px', background: '#f0f4ff', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, color: '#1849a9', textTransform: 'uppercase', letterSpacing: '.07em' }}>Container &amp; Layout</div>
{[
  ['--command/background/default', '#ffffff',   'Bar background, item default bg'],
  ['--command/background/hover',   '#eff8ff',   'Item hover state background'],
  ['--command/background/focus',   '#f0e8f4',   'Item focus state background (active)'],
  ['--command/border/default',     '#e5e7eb',   'Section dividers, footer top border'],
  ['--command/icon/command-bg',    '#f3f4f6',   'Input shortcut badge, dual shortcut key bg'],
].map(([token, value, usage]) => (
  <div key={token} style={{ display: 'grid', gridTemplateColumns: '240px 140px 1fr', borderBottom: '1px solid #f3f4f6', alignItems: 'center' }}>
    <div style={{ padding: '9px 16px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9', fontWeight: 600 }}>{token}</div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', display: 'flex', alignItems: 'center', gap: 8 }}>
      <div style={{ width: 16, height: 16, borderRadius: 4, background: value, border: '1px solid #e5e7eb', flexShrink: 0 }} />
      <code style={{ fontSize: 11, color: '#6b7280' }}>{value}</code>
    </div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', fontSize: 12, color: '#374151' }}>{usage}</div>
  </div>
))}

{/* Typography & Colors */}
<div style={{ padding: '8px 16px', background: '#f0f4ff', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, color: '#1849a9', textTransform: 'uppercase', letterSpacing: '.07em' }}>Typography &amp; Colors</div>
{[
  ['--command/text/primary',       '#000b36',   'Primary item label, filled input text'],
  ['--command/text/secondary',     '#6c7c96',   'Supporting text, section heading, footer text'],
  ['Font — input/label',           '16px/24px', 'Input field text: 16px, weight 400, line-height 24px'],
  ['Font — item label',            '14px/20px', 'Item primary text: 14px, weight 600'],
  ['Font — supporting text',       '14px/20px', 'Item secondary text: 14px, weight 400'],
  ['Font — section heading',       '14px/20px', 'Section label: 14px, weight 400'],
  ['Font — shortcut badge',        '14px/20px', 'Shortcut key badge text: 14px'],
].map(([token, value, usage]) => (
  <div key={token} style={{ display: 'grid', gridTemplateColumns: '240px 140px 1fr', borderBottom: '1px solid #f3f4f6', alignItems: 'center' }}>
    <div style={{ padding: '9px 16px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9', fontWeight: 600 }}>{token}</div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', display: 'flex', alignItems: 'center', gap: 8 }}>
      {value.startsWith('#') && <div style={{ width: 16, height: 16, borderRadius: 4, background: value, border: '1px solid #e5e7eb', flexShrink: 0 }} />}
      <code style={{ fontSize: 11, color: '#6b7280' }}>{value}</code>
    </div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', fontSize: 12, color: '#374151' }}>{usage}</div>
  </div>
))}

{/* Sizing */}
<div style={{ padding: '8px 16px', background: '#f0f4ff', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, color: '#1849a9', textTransform: 'uppercase', letterSpacing: '.07em' }}>Sizing</div>
{[
  ['Bar — border-radius',         '12px',      'Outer wrapper rounded corners'],
  ['Item — border-radius',        '8px',       'Item content rounded corners'],
  ['Input — padding',             '16px',      'Input row uniform padding'],
  ['Section — padding (block)',   '16px 0',    'Section top/bottom padding'],
  ['Item — padding',              '2px 8px',   'Menu item outer padding'],
  ['Item content — padding (sm)', '8px 8px 8px 10px', 'Inner content area (default)'],
  ['Item content — padding (stacked)', '10px 10px 10px 14px', 'Inner content (stacked variant)'],
  ['Avatar icon size (default)',  '24 × 24 px','Leading avatar icon'],
  ['Avatar icon size (stacked)',  '32 × 32 px','Stacked avatar icon'],
  ['Focus ring',                  '0 0 0 4px #f2f4f7','Input focus ring and item active ring'],
].map(([token, value, usage]) => (
  <div key={token} style={{ display: 'grid', gridTemplateColumns: '240px 140px 1fr', borderBottom: '1px solid #f3f4f6', alignItems: 'center' }}>
    <div style={{ padding: '9px 16px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9', fontWeight: 600 }}>{token}</div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6' }}>
      <code style={{ fontSize: 11, color: '#6b7280' }}>{value}</code>
    </div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', fontSize: 12, color: '#374151' }}>{usage}</div>
  </div>
))}

</div>

## Do / Don't

<div id="do--dont" style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 32 }}>

<div>
  <div style={{ fontSize: 13, fontWeight: 700, color: '#059669', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 6 }}>
    <span style={{ background: '#d1fae5', borderRadius: 4, padding: '2px 8px' }}>Do</span>
    Use sections to group related actions
  </div>
  <div style={{ border: '2px solid #d1fae5', borderRadius: 12, overflow: 'hidden', padding: 16, background: '#f0fdf4' }}>
    <Command
      sections={[
        { heading: 'Recent', items: [
          { type: 'icon', label: 'Marketing site redesign', shortcut: null },
          { type: 'icon', label: 'Create new document',     shortcut: '⌘N' },
        ]},
        { heading: 'Actions', items: [
          { type: 'icon', label: 'Invite colleagues', shortcut: '⌘I' },
          { type: 'icon', label: 'Support',           shortcut: '⌘H' },
        ]},
      ]}
      footer={false}
      autoFocus={false}
    />
  </div>
</div>

<div>
  <div style={{ fontSize: 13, fontWeight: 700, color: '#dc2626', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 6 }}>
    <span style={{ background: '#fee2e2', borderRadius: 4, padding: '2px 8px' }}>Don't</span>
    Dump all items into one flat list with no sections
  </div>
  <div style={{ border: '2px solid #fecaca', borderRadius: 12, overflow: 'hidden', padding: 16, background: '#fff5f5' }}>
    <Command
      sections={[
        { items: [
          { type: 'icon', label: 'Marketing site redesign', shortcut: null },
          { type: 'icon', label: 'Create new document',     shortcut: '⌘N' },
          { type: 'icon', label: 'Invite colleagues',       shortcut: '⌘I' },
          { type: 'icon', label: 'Support',                 shortcut: '⌘H' },
          { type: 'icon', label: 'Keyboard shortcuts',      shortcut: '⌘?' },
          { type: 'icon', label: 'Changelog',               shortcut: '⌘C' },
        ]},
      ]}
      footer={false}
      autoFocus={false}
    />
  </div>
</div>

<div>
  <div style={{ fontSize: 13, fontWeight: 700, color: '#059669', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 6 }}>
    <span style={{ background: '#d1fae5', borderRadius: 4, padding: '2px 8px' }}>Do</span>
    Assign consistent shortcut badges to common actions
  </div>
  <div style={{ border: '2px solid #d1fae5', borderRadius: 12, overflow: 'hidden', background: '#f0fdf4', padding: 16 }}>
    <div style={{ background: '#fff', borderRadius: 10, overflow: 'hidden', border: '1px solid #e5e7eb' }}>
      <CommandMenuItem type="icon" state="hover"   label="Create new document"  shortcut="⌘N" />
      <CommandMenuItem type="icon" state="default" label="Invite colleagues"     shortcut="⌘I" />
      <CommandMenuItem type="icon" state="default" label="Keyboard shortcuts"    shortcut="⌘?" />
    </div>
  </div>
</div>

<div>
  <div style={{ fontSize: 13, fontWeight: 700, color: '#dc2626', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 6 }}>
    <span style={{ background: '#fee2e2', borderRadius: 4, padding: '2px 8px' }}>Don't</span>
    Show shortcuts for every item — reserve them for frequent actions
  </div>
  <div style={{ border: '2px solid #fecaca', borderRadius: 12, overflow: 'hidden', background: '#fff5f5', padding: 16 }}>
    <div style={{ background: '#fff', borderRadius: 10, overflow: 'hidden', border: '1px solid #e5e7eb' }}>
      <CommandMenuItem type="icon" state="default" label="Marketing site redesign" shortcut="⌘1" />
      <CommandMenuItem type="icon" state="default" label="Q4 Budget Spreadsheet"   shortcut="⌘2" />
      <CommandMenuItem type="icon" state="default" label="Team onboarding doc"      shortcut="⌘3" />
    </div>
  </div>
</div>

</div>

## Accessibility

<div id="accessibility" style={{ marginBottom: 32 }}>

<div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', marginBottom: 20 }}>
  {[
    { label: 'Keyboard Navigation', color: '#dbeafe', text: '#1849a9' },
    { label: 'Screen Reader',       color: '#ede9fe', text: '#6d28d9' },
    { label: 'Focus Management',    color: '#d1fae5', text: '#059669' },
    { label: 'ARIA Roles',          color: '#fff7ed', text: '#c2410c' },
    { label: 'RTL',                 color: '#f3f4f6', text: '#374151' },
  ].map(({ label, color, text }) => (
    <div key={label} style={{ padding: '6px 14px', borderRadius: 20, background: color, color: text, fontSize: 12, fontWeight: 700 }}>{label}</div>
  ))}
</div>

<div style={{ display: 'flex', flexDirection: 'column', gap: 12, marginBottom: 20 }}>
  {[
    { cat: 'Keyboard Navigation', items: [
      'Arrow Down / Arrow Up — move focus through filtered results (wraps at boundaries)',
      'Enter — selects the currently active item and fires onSelect',
      'Escape — closes the palette and fires onClose',
    ]},
    { cat: 'ARIA Roles', items: [
      'Input carries role="combobox" semantics via aria-label',
      'Item list uses role="listbox"; each item uses role="option" with aria-selected',
      'Section groups use role="group" with aria-label set to the heading',
    ]},
    { cat: 'Focus Management', items: [
      'autoFocus={true} places focus inside the input on mount for immediate keyboard use',
      'Active item is styled with a focus ring (box-shadow: 0 0 0 4px #f2f4f7)',
    ]},
    { cat: 'RTL', items: [
      'dir="rtl" on the bar flips icon positions, shortcut badge sides, and footer layout',
      'Arabic section headings rendered verbatim when RTL locale is detected',
    ]},
  ].map(({ cat, items }) => (
    <div key={cat} style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
      <div style={{ padding: '8px 14px', background: '#f8fafc', borderBottom: '1px solid #e5e7eb', fontSize: 12, fontWeight: 700, color: '#374151' }}>{cat}</div>
      <div style={{ padding: '10px 16px', display: 'flex', flexDirection: 'column', gap: 6 }}>
        {items.map(item => (
          <div key={item} style={{ fontSize: 13, color: '#374151', lineHeight: 1.5, display: 'flex', gap: 8 }}>
            <span style={{ color: '#059669', flexShrink: 0 }}>–</span>
            <span>{item}</span>
          </div>
        ))}
      </div>
    </div>
  ))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 8 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '160px 60px 1fr 100px', gap: 0 }}>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb' }}>Criterion</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>Level</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb' }}>Requirement</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>Status</div>
    {[
      ['1.3.1 Info & Relationships', 'A',  'Lists and groups conveyed through ARIA role attributes (listbox, option, group)'],
      ['1.4.3 Contrast',             'AA', 'Primary text #000b36 on white exceeds 7:1; secondary #6c7c96 meets 4.5:1'],
      ['2.1.1 Keyboard',             'A',  'All functionality accessible via keyboard (Arrow keys, Enter, Escape)'],
      ['2.4.3 Focus Order',          'A',  'Focus follows logical DOM order as items are navigated with keyboard'],
      ['2.4.7 Focus Visible',        'AA', 'Active item displayed with 4px focus ring; input receives visible focus on mount'],
      ['4.1.2 Name, Role, Value',    'A',  'Input has aria-label; options have role="option" and aria-selected state'],
    ].map(([criterion, level, req]) => (
      <div key={criterion} style={{ display: 'contents' }}>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', fontFamily: 'monospace', fontSize: 12, color: '#1849a9', fontWeight: 600 }}>{criterion}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: level === 'AAA' ? '#ede9fe' : '#dbeafe', color: level === 'AAA' ? '#6d28d9' : '#1849a9' }}>{level}</span>
        </div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', fontSize: 12, color: '#374151', lineHeight: 1.4 }}>{req}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: '#d1fae5', color: '#059669' }}>✓ Pass</span>
        </div>
      </div>
    ))}
  </div>
</div>

</div>
