import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as FileUploadStories from './FileUpload.stories';

<Meta of={FileUploadStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif", padding: '32px 0', maxWidth: 860 }}>

{/* ── Header ── */}
<div style={{ marginBottom: 32 }}>
  <p style={{ margin: '0 0 4px', fontSize: 13, color: '#6b7280' }}>Components</p>
  <h1 style={{ margin: '0 0 10px', fontSize: 28, fontWeight: 700, color: '#000b36', lineHeight: 1.25 }}>
    File Upload
  </h1>
  <p style={{ margin: '0 0 16px', fontSize: 15, color: '#374151', lineHeight: 1.6, maxWidth: 700 }}>
    File uploader facilitates selecting and uploading files from the user's device to a web platform.
    Users initiate the process through a designated area, review selections, and monitor upload progress.
    Four composable component sets: <strong>_File</strong>, <strong>_Drop Zone</strong>,{' '}
    <strong>File Upload / Single</strong>, and <strong>File Upload / Multiple</strong>.
  </p>
  <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6, background: '#d1fae5', color: '#065f46', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#10b981', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>4 Component Sets</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>3 File States</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>Drag &amp; Drop Support</span>
  </div>
</div>

{/* ── Table of Contents ── */}
<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 10, padding: '20px 24px', marginBottom: 40 }}>
  <p style={{ margin: '0 0 10px', fontSize: 13, fontWeight: 700, color: '#000b36' }}>On this page</p>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
    {[
      ['#overview',  'Overview'],
      ['#file-item', '_File'],
      ['#drop-zone', '_Drop Zone'],
      ['#single',    'File Upload / Single'],
      ['#multiple',  'File Upload / Multiple'],
      ['#tokens',    'Design Tokens'],
    ].map(([href, label]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none' }}>{label}</a>
    ))}
  </div>
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* OVERVIEW                                                       */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="overview" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Overview</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  The four component sets are independently composable. Use <strong>_File</strong> and <strong>_Drop Zone</strong> as primitives when building custom upload UIs, or use <strong>File Upload / Single</strong> and <strong>File Upload / Multiple</strong> as ready-made field-level components.
</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1.5fr 1fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Name', 'Figma ID', 'States / Variants', 'Export'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['_File',               '4451:97149', 'Loading · Uploaded · Error',         'FileItem'],
    ['_Drop Zone',          '4451:97112', 'Default · Drag-over · Disabled',     'DropZone'],
    ['File Upload / Single','4451:97211', 'Default · Uploaded · Disabled',      'FileUploadSingle'],
    ['File Upload / Multiple','4451:97198','Default · With files · Disabled',   'FileUploadMultiple'],
  ].map(([name, figmaId, states, exp]) => (
    <div key={name} style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1.5fr 1fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{name}</div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{figmaId}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{states}</div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{exp}</code>
      </div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* _FILE                                                          */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="file-item" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>_File</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Displays the name of an uploaded or currently uploading file. Three states:
  <strong> Loading</strong> (spinner), <strong>Uploaded</strong> (green checkmark), <strong>Error</strong> (red info icon + error message below).
</p>

<Canvas of={FileUploadStories.FileItemStates} />

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>State Reference</h3>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 28 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr 1fr 1.5fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['State', 'Icon', 'Icon Color', 'Border', 'Extra'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['Loading',  'Spinner',            '#000B36 (top arc)', '#D2D6DB', '—'],
    ['Uploaded', 'tick-circle',         '#006121',           '#D2D6DB', '—'],
    ['Error',    'information-circle',  '#A30000',           '#A30000', 'Error message row below separator'],
  ].map(([state, icon, iconColor, border, extra]) => (
    <div key={state} style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr 1fr 1.5fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{state}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{icon}</div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{iconColor}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{border}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{extra}</div>
    </div>
  ))}
</div>

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>Props</h3>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 48 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.2fr 0.8fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Prop', 'Type', 'Default', 'Description'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['name',         'string',                          '—',         'File name displayed in the row'],
    ['status',       "'loading' | 'uploaded' | 'error'", "'uploaded'", 'Controls the icon and border style'],
    ['errorMessage', 'string',                          '—',         "Shown in the error row when status = 'error'"],
    ['onRemove',     '() => void',                      '—',         'Called when the × button is clicked. Button hidden if not provided.'],
  ].map(([prop, type, def, desc]) => (
    <div key={prop} style={{ display: 'grid', gridTemplateColumns: '1fr 1.2fr 0.8fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{prop}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{def}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* _DROP ZONE                                                     */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="drop-zone" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>_Drop Zone</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  The drag-and-drop target area. Has three states: <strong>Default</strong>, <strong>Drag-over</strong> (green accent),
  and <strong>Disabled</strong>. Clicking anywhere in the zone opens the file picker.
</p>

<Canvas of={FileUploadStories.DropZoneStates} />

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>State Reference</h3>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 28 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1.2fr 1.2fr 1.5fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['State', 'Background', 'Border', 'Text / Icon', 'Browse Button'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['Default',   '#F3F4F6', '1px dashed #D2D6DB', '#000B36 / #3C5073', 'bg #F3F4F6, text #000B36'],
    ['Drag-over', '#E6F4EE', '1px dashed #006121', '#006121',           'bg #D2D6DB, text #6C7C96'],
    ['Disabled',  '#F3F4F6', '1px dashed #D2D6DB', '#6C7C96',           'bg #D2D6DB, text #6C7C96'],
  ].map(([state, bg, border, text, btn]) => (
    <div key={state} style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1.2fr 1.2fr 1.5fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{state}</div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{bg}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{border}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{text}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{btn}</code>
      </div>
    </div>
  ))}
</div>

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>Props</h3>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 48 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.2fr 0.8fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Prop', 'Type', 'Default', 'Description'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['hint',         'string',           '—',     'Helper text shown below the title (file size / format info)'],
    ['accept',       'string',           '—',     "Passed to the hidden file input's accept attribute"],
    ['multiple',     'boolean',          'false', 'Allows selecting multiple files'],
    ['disabled',     'boolean',          'false', 'Disables all interactions'],
    ['isDragOver',   'boolean',          'false', 'Forces the drag-over visual state (useful for stories)'],
    ['onFileSelect', 'FileList => void', '—',     'Called with the FileList when files are dropped or browsed'],
  ].map(([prop, type, def, desc]) => (
    <div key={prop} style={{ display: 'grid', gridTemplateColumns: '1fr 1.2fr 0.8fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{prop}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{def}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* FILE UPLOAD / SINGLE                                          */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="single" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>File Upload / Single</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  A label + hint text + solid blue "Browse Files" button. When a file is selected the button is
  replaced with the <strong>_File</strong> row showing the upload state. Does not include a drag-and-drop zone.
</p>

<Canvas of={FileUploadStories.SingleDefault} />
<Canvas of={FileUploadStories.SingleUploaded} />
<Canvas of={FileUploadStories.SingleDisabled} />

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>Props</h3>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 48 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Prop', 'Type', 'Default', 'Description'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['label',    'string',                         "'Upload files'", 'Label shown above the hint and button'],
    ['hint',     'string',                         '—',              'Helper text (file size, format info) below the label'],
    ['required', 'boolean',                        'true',           'Shows the red asterisk beside the label'],
    ['value',    '{ name, status, errorMessage? }','—',              'If provided, renders a FileItem row instead of the button'],
    ['onChange', 'File => void',                   '—',              'Called with the File object when a file is selected'],
    ['onRemove', '() => void',                     '—',              'Called when the × button on the file row is clicked'],
    ['disabled', 'boolean',                        'false',          'Disables the button and mutes all text'],
  ].map(([prop, type, def, desc]) => (
    <div key={prop} style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{prop}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{def}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* FILE UPLOAD / MULTIPLE                                        */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="multiple" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>File Upload / Multiple</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Combines the <strong>_Drop Zone</strong> with a list of <strong>_File</strong> rows below it.
  Files in various states (loading, uploaded, error) can appear simultaneously in the list.
</p>

<Canvas of={FileUploadStories.MultipleDefault} />
<Canvas of={FileUploadStories.MultipleWithFiles} />

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>Props</h3>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 48 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.6fr 0.8fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Prop', 'Type', 'Default', 'Description'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['hint',     'string',                                  '—',    'Helper text shown inside the drop zone'],
    ['accept',   'string',                                  '—',    'File types accepted by the input'],
    ['multiple', 'boolean',                                 'true', 'Allows selecting multiple files at once'],
    ['files',    'Array<{ id, name, status, errorMessage? }>','[]', 'Controlled list of file rows rendered below the drop zone'],
    ['onChange', 'FileList => void',                        '—',    'Called with the FileList when files are dropped or browsed'],
    ['onRemove', 'id => void',                              '—',    "Called with the file's id when × is clicked"],
    ['disabled', 'boolean',                                 'false','Passes disabled state to the drop zone'],
  ].map(([prop, type, def, desc]) => (
    <div key={prop} style={{ display: 'grid', gridTemplateColumns: '1fr 1.6fr 0.8fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{prop}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{def}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* DESIGN TOKENS                                                  */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="tokens" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Design Tokens</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1.2fr 1.2fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Token', 'Value', 'Usage'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['Background',           '#F3F4F6', 'Drop zone + file item background'],
    ['Border default',       '#D2D6DB', 'Default border on drop zone and file items'],
    ['Border error',         '#A30000', 'Error state border on file items'],
    ['Text primary',         '#000B36', 'File name, drop zone title'],
    ['Text secondary',       '#3C5073', 'Helper / hint text'],
    ['Text disabled',        '#6C7C96', 'All disabled text and icons'],
    ['Drag-over bg',         '#E6F4EE', 'Drop zone background when dragging over'],
    ['Drag-over border',     '#006121', 'Drop zone border / icon / text when dragging'],
    ['Success icon',         '#006121', 'Checkmark circle in uploaded state'],
    ['Error text/icon',      '#A30000', 'Error message text and information-circle icon'],
    ['Browse btn (Single)',  '#1849A9', 'Solid blue Browse Files button in Single'],
    ['Browse btn disabled',  '#D2D6DB', 'Disabled button background (drop zone + Single)'],
    ['Required asterisk',    '#B42318', 'The * beside the label in Single'],
    ['Corner radius',        '4px',     'All containers — drop zone, file items'],
    ['Drop zone padding',    '24px',    'Padding inside drop zone'],
    ['File item padding',    '8px',     'Padding inside file row'],
    ['Single gap',           '16px',    'Gap between header and button/file item'],
    ['Multiple gap',         '16px',    'Gap between drop zone and file list'],
  ].map(([token, value, usage]) => (
    <div key={token} style={{ display: 'grid', gridTemplateColumns: '1.2fr 1.2fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{token}</div>
      <div style={{ padding: '12px 16px', display: 'flex', alignItems: 'center', gap: 8 }}>
        {value.startsWith('#') && (
          <span style={{ display: 'inline-block', width: 14, height: 14, borderRadius: 3, background: value, border: '1px solid #e5e7eb', flexShrink: 0 }} />
        )}
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{value}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{usage}</div>
    </div>
  ))}
</div>

</div>
