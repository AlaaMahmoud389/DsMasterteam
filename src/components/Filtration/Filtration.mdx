import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as FiltrationStories from './Filtration.stories';

<Meta of={FiltrationStories} />

<div style={{ borderBottom: '1px solid #e5e7eb', paddingBottom: 12, marginBottom: 24 }}>
  <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 6 }}>Components / Filtration</div>
  <h1 style={{ margin: 0, fontSize: 28, fontWeight: 700, color: '#000b36' }}>Filtration</h1>
  <p style={{ margin: '8px 0 0', fontSize: 14, color: '#3c5073', lineHeight: 1.6 }}>
    A sidebar panel that combines multiple filter widget types — checkboxes, radio buttons, chips, sliders, date pickers, and more — letting users narrow down content in a consistent, accessible way.
  </p>
  <div style={{ display: 'flex', gap: 8, marginTop: 12, flexWrap: 'wrap' }}>
    <span style={{ background: '#d1fae5', color: '#059669', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20 }}>Available</span>
    <span style={{ background: '#f0f4ff', color: '#1849a9', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20 }}>Node ID: 4804:122722</span>
    <a href="https://www.figma.com/design/WTmRAkJVvw0IvZMA7wBdTC/Masterteam-Ds-For-Experiment-ONLY?node-id=4804-122722" target="_blank" rel="noreferrer" style={{ background: '#fff7ed', color: '#c2410c', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20, textDecoration: 'none' }}>Figma ↗</a>
  </div>
</div>

<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '14px 20px', marginBottom: 24 }}>
  <div style={{ fontSize: 12, fontWeight: 700, color: '#1849a9', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.07em' }}>On this page</div>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '6px 0' }}>
    {[['Overview','#overview'],['Anatomy','#anatomy'],['Filter Types','#filter-types'],['Filter Trigger','#filter-trigger'],['RTL Support','#rtl-support'],['Props Reference','#props-reference'],['Token Table','#token-table'],['Do / Don\'t','#do--dont'],['Accessibility','#accessibility']].map(([label, href]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none', lineHeight: 2 }}>{label}</a>
    ))}
  </div>
</div>

---

## Overview

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24 }}>
  <Canvas of={FiltrationStories.Default} />
</div>

---

## Anatomy

<div id="anatomy" style={{ display: 'flex', flexDirection: 'column', gap: 12, marginBottom: 24 }}>
  {[
    [1,  'Panel container',       'White card — 320px wide, border 1px #e5e7eb, border-radius 4px, overflow hidden, scrollable'],
    [2,  'Section label',         '14px Semibold #000b36 — names each filter group'],
    [3,  'Selection count badge', 'Gray (#6c7c96) count appended to label when items are selected'],
    [4,  'Search box',            'Input with magnifier icon — filters the visible checkbox options in real-time'],
    [5,  'Checkbox',              '16×16px — unchecked: #b6bdca border; checked: #1849a9 fill with white checkmark'],
    [6,  'Radio button',          '32×32 wrapper, 24×24 ring — unchecked: gray border; checked: blue ring + inner dot image'],
    [7,  'Chip',                  'Pill tag — bg rgba(24,73,169,0.1), text #1849a9, 14×14px × close icon on the trailing edge'],
    [8,  'Slider',                'Dual-handle range with blue (#1849a9) progress fill on gray (#d2d6db) track'],
    [9,  'Input range',           'Max + Min text inputs with SAR currency suffix — separated by a thin dash icon'],
    [10, 'Date field',            'Native date input with DD/MM/YY placeholder'],
    [11, 'Divider',               '1px #e5e7eb separator between filter groups'],
    [12, 'Swap placeholder',      'Dashed blue-border box — insert any custom content component here'],
    [13, 'Footer actions',        '"Apply Filters (n)" primary blue button + "Clear Filter" neutral button — fixed at panel bottom'],
  ].map(([n, name, desc]) => (
    <div key={n} style={{ display: 'flex', gap: 12, alignItems: 'flex-start', border: '1px solid #f3f4f6', borderRadius: 8, padding: '10px 14px' }}>
      <div style={{ width: 24, height: 24, borderRadius: '50%', background: '#1849a9', color: '#fff', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 12, fontWeight: 700, flexShrink: 0 }}>{n}</div>
      <div>
        <div style={{ fontSize: 13, fontWeight: 600, color: '#111827' }}>{name}</div>
        <div style={{ fontSize: 12, color: '#6b7280', marginTop: 2 }}>{desc}</div>
      </div>
    </div>
  ))}
</div>

---

## Filter Types

<div id="filter-types" style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 24 }}>
  {[
    { type: 'Multi-Select Checkbox', desc: 'Allows selecting multiple options. Includes an inline search field and a "Show more" toggle for long lists. Shows selection count in the label.', when: 'Best for categorical data with many options (e.g. brands, categories).' },
    { type: 'Single Select',         desc: 'One option can be active at a time, indicated by a checkmark tick. Clean list with no checkbox — tick appears on the active row only.',               when: 'Best for mutually exclusive options (e.g. sort order, status).' },
    { type: 'Radio Button',          desc: 'Standard radio group. The selected radio shows a blue ring with inner dot. Supports keyboard navigation and single-choice semantics.',                when: 'Best when exactly one option must be chosen from a small set.' },
    { type: 'Chips Multi-Select',    desc: 'Selected values rendered as removable pill tags. Chips wrap to multiple lines and each has an × close button on the trailing edge.',                  when: 'Best for showing active filter tags at a glance — makes it easy to remove individual selections.' },
    { type: 'Slider Range',          desc: 'Dual-handle slider for selecting a numeric range. Blue progress fill between the two handles on a gray track.',                                       when: 'Best for continuous ranges (price, age, weight) where approximate values are acceptable.' },
    { type: 'Input Range',           desc: 'Two labeled text inputs (Max / Min) with a currency suffix (SAR). Connected by a thin dash separator.',                                              when: 'Best when users need to enter precise numeric values rather than dragging a handle.' },
    { type: 'Date Picker',           desc: 'A date field with placeholder DD/MM/YY. Opens the native date picker on interaction. Supports RTL input direction.',                                 when: 'Best for filtering by date (creation date, expiry, event date).' },
    { type: 'Rating Text-Based',     desc: 'Simple list with descriptive rating labels (Highest / Lowest). Selected row shows a tick icon — same interaction model as Single Select.',            when: 'Best for sorting or filtering by rating or review score.' },
  ].map(({ type, desc, when }) => (
    <div key={type} style={{ border: '1px solid #e5e7eb', borderRadius: 8, padding: '14px 16px' }}>
      <div style={{ fontSize: 13, fontWeight: 700, color: '#111827', marginBottom: 6 }}>{type}</div>
      <div style={{ fontSize: 12, color: '#374151', lineHeight: 1.5, marginBottom: 8 }}>{desc}</div>
      <div style={{ fontSize: 11, color: '#1849a9', background: '#f0f4ff', borderRadius: 4, padding: '4px 8px' }}><strong>When to use:</strong> {when}</div>
    </div>
  ))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24 }}>
  <Canvas of={FiltrationStories.AllStates} />
</div>

---

## Filter Trigger

<div id="filter-trigger" style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24 }}>
  <Canvas of={FiltrationStories.FilterTriggerStory} />
</div>

The `FilterTrigger` is a separate exported component (`Figma node 4804:122763`) that represents the **closed state** of the filter panel — a blue "Filter" button paired with removable result chips.

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: 12, marginBottom: 24 }}>
  {[
    { label: 'Filter button', desc: 'Blue background, filter icon (24×24) + "Filter" text + chevron-down arrow. Opens the panel on click.' },
    { label: 'Result chips',  desc: 'Neutral grey pills showing active filter labels. Each chip has a × button to remove the filter individually.' },
    { label: 'No results',    desc: 'When no filters are applied the chip row is hidden — only the Filter button is shown.' },
  ].map(({ label, desc }) => (
    <div key={label} style={{ border: '1px solid #e5e7eb', borderRadius: 8, padding: '12px 14px' }}>
      <div style={{ fontSize: 13, fontWeight: 700, color: '#111827', marginBottom: 4 }}>{label}</div>
      <div style={{ fontSize: 12, color: '#6b7280', lineHeight: 1.4 }}>{desc}</div>
    </div>
  ))}
</div>

---

## RTL Support

<div id="rtl-support" style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24 }}>
  <Canvas of={FiltrationStories.RTLSupport} />
</div>

Pass `rtl={true}` to flip the entire panel. The `dir="rtl"` attribute is applied to the root element — all text, inputs, chip close icons, and the scrollbar track flip to the opposite side automatically.

---

## Props Reference

<div id="props-reference" style={{ display: 'grid', gridTemplateColumns: '180px 120px 110px 100px 1fr', gap: 0, marginBottom: 24, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
  <div style={{ display: 'contents' }}>
    {['Prop','Type','Default','Required','Description'].map((h, i) => (
      <div key={h} style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
    ))}
  </div>
  {[
    ['rtl',     'boolean',  'false','No','#f0f4ff','#1849a9','Enables right-to-left layout — sets dir="rtl" on the panel root'],
    ['onApply', 'function', '—',    'No','#f0fdf4','#059669','Called when the "Apply Filters" button is clicked. Receives no arguments.'],
    ['onClear', 'function', '—',    'No','#f0fdf4','#059669','Called when the "Clear Filter" button is clicked. Receives no arguments.'],
  ].map(([prop, type, def, req, bg, color, desc]) => (
    <div key={prop} style={{ display: 'contents' }}>
      <div style={{ padding: '9px 14px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
      <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#7c3aed' }}>{type}</div>
      <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#6b7280' }}>{def}</div>
      <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
        <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: req === 'Yes' ? '#fee2e2' : '#f3f4f6', color: req === 'Yes' ? '#dc2626' : '#6b7280' }}>{req}</span>
      </div>
      <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4 }}>{desc}</div>
    </div>
  ))}
</div>

**FilterTrigger props:**

<div style={{ display: 'grid', gridTemplateColumns: '180px 120px 110px 100px 1fr', gap: 0, marginBottom: 24, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
  <div style={{ display: 'contents' }}>
    {['Prop','Type','Default','Required','Description'].map((h, i) => (
      <div key={h} style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
    ))}
  </div>
  {[
    ['rtl',          'boolean',  'false',      'No','#f0f4ff','#1849a9','Enables RTL layout on the trigger and chips'],
    ['results',      'string[]', '["Item"×3]', 'No','#fdf4ff','#7c3aed','Array of active filter label strings. Empty array hides the chip row.'],
    ['onFilterClick','function', '—',          'No','#f0fdf4','#059669','Called when the Filter button is clicked'],
    ['onRemoveChip', 'function', '—',          'No','#f0fdf4','#059669','Called with the chip index when a result chip × is clicked'],
  ].map(([prop, type, def, req, bg, color, desc]) => (
    <div key={prop} style={{ display: 'contents' }}>
      <div style={{ padding: '9px 14px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
      <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#7c3aed' }}>{type}</div>
      <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#6b7280' }}>{def}</div>
      <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
        <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: req === 'Yes' ? '#fee2e2' : '#f3f4f6', color: req === 'Yes' ? '#dc2626' : '#6b7280' }}>{req}</span>
      </div>
      <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4 }}>{desc}</div>
    </div>
  ))}
</div>

---

## Token Table

<div id="token-table" style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: 16, marginBottom: 24 }}>
  <div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
    <div style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Container</div>
    {[
      { token: '--filtration/panel/background', hex: '#ffffff',  label: 'Panel background' },
      { token: '--filtration/panel/border',     hex: '#e5e7eb',  label: 'Panel border colour' },
      { token: '--filtration/panel/radius',     hex: '4px',      label: 'Panel border radius' },
      { token: '--filtration/panel/width',      hex: '320px',    label: 'Panel fixed width' },
      { token: '--filtration/divider',          hex: '#e5e7eb',  label: 'Group divider line' },
      { token: '--filtration/section/gap',      hex: '16–20px',  label: 'Gap between section items' },
    ].map(({ token, hex, label }) => (
      <div key={token} style={{ padding: '10px 14px', borderBottom: '1px solid #f3f4f6' }}>
        <code style={{ fontSize: 10, color: '#1849a9', display: 'block' }}>{token}</code>
        <span style={{ fontSize: 11, color: '#6b7280' }}>{hex} · {label}</span>
      </div>
    ))}
  </div>
  <div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
    <div style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Typography & Colors</div>
    {[
      { token: '--filtration/label/text',           hex: '#000b36',             label: 'Section label (14px Semibold)' },
      { token: '--filtration/label/count',          hex: '#6c7c96',             label: 'Selection count badge text' },
      { token: '--filtration/option/text',          hex: '#000b36',             label: 'Option row text (14px Regular)' },
      { token: '--filtration/checkbox/checked-bg',  hex: '#1849a9',             label: 'Checked checkbox fill' },
      { token: '--filtration/chip/background',      hex: 'rgba(24,73,169,0.1)', label: 'Active chip background' },
      { token: '--filtration/chip/text',            hex: '#1849a9',             label: 'Active chip text' },
      { token: '--filtration/slider/progress',      hex: '#1849a9',             label: 'Slider progress fill' },
      { token: '--filtration/slider/track',         hex: '#d2d6db',             label: 'Slider track background' },
    ].map(({ token, hex, label }) => (
      <div key={token} style={{ padding: '10px 14px', borderBottom: '1px solid #f3f4f6', display: 'flex', alignItems: 'center', gap: 8 }}>
        {hex.startsWith('#') && <div style={{ width: 14, height: 14, borderRadius: 3, background: hex, border: '1px solid #e5e7eb', flexShrink: 0 }} />}
        <div>
          <code style={{ fontSize: 10, color: '#1849a9', display: 'block' }}>{token}</code>
          <span style={{ fontSize: 10, color: '#6b7280' }}>{hex} · {label}</span>
        </div>
      </div>
    ))}
  </div>
  <div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
    <div style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Footer Buttons</div>
    {[
      { token: '--button/primary/bg',      hex: '#1849a9', label: 'Apply Filters background' },
      { token: '--button/primary/text',    hex: '#f9fafb', label: 'Apply Filters text' },
      { token: '--button/neutral/bg',      hex: '#f3f4f6', label: 'Clear Filter background' },
      { token: '--button/neutral/text',    hex: '#000b36', label: 'Clear Filter text' },
      { token: '--button/height',          hex: '40px',    label: 'Footer button height' },
      { token: '--button/radius',          hex: '4px',     label: 'Footer button radius' },
      { token: '--filter-btn/bg',          hex: '#1849a9', label: 'Trigger button background' },
      { token: '--result-chip/bg',         hex: 'rgba(31,42,55,0.05)', label: 'Result chip background' },
    ].map(({ token, hex, label }) => (
      <div key={token} style={{ padding: '10px 14px', borderBottom: '1px solid #f3f4f6', display: 'flex', alignItems: 'center', gap: 8 }}>
        {hex.startsWith('#') && <div style={{ width: 14, height: 14, borderRadius: 3, background: hex, border: '1px solid #e5e7eb', flexShrink: 0 }} />}
        <div>
          <code style={{ fontSize: 10, color: '#1849a9', display: 'block' }}>{token}</code>
          <span style={{ fontSize: 10, color: '#6b7280' }}>{label}</span>
        </div>
      </div>
    ))}
  </div>
</div>

---

## Do / Don't

<div id="do--dont" style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 24 }}>
  <div>
    <div style={{ fontSize: 12, fontWeight: 700, color: '#059669', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.05em' }}>✓ Do</div>
    <div style={{ border: '2px solid #d1fae5', borderRadius: 10, padding: 20 }}>
      <div style={{ fontSize: 12, color: '#374151', marginBottom: 12 }}>Match each filter widget to the data type — checkboxes for multi-select, slider for ranges, radio for single-choice.</div>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        {[
          { icon: '☑', label: 'Multi-Select Checkbox', desc: 'Multiple independent items' },
          { icon: '◉', label: 'Radio Button',           desc: 'Exactly one of N options' },
          { icon: '⟷', label: 'Slider Range',           desc: 'Continuous numeric range' },
        ].map(({ icon, label, desc }) => (
          <div key={label} style={{ display: 'flex', gap: 8, alignItems: 'center', background: '#f0fdf4', borderRadius: 6, padding: '6px 10px' }}>
            <span style={{ fontSize: 16, flexShrink: 0 }}>{icon}</span>
            <div>
              <div style={{ fontSize: 12, fontWeight: 600, color: '#111827' }}>{label}</div>
              <div style={{ fontSize: 11, color: '#6b7280' }}>{desc}</div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
  <div>
    <div style={{ fontSize: 12, fontWeight: 700, color: '#dc2626', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.05em' }}>✗ Don't</div>
    <div style={{ border: '2px solid #fee2e2', borderRadius: 10, padding: 20 }}>
      <div style={{ fontSize: 12, color: '#374151', marginBottom: 12 }}>Don't use the wrong widget type for the data — radio buttons for multi-select, or checkboxes for single-choice, create confusing expectations.</div>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        {[
          { icon: '◉', label: 'Radio for multi-select', desc: 'Implies only one can be chosen' },
          { icon: '☑', label: 'Checkbox for single-choice', desc: 'Implies many can be selected' },
          { icon: '⟷', label: 'Slider for text categories', desc: 'Slider implies numeric, not named' },
        ].map(({ icon, label, desc }) => (
          <div key={label} style={{ display: 'flex', gap: 8, alignItems: 'center', background: '#fff5f5', borderRadius: 6, padding: '6px 10px' }}>
            <span style={{ fontSize: 16, flexShrink: 0 }}>{icon}</span>
            <div>
              <div style={{ fontSize: 12, fontWeight: 600, color: '#dc2626' }}>{label}</div>
              <div style={{ fontSize: 11, color: '#6b7280' }}>{desc}</div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
  <div>
    <div style={{ fontSize: 12, fontWeight: 700, color: '#059669', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.05em' }}>✓ Do</div>
    <div style={{ border: '2px solid #d1fae5', borderRadius: 10, padding: 20 }}>
      <div style={{ fontSize: 12, color: '#374151', marginBottom: 0 }}>Show selection count in the section label ("Variant (2 selections)") so users always know how many filters are active per group — even when the list is collapsed.</div>
    </div>
  </div>
  <div>
    <div style={{ fontSize: 12, fontWeight: 700, color: '#dc2626', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.05em' }}>✗ Don't</div>
    <div style={{ border: '2px solid #fee2e2', borderRadius: 10, padding: 20 }}>
      <div style={{ fontSize: 12, color: '#374151', marginBottom: 0 }}>Don't put more than 3–4 visible checkbox options before a "Show more" toggle — a wall of uncollapsed options makes the panel feel overwhelming and hard to scan.</div>
    </div>
  </div>
</div>

---

## Accessibility

<div id="accessibility" style={{ display: 'flex', flexDirection: 'column', gap: 16, marginBottom: 24 }}>
  {[
    { category: 'Keyboard',       color: '#eff8ff', text: '#1849a9', items: ['Checkboxes and radios are focusable with Tab — Space toggles checkboxes, arrow keys navigate radio groups', 'Chip × buttons are keyboard-reachable and respond to Enter/Space', '"Apply Filters" and "Clear Filter" buttons respond to Enter', '"Show more" button is reachable via Tab'] },
    { category: 'Semantics',      color: '#f0fdf4', text: '#059669', items: ['All interactive controls use native HTML elements (input[type=checkbox], input[type=radio], button)', 'Search input has a visible placeholder and is associated with the section above it', 'Date input uses type="date" for native assistive technology support', 'Section labels describe their group — no generic "Filter" labels'] },
    { category: 'Color contrast', color: '#fdf4ff', text: '#7c3aed', items: ['Label text #000b36 on white — 21:1 (AAA)', 'Chip text #1849a9 on rgba(24,73,169,0.1) — verified WCAG AA', 'Option text #000b36 on white — 21:1 (AAA)', 'Placeholder #b6bdca is decorative — contrast exemption applies'] },
    { category: 'RTL',            color: '#fff7ed', text: '#c2410c', items: ['dir="rtl" propagates to all child inputs, text, and the scrollbar', 'Scrollbar moves to the left side in RTL mode', 'Apply / Clear button order follows the reading direction', 'Chip × button remains on the trailing (left) edge in RTL'] },
  ].map(({ category, color, text, items }) => (
    <div key={category} style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden' }}>
      <div style={{ padding: '8px 14px', background: color, fontWeight: 700, fontSize: 12, color: text }}>{category}</div>
      <ul style={{ margin: 0, padding: '10px 14px 10px 28px' }}>
        {items.map(item => (
          <li key={item} style={{ fontSize: 13, color: '#374151', lineHeight: 1.6 }}>{item}</li>
        ))}
      </ul>
    </div>
  ))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 8 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '160px 60px 1fr 100px', gap: 0 }}>
    <div style={{ display: 'contents' }}>
      {[['Criterion','Level','Requirement','Status']].map(([a,b,c,d]) => (
        <div key="hdr" style={{ display: 'contents' }}>
          <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb' }}>{a}</div>
          <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>{b}</div>
          <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb' }}>{c}</div>
          <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>{d}</div>
        </div>
      ))}
    </div>
    {[
      ['1.4.3 Contrast',     'AA',  'Text and interactive elements must have ≥ 4.5:1 contrast ratio against background'],
      ['1.3.1 Info & Relationships', 'A', 'Filter groups use semantic structure; labels are programmatically associated with inputs'],
      ['2.1.1 Keyboard',     'A',   'All filter controls operable without a mouse via keyboard navigation'],
      ['2.4.3 Focus Order',  'A',   'Tab order follows visual reading order; no focus traps within the panel'],
      ['4.1.2 Name, Role, Value', 'A', 'Checkboxes, radios, and buttons expose correct roles and states to assistive technologies'],
    ].map(([criterion, level, req]) => (
      <div key={criterion} style={{ display: 'contents' }}>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', fontFamily: 'monospace', fontSize: 12, color: '#1849a9', fontWeight: 600 }}>{criterion}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: level === 'AAA' ? '#ede9fe' : '#dbeafe', color: level === 'AAA' ? '#6d28d9' : '#1849a9' }}>{level}</span>
        </div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', fontSize: 12, color: '#374151', lineHeight: 1.4 }}>{req}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: '#d1fae5', color: '#059669' }}>✓ Pass</span>
        </div>
      </div>
    ))}
  </div>
</div>
