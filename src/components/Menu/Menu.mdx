{/* Menu.mdx — Masterteam Design System */}
import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as MenuStories from './Menu.stories';

<Meta of={MenuStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif" }}>

{/* ── Header ───────────────────────────────────────────────── */}
<div style={{ marginBottom: 40 }}>
  <h1 style={{ fontSize: 28, fontWeight: 700, marginBottom: 12, color: '#000b36' }}>Menu</h1>
  <p style={{ margin: '0 0 16px', fontSize: 15, color: '#374151', lineHeight: 1.6, maxWidth: 700 }}>
    Two composable components — <strong>MenuListItem</strong> and <strong>Menu</strong> — that together
    build contextual dropdown menus. Each item supports 6 interactive states, 6 trail element types
    (None, Text, Icon, Button, Tag, Switch), a Lead Icon, Sub Item flag, and full RTL support.
    The Menu panel groups items into labelled sections with three style variants.
  </p>
  <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginTop: 16 }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6, background: '#ecfdf5', color: '#065f46', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#059669', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ display: 'inline-flex', alignItems: 'center', background: '#eff3fb', color: '#1849a9', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>6 States</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', background: '#eff3fb', color: '#1849a9', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>6 Trail Elements</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', background: '#eff3fb', color: '#1849a9', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>3 Menu Styles</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', background: '#eff3fb', color: '#1849a9', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>RTL ✓</span>
  </div>
</div>

{/* ── Table of Contents ────────────────────────────────────── */}
<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '16px 20px', marginBottom: 40, display: 'inline-block', minWidth: 220 }}>
  <p style={{ margin: '0 0 8px', fontSize: 12, fontWeight: 700, color: '#000b36', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Contents</p>
  <ol style={{ margin: 0, padding: '0 0 0 16px', fontSize: 13, color: '#1849a9', lineHeight: 2 }}>
    <li><a href="#overview" style={{ color: '#1849a9', textDecoration: 'none' }}>Overview</a></li>
    <li><a href="#states" style={{ color: '#1849a9', textDecoration: 'none' }}>States</a></li>
    <li><a href="#trail" style={{ color: '#1849a9', textDecoration: 'none' }}>Trail Elements</a></li>
    <li><a href="#sub-item" style={{ color: '#1849a9', textDecoration: 'none' }}>Sub Item</a></li>
    <li><a href="#rtl-item" style={{ color: '#1849a9', textDecoration: 'none' }}>RTL (List Item)</a></li>
    <li><a href="#menu-panel" style={{ color: '#1849a9', textDecoration: 'none' }}>Menu Panel — Style Variants</a></li>
    <li><a href="#menu-rtl" style={{ color: '#1849a9', textDecoration: 'none' }}>Menu Panel — RTL</a></li>
    <li><a href="#props" style={{ color: '#1849a9', textDecoration: 'none' }}>Props</a></li>
    <li><a href="#tokens" style={{ color: '#1849a9', textDecoration: 'none' }}>Design Tokens</a></li>
    <li><a href="#accessibility" style={{ color: '#1849a9', textDecoration: 'none' }}>Accessibility</a></li>
  </ol>
</div>

{/* ── Overview ─────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="overview">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 16 }}>Overview</h2>
  <Canvas of={MenuStories.PlaygroundListItem} />
  <Controls of={MenuStories.PlaygroundListItem} />
</div>

{/* ── States ───────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="states">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Menu List Item — States</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    Six interactive states. <strong>Hovered</strong> and <strong>Selected</strong> share the same background
    (<code>#E5E7EB</code>). <strong>Focused</strong> shows a 2 px outline. <strong>Disabled</strong> mutes
    all color to <code>#6C7C96</code>.
  </p>
  <Canvas of={MenuStories.AllStates} />

  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13, marginTop: 24 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>State</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Background</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Text & Icon</div>

    {[
      { state: 'Default',  bg: 'transparent', bgHex: null,      color: '#000B36', colorHex: '#000B36' },
      { state: 'Hovered',  bg: '#E5E7EB',     bgHex: '#E5E7EB', color: '#000B36', colorHex: '#000B36' },
      { state: 'Pressed',  bg: '#D2D6DB',     bgHex: '#D2D6DB', color: '#000B36', colorHex: '#000B36' },
      { state: 'Focused',  bg: 'transparent', bgHex: null,      color: '#000B36', colorHex: '#000B36' },
      { state: 'Selected', bg: '#E5E7EB',     bgHex: '#E5E7EB', color: '#000B36', colorHex: '#000B36' },
      { state: 'Disabled', bg: 'transparent', bgHex: null,      color: '#6C7C96', colorHex: '#6C7C96' },
    ].map(({ state, bg, bgHex, color, colorHex }) => (
      <>
        <div key={`${state}-s`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>{state}</div>
        <div key={`${state}-b`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', display: 'flex', alignItems: 'center', gap: 8 }}>
          {bgHex && <span style={{ width: 14, height: 14, borderRadius: 3, background: bgHex, border: '1px solid #e5e7eb', display: 'inline-block', flexShrink: 0 }} />}
          <code style={{ color: '#374151', fontSize: 12 }}>{bg}</code>
        </div>
        <div key={`${state}-c`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', display: 'flex', alignItems: 'center', gap: 8 }}>
          <span style={{ width: 14, height: 14, borderRadius: 3, background: colorHex, border: '1px solid #e5e7eb', display: 'inline-block', flexShrink: 0 }} />
          <code style={{ color: '#374151', fontSize: 12 }}>{color}</code>
        </div>
      </>
    ))}
  </div>
</div>

{/* ── Trail Elements ───────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="trail">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Menu List Item — Trail Elements</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    Six trail element options sit at the end of each item row. The Lead Icon always appears at the start
    (left in LTR, right in RTL).
  </p>
  <Canvas of={MenuStories.AllTrailElements} />
</div>

{/* ── Sub Item ─────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="sub-item">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Menu List Item — Sub Item</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    The <code>subItem</code> boolean marks an item as a child of another item. It carries the same visual
    style as a regular item — the semantic distinction is used by the consuming application for accessibility
    and grouping.
  </p>
  <Canvas of={MenuStories.SubItems} />
</div>

{/* ── RTL (List Item) ──────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="rtl-item">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Menu List Item — RTL</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    Set <code>rtl</code> to mirror the item layout. In RTL, the lead icon appears on the right and the trail
    element on the left.
  </p>
  <Canvas of={MenuStories.RTLItems} />
</div>

{/* ── Menu Panel — Style Variants ──────────────────────────── */}
<div style={{ marginBottom: 48 }} id="menu-panel">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Menu Panel — Style Variants</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    The Menu panel groups items into sections separated by dividers. Three style variants control Lead Icon visibility.
  </p>
  <Canvas of={MenuStories.AllMenuStyles} />

  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13, marginTop: 24 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Style</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Behaviour</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#1849a9', fontSize: 12 }}>all</code></div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>Lead icon always shown regardless of per-item setting.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#1849a9', fontSize: 12 }}>text-only</code></div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>Lead icon hidden on all items — text and trail element only.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#1849a9', fontSize: 12 }}>text-icon</code></div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>Lead icon shown per individual item's <code>leadIcon</code> prop.</div>
  </div>
</div>

{/* ── Menu Panel — RTL ─────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="menu-rtl">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Menu Panel — RTL</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    Setting <code>rtl</code> on the <code>Menu</code> component propagates to all items and the group label.
  </p>
  <Canvas of={MenuStories.MenuRTL} />
</div>

{/* ── Props ─────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="props">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 20 }}>Props</h2>

  <h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 12 }}>MenuListItem</h3>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr 1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13, marginBottom: 32 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Prop</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Type</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Default</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Description</div>

    {[
      ['label',          'string',                                                                 "'Item Label'",  'The item text.'],
      ['leadIcon',       'string | null',                                                          "'list'",        'Icon name for the leading icon. Pass null to hide it (text-only style).'],
      ['trailElement',   "'none'|'text'|'icon'|'button'|'tag'|'switch'",                         "'none'",        'The trailing element type.'],
      ['trailText',      'string',                                                                 "'+99'",         "Text shown when trailElement='text'."],
      ['trailIcon',      'string',                                                                 "'tick'",        "Icon name when trailElement='icon'."],
      ['trailTagText',   'string',                                                                 "'Label'",       "Tag label text when trailElement='tag'."],
      ['switchChecked',  'boolean',                                                                'false',         "Checked state when trailElement='switch'."],
      ['onSwitchChange', 'function',                                                               '—',             'Called with new boolean value when switch is toggled.'],
      ['state',          "'default'|'hovered'|'pressed'|'focused'|'disabled'|'selected'",        "'default'",     'Visual state of the item.'],
      ['subItem',        'boolean',                                                                'false',         'Marks as a sub-item (semantic flag, no visual indent).'],
      ['rtl',            'boolean',                                                                'false',         'Right-to-left layout.'],
      ['onClick',        'function',                                                               '—',             'Click handler (disabled when state=disabled).'],
    ].map(([prop, type, def, desc]) => (
      <>
        <div key={`${prop}-p`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#1849a9', fontSize: 12 }}>{prop}</code></div>
        <div key={`${prop}-t`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#374151', fontSize: 12 }}>{type}</code></div>
        <div key={`${prop}-d`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#374151', fontSize: 12 }}>{def}</code></div>
        <div key={`${prop}-desc`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>{desc}</div>
      </>
    ))}
  </div>

  <h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 12 }}>Menu</h3>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr 1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Prop</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Type</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Default</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Description</div>

    {[
      ['sections', "Array<{ groupLabel?: string, items: MenuListItem[] }>", '[]',    'Array of sections. Each section has an optional label and an array of item prop objects.'],
      ['style',    "'all'|'text-only'|'text-icon'",                         "'all'", 'all = lead icon always shown. text-only = lead icon hidden. text-icon = lead icon shown per item.'],
      ['rtl',      'boolean',                                                'false', 'Propagates RTL to all items and section labels.'],
    ].map(([prop, type, def, desc]) => (
      <>
        <div key={`${prop}-p`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#1849a9', fontSize: 12 }}>{prop}</code></div>
        <div key={`${prop}-t`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#374151', fontSize: 12 }}>{type}</code></div>
        <div key={`${prop}-d`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#374151', fontSize: 12 }}>{def}</code></div>
        <div key={`${prop}-desc`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>{desc}</div>
      </>
    ))}
  </div>
</div>

{/* ── Design Tokens ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="tokens">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 20 }}>Design Tokens</h2>

  <div style={{ display: 'grid', gridTemplateColumns: '2fr 1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Token</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Value</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Usage</div>

    {[
      { token: 'Item text / icon',              value: '#000B36',                    swatch: '#000B36',  usage: 'Default label and icon color' },
      { token: 'Item bg — Hovered / Selected',  value: '#E5E7EB',                    swatch: '#E5E7EB',  usage: 'Background on hover and selection' },
      { token: 'Item bg — Pressed',             value: '#D2D6DB',                    swatch: '#D2D6DB',  usage: 'Background while pressing' },
      { token: 'Item focus outline',            value: '#00001C 2px',                swatch: '#00001C',  usage: 'Focus ring color and width' },
      { token: 'Item — Disabled color',         value: '#6C7C96',                    swatch: '#6C7C96',  usage: 'Text and icon color in disabled state' },
      { token: 'Trail text color',              value: '#3C5073',                    swatch: '#3C5073',  usage: 'Color of trailing text element (e.g. "+99")' },
      { token: 'Trail button / Tag bg',         value: '#F3F4F6',                    swatch: '#F3F4F6',  usage: 'Background for button and tag trail elements' },
      { token: 'Tag border',                    value: '#D2D6DB',                    swatch: '#D2D6DB',  usage: 'Border color of the tag trail element' },
      { token: 'Tag text',                      value: '12px / 500 / 18lh',          swatch: null,       usage: 'Typography for tag label' },
      { token: 'Switch track (off)',            value: 'border #1F2A37',             swatch: '#1F2A37',  usage: 'Unfilled track with dark border' },
      { token: 'Switch thumb (off)',            value: '#1F2A37',                    swatch: '#1F2A37',  usage: 'Dark filled circle on left' },
      { token: 'Switch (on)',                   value: 'bg #1F2A37 + white thumb',   swatch: '#1F2A37',  usage: 'Filled track, white thumb slides right' },
      { token: 'Menu panel bg',                 value: '#FFFFFF',                    swatch: '#FFFFFF',  usage: 'Menu background' },
      { token: 'Menu panel border',             value: '#E5E7EB 1px r=8px',          swatch: '#E5E7EB',  usage: 'Menu border and corner radius' },
      { token: 'Section divider',               value: '#E5E7EB 1px',               swatch: '#E5E7EB',  usage: 'Horizontal rule between sections' },
      { token: 'Group label',                   value: '#000B36 12px/700/18lh',      swatch: '#000B36',  usage: 'Section header typography' },
      { token: 'Item height',                   value: '40px',                       swatch: null,       usage: 'Minimum item row height' },
      { token: 'Item padding',                  value: '8px all sides',              swatch: null,       usage: 'Internal item padding' },
      { token: 'Item gap',                      value: '8px',                        swatch: null,       usage: 'Gap between lead icon, label, and trail element' },
      { token: 'Item border-radius',            value: '4px',                        swatch: null,       usage: 'Corner radius of each item' },
      { token: 'Font',                          value: 'IBM Plex Sans Arabic',        swatch: null,       usage: 'All text, LTR and RTL' },
      { token: 'Label typography',              value: '16px / 400 / 24lh',          swatch: null,       usage: 'Item label font settings' },
    ].map(({ token, value, swatch, usage }) => (
      <>
        <div key={`${token}-t`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>{token}</div>
        <div key={`${token}-v`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', display: 'flex', alignItems: 'center', gap: 8 }}>
          {swatch && <span style={{ width: 14, height: 14, borderRadius: 3, background: swatch, border: '1px solid #e5e7eb', display: 'inline-block', flexShrink: 0 }} />}
          <code style={{ color: '#374151', fontSize: 12 }}>{value}</code>
        </div>
        <div key={`${token}-u`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#6b7280' }}>{usage}</div>
      </>
    ))}
  </div>
</div>

{/* ── Accessibility ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="accessibility">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 20 }}>Accessibility</h2>

  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Concern</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Implementation</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Role semantics</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>The <code style={{ color: '#1849a9', fontSize: 12 }}>Menu</code> root has <code style={{ color: '#1849a9', fontSize: 12 }}>role="menu"</code>. Each <code style={{ color: '#1849a9', fontSize: 12 }}>MenuListItem</code> has <code style={{ color: '#1849a9', fontSize: 12 }}>role="menuitem"</code>.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Disabled items</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>Disabled items get <code style={{ color: '#1849a9', fontSize: 12 }}>aria-disabled="true"</code> and <code style={{ color: '#1849a9', fontSize: 12 }}>tabIndex="-1"</code> — they cannot receive focus.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Selected items</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>Selected items get <code style={{ color: '#1849a9', fontSize: 12 }}>aria-selected="true"</code> for screen reader announcement.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Switch trail</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>The Switch trail element has <code style={{ color: '#1849a9', fontSize: 12 }}>role="switch"</code> and <code style={{ color: '#1849a9', fontSize: 12 }}>aria-checked</code> for state announcement.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Keyboard navigation</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>Focus is managed via <code style={{ color: '#1849a9', fontSize: 12 }}>tabIndex={0}</code> on each enabled item. Keyboard users can press <code style={{ color: '#1849a9', fontSize: 12 }}>Enter</code> or <code style={{ color: '#1849a9', fontSize: 12 }}>Space</code> to activate an item.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>RTL support</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>The <code style={{ color: '#1849a9', fontSize: 12 }}>rtl</code> prop sets <code style={{ color: '#1849a9', fontSize: 12 }}>dir="rtl"</code> on the item for correct screen reader reading order.</div>
  </div>
</div>

</div>
