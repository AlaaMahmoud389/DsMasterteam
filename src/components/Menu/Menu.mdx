{/* Menu.mdx — Masterteam Design System */}
import { Meta } from '@storybook/addon-docs/blocks';
import { MenuListItem, MenuSection, Menu } from './Menu';
import * as MenuStories from './Menu.stories';

<Meta of={MenuStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif" }}>

{/* ── Header ───────────────────────────────────────────────── */}
<div style={{ marginBottom: 40, paddingBottom: 28, borderBottom: '1px solid #e5e7eb' }}>
  <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 10 }}>Components / Menu</div>
  <h1 style={{ fontSize: 34, fontWeight: 600, marginBottom: 10, color: '#111827' }}>Menu</h1>
  <div style={{ fontSize: 15, color: '#6b7280', maxWidth: 720, lineHeight: 1.7 }}>
    Two composable components — <strong>MenuListItem</strong> and <strong>Menu</strong> — that together
    build contextual dropdown menus. Each item supports 6 interactive states, 6 trail element types
    (None, Text, Icon, Button, Tag, Switch), a Lead Icon, Sub Item flag, and full RTL support.
    The Menu panel groups items into labelled sections with three style variants.
  </div>
  <div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', marginTop: 16 }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#059669', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>6 states</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>6 trail elements</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>3 menu styles</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>RTL ✓</span>
    <a href="https://www.figma.com/design/WTmRAkJVvw0IvZMA7wBdTC/Masterteam-Ds-For-Experiment-ONLY?node-id=4250-1938" target="_blank" rel="noopener" style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#eff3fb', color: '#1849a9', padding: '3px 12px', borderRadius: 100, fontSize: 12, textDecoration: 'none', fontWeight: 500 }}>Figma ↗</a>
  </div>
</div>

---

{/* ── States ───────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>Menu List Item — States</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Six interactive states. <strong>Hovered</strong> and <strong>Selected</strong> share the same background
    (<code>#E5E7EB</code>). <strong>Focused</strong> shows a 2 px outline. <strong>Disabled</strong> mutes
    all color to <code>#6C7C96</code>.
  </p>

  <table style={{ borderCollapse: 'collapse', fontSize: 13, width: '100%', marginBottom: 24 }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>State</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Background</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Text / Icon color</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Preview</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['Default',  'transparent',      '#000B36', 'default'],
        ['Hovered',  '#E5E7EB',          '#000B36', 'hovered'],
        ['Pressed',  '#D2D6DB',          '#000B36', 'pressed'],
        ['Focused',  'transparent',      '#000B36', 'focused'],
        ['Selected', '#E5E7EB',          '#000B36', 'selected'],
        ['Disabled', 'transparent',      '#6C7C96', 'disabled'],
      ].map(([state, bg, color, stateVal]) => (
        <tr key={state} style={{ borderTop: '1px solid #f3f4f6' }}>
          <td style={{ padding: '12px', fontWeight: 500, color: '#374151' }}>{state}</td>
          <td style={{ padding: '12px', fontFamily: 'monospace', color: '#6b7280', fontSize: 12 }}>{bg}</td>
          <td style={{ padding: '12px', fontFamily: 'monospace', color: '#6b7280', fontSize: 12 }}>{color}</td>
          <td style={{ padding: '12px', width: 250 }}><MenuListItem label="Item Label" leadIcon="list" state={stateVal} /></td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

{/* ── Trail Elements ───────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>Menu List Item — Trail Elements</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Six trail element options sit at the end of each item row. The Lead Icon always appears at the start
    (left in LTR, right in RTL).
  </p>

  <div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
    {[
      { label: 'None', props: { trailElement: 'none' } },
      { label: 'Text (+99)', props: { trailElement: 'text', trailText: '+99' } },
      { label: 'Icon (tick)', props: { trailElement: 'icon', trailIcon: 'tick' } },
      { label: 'Button (chevron arrow)', props: { trailElement: 'button' } },
      { label: 'Tag', props: { trailElement: 'tag', trailTagText: 'Label' } },
      { label: 'Switch (off)', props: { trailElement: 'switch', switchChecked: false } },
      { label: 'Switch (on)', props: { trailElement: 'switch', switchChecked: true } },
    ].map(({ label, props }) => (
      <div key={label} style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
        <span style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', width: 180, flexShrink: 0 }}>{label}</span>
        <MenuListItem label="Item Label" leadIcon="list" {...props} />
      </div>
    ))}
  </div>
</div>

---

{/* ── Sub Item ─────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>Menu List Item — Sub Item</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    The <code>subItem</code> boolean marks an item as a child of another item. It carries the same visual
    style as a regular item — the semantic distinction is used by the consuming application for accessibility
    and grouping.
  </p>

  <div style={{ display: 'flex', flexDirection: 'column', gap: 0, width: 260 }}>
    <MenuListItem label="Main Item" leadIcon="list" />
    <MenuListItem label="Sub Item A" leadIcon="list" subItem />
    <MenuListItem label="Sub Item B" leadIcon="list" subItem />
    <MenuListItem label="Another Main Item" leadIcon="list" />
  </div>
</div>

---

{/* ── RTL ──────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>Menu List Item — RTL</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Set <code>rtl</code> to mirror the item layout. In RTL, the lead icon appears on the right and the trail
    element on the left.
  </p>

  <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
    {[
      { label: 'None', props: { trailElement: 'none' } },
      { label: 'Icon (tick) — Selected', props: { trailElement: 'icon', trailIcon: 'tick', state: 'selected' } },
      { label: 'Tag', props: { trailElement: 'tag', trailTagText: 'وسم' } },
      { label: 'Switch', props: { trailElement: 'switch', switchChecked: true } },
      { label: 'Disabled', props: { state: 'disabled' } },
    ].map(({ label, props }) => (
      <div key={label} style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
        <span style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', width: 200, flexShrink: 0 }}>{label}</span>
        <MenuListItem label="نص العنصر" leadIcon="list" rtl {...props} />
      </div>
    ))}
  </div>
</div>

---

{/* ── Menu panel ───────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>Menu Panel — Style Variants</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    The Menu panel groups items into sections separated by dividers. Three style variants control Lead Icon visibility.
  </p>

  <div style={{ display: 'flex', gap: 32, flexWrap: 'wrap', alignItems: 'flex-start' }}>

    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Style = All</div>
      <Menu
        style="all"
        sections={[
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', leadIcon: 'list', trailElement: 'icon', trailIcon: 'tick', state: 'selected' },
              { label: 'Item Label', leadIcon: 'list' },
              { label: 'Item Label', leadIcon: 'list' },
              { label: 'Item Label', leadIcon: 'list' },
            ],
          },
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', leadIcon: 'list', trailElement: 'switch', switchChecked: false },
              { label: 'Item Label', leadIcon: 'list', trailElement: 'switch', switchChecked: true },
              { label: 'Item Label', leadIcon: 'list', trailElement: 'switch', switchChecked: false },
            ],
          },
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', leadIcon: 'list', trailElement: 'tag', trailTagText: 'Label' },
              { label: 'Item Label', leadIcon: 'list', trailElement: 'text', trailText: '+99' },
              { label: 'Item Label', leadIcon: 'list', trailElement: 'button' },
              { label: 'Item Label', leadIcon: 'list', trailElement: 'none', state: 'disabled' },
            ],
          },
        ]}
      />
    </div>

    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Style = Text Only</div>
      <Menu
        style="text-only"
        sections={[
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', trailElement: 'icon', trailIcon: 'tick', state: 'selected' },
              { label: 'Item Label' },
              { label: 'Item Label' },
              { label: 'Item Label' },
            ],
          },
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', trailElement: 'switch', switchChecked: false },
              { label: 'Item Label', trailElement: 'switch', switchChecked: true },
              { label: 'Item Label', trailElement: 'switch', switchChecked: false },
            ],
          },
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', trailElement: 'tag', trailTagText: 'Label' },
              { label: 'Item Label', trailElement: 'text', trailText: '+99' },
              { label: 'Item Label', trailElement: 'button' },
              { label: 'Item Label', state: 'disabled' },
            ],
          },
        ]}
      />
    </div>

    <div>
      <div style={{ fontSize: 12, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 12 }}>Style = Text + Icon</div>
      <Menu
        style="text-icon"
        sections={[
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', leadIcon: 'list', trailElement: 'icon', trailIcon: 'tick', state: 'selected' },
              { label: 'Item Label', leadIcon: 'star' },
              { label: 'Item Label', leadIcon: 'heart' },
              { label: 'Item Label', leadIcon: 'bookmark' },
            ],
          },
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', leadIcon: 'user', trailElement: 'switch', switchChecked: false },
              { label: 'Item Label', leadIcon: 'bell', trailElement: 'switch', switchChecked: true },
              { label: 'Item Label', leadIcon: 'setting', trailElement: 'switch', switchChecked: false },
            ],
          },
          {
            groupLabel: 'GROUP LABEL',
            items: [
              { label: 'Item Label', leadIcon: 'download', trailElement: 'tag', trailTagText: 'Label' },
              { label: 'Item Label', leadIcon: 'edit', trailElement: 'text', trailText: '+99' },
              { label: 'Item Label', leadIcon: 'share', trailElement: 'button' },
              { label: 'Item Label', leadIcon: 'delete', state: 'disabled' },
            ],
          },
        ]}
      />
    </div>

  </div>
</div>

---

{/* ── Menu RTL ─────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 8 }}>Menu Panel — RTL</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.6 }}>
    Setting <code>rtl</code> on the <code>Menu</code> component propagates to all items and the group label.
  </p>

  <Menu
    style="all"
    rtl
    sections={[
      {
        groupLabel: 'عنوان مجموعة',
        items: [
          { label: 'نص العنصر', leadIcon: 'list', trailElement: 'icon', trailIcon: 'tick', state: 'selected' },
          { label: 'نص العنصر', leadIcon: 'list' },
          { label: 'نص العنصر', leadIcon: 'list' },
        ],
      },
      {
        groupLabel: 'عنوان مجموعة',
        items: [
          { label: 'نص العنصر', leadIcon: 'list', trailElement: 'switch', switchChecked: false },
          { label: 'نص العنصر', leadIcon: 'list', trailElement: 'switch', switchChecked: true },
          { label: 'نص العنصر', leadIcon: 'list', trailElement: 'switch', switchChecked: false },
        ],
      },
      {
        groupLabel: 'عنوان مجموعة',
        items: [
          { label: 'نص العنصر', leadIcon: 'list', trailElement: 'tag', trailTagText: 'وسم' },
          { label: 'نص العنصر', leadIcon: 'list', trailElement: 'text', trailText: '+99' },
          { label: 'نص العنصر', leadIcon: 'list', trailElement: 'button' },
          { label: 'نص العنصر', leadIcon: 'list', state: 'disabled' },
        ],
      },
    ]}
  />
</div>

---

{/* ── Props ─────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 24 }}>Props</h2>

  <h3 style={{ fontSize: 16, fontWeight: 600, color: '#111827', marginBottom: 16 }}>MenuListItem</h3>
  <table style={{ borderCollapse: 'collapse', fontSize: 13, width: '100%', marginBottom: 32 }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Prop</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Type</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Default</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Description</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['label',          'string',                                                                          "'Item Label'",   'The item text.'],
        ['leadIcon',       'string | null',                                                                   "'list'",         'Icon name for the leading icon. Pass null to hide it (text-only style).'],
        ['trailElement',   "'none'|'text'|'icon'|'button'|'tag'|'switch'",                                  "'none'",         'The trailing element type.'],
        ['trailText',      'string',                                                                          "'+99'",          "Text shown when trailElement='text'."],
        ['trailIcon',      'string',                                                                          "'tick'",         "Icon name when trailElement='icon'."],
        ['trailTagText',   'string',                                                                          "'Label'",        "Tag label text when trailElement='tag'."],
        ['switchChecked',  'boolean',                                                                         'false',          "Checked state when trailElement='switch'."],
        ['onSwitchChange', 'function',                                                                        '—',              "Called with new boolean value when switch is toggled."],
        ['state',          "'default'|'hovered'|'pressed'|'focused'|'disabled'|'selected'",                 "'default'",      'Visual state of the item.'],
        ['subItem',        'boolean',                                                                         'false',          'Marks as a sub-item (semantic flag, no visual indent).'],
        ['rtl',            'boolean',                                                                         'false',          'Right-to-left layout.'],
        ['onClick',        'function',                                                                         '—',              'Click handler (disabled when state=disabled).'],
      ].map(([prop, type, def, desc]) => (
        <tr key={prop} style={{ borderTop: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#1849a9' }}>{prop}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#6b7280', fontSize: 12 }}>{type}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#374151' }}>{def}</td>
          <td style={{ padding: '10px 12px', color: '#374151' }}>{desc}</td>
        </tr>
      ))}
    </tbody>
  </table>

  <h3 style={{ fontSize: 16, fontWeight: 600, color: '#111827', marginBottom: 16 }}>Menu</h3>
  <table style={{ borderCollapse: 'collapse', fontSize: 13, width: '100%' }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Prop</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Type</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Default</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Description</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['sections', "Array<{ groupLabel?: string, items: MenuListItem[] }>", '[]',       'Array of sections. Each section has an optional label and an array of item prop objects.'],
        ['style',    "'all'|'text-only'|'text-icon'",                         "'all'",    'all = lead icon always shown. text-only = lead icon hidden. text-icon = lead icon shown per item.'],
        ['rtl',      'boolean',                                                'false',    'Propagates RTL to all items and section labels.'],
      ].map(([prop, type, def, desc]) => (
        <tr key={prop} style={{ borderTop: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#1849a9' }}>{prop}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#6b7280', fontSize: 12 }}>{type}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#374151' }}>{def}</td>
          <td style={{ padding: '10px 12px', color: '#374151' }}>{desc}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

{/* ── Design Tokens ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 24 }}>Design Tokens</h2>

  <table style={{ borderCollapse: 'collapse', fontSize: 13, width: '100%' }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Token</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Value</th>
        <th style={{ padding: '8px 12px', textAlign: 'left', color: '#374151', borderBottom: '1px solid #e5e7eb' }}>Usage</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['Item text / icon', '#000B36', 'Default label and icon color'],
        ['Item bg — Hovered / Selected', '#E5E7EB', 'Background on hover and selection'],
        ['Item bg — Pressed', '#D2D6DB', 'Background while pressing'],
        ['Item focus outline', '#00001C 2px', 'Focus ring color and width'],
        ['Item — Disabled color', '#6C7C96', 'Text and icon color in disabled state'],
        ['Trail text color', '#3C5073', 'Color of trailing text element (e.g. "+99")'],
        ['Trail button / Tag bg', '#F3F4F6', 'Background for button and tag trail elements'],
        ['Tag border', '#D2D6DB', 'Border color of the tag trail element'],
        ['Tag text', '12px / 500 / 18lh', 'Typography for tag label'],
        ['Switch track (off)', 'border #1F2A37', 'Unfilled track with dark border'],
        ['Switch thumb (off)', '#1F2A37', 'Dark filled circle on left'],
        ['Switch (on)', 'bg #1F2A37 + white thumb', 'Filled track, white thumb slides right'],
        ['Menu panel bg', '#FFFFFF', 'Menu background'],
        ['Menu panel border', '#E5E7EB 1px r=8px', 'Menu border and corner radius'],
        ['Section divider', '#E5E7EB 1px', 'Horizontal rule between sections'],
        ['Group label', '#000B36 12px/700/18lh uppercase', 'Section header typography'],
        ['Item height', '40px', 'Minimum item row height'],
        ['Item padding', '8px all sides', 'Internal item padding'],
        ['Item gap', '8px', 'Gap between lead icon, label, and trail element'],
        ['Item border-radius', '4px', 'Corner radius of each item'],
        ['Font', 'IBM Plex Sans Arabic', 'All text, LTR and RTL'],
        ['Label typography', '16px / 400 / 24lh', 'Item label font settings'],
      ].map(([token, value, usage]) => (
        <tr key={token} style={{ borderTop: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 12px', color: '#374151', fontWeight: 500 }}>{token}</td>
          <td style={{ padding: '10px 12px', fontFamily: 'monospace', color: '#1849a9', fontSize: 12 }}>{value}</td>
          <td style={{ padding: '10px 12px', color: '#6b7280' }}>{usage}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

{/* ── Usage ─────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 24 }}>Usage Examples</h2>

  <h3 style={{ fontSize: 16, fontWeight: 600, color: '#111827', marginBottom: 12 }}>Standalone MenuListItem</h3>
  <pre style={{ background: '#f9fafb', border: '1px solid #e5e7eb', borderRadius: 6, padding: 20, fontSize: 13, lineHeight: 1.7, overflow: 'auto' }}>{`import { MenuListItem } from './Menu';

// Default item with lead icon
<MenuListItem label="Edit" leadIcon="edit" />

// Selected item with tick trail
<MenuListItem label="Dark Mode" leadIcon="eye" trailElement="icon" trailIcon="tick" state="selected" />

// Item with switch
<MenuListItem label="Notifications" leadIcon="bell" trailElement="switch" switchChecked={true} onSwitchChange={setNotif} />

// Disabled item
<MenuListItem label="Delete" leadIcon="delete" state="disabled" />

// RTL item
<MenuListItem label="نص العنصر" leadIcon="list" rtl trailElement="tag" trailTagText="وسم" />`}</pre>

  <h3 style={{ fontSize: 16, fontWeight: 600, color: '#111827', marginBottom: 12, marginTop: 32 }}>Full Menu Panel</h3>
  <pre style={{ background: '#f9fafb', border: '1px solid #e5e7eb', borderRadius: 6, padding: 20, fontSize: 13, lineHeight: 1.7, overflow: 'auto' }}>{`import { Menu } from './Menu';

<Menu
  style="all"
  sections={[
    {
      groupLabel: 'ACCOUNT',
      items: [
        { label: 'Profile',    leadIcon: 'user',     state: 'selected', trailElement: 'icon', trailIcon: 'tick' },
        { label: 'Settings',   leadIcon: 'setting',  trailElement: 'none' },
        { label: 'Dark Mode',  leadIcon: 'eye',      trailElement: 'switch', switchChecked: false },
      ],
    },
    {
      groupLabel: 'ACTIONS',
      items: [
        { label: 'Notifications', leadIcon: 'bell',   trailElement: 'text',   trailText: '+5' },
        { label: 'Download',      leadIcon: 'download', trailElement: 'button' },
        { label: 'Delete',        leadIcon: 'delete',   state: 'disabled' },
      ],
    },
  ]}
/>

// RTL Arabic menu
<Menu style="all" rtl sections={[
  { groupLabel: 'عنوان', items: [{ label: 'نص العنصر', leadIcon: 'list' }] },
]} />`}</pre>
</div>

---

{/* ── Accessibility ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 22, fontWeight: 600, color: '#111827', marginBottom: 16 }}>Accessibility</h2>
  <ul style={{ fontSize: 14, color: '#374151', lineHeight: 1.8, paddingLeft: 20 }}>
    <li>The <code>Menu</code> root has <code>role="menu"</code>. Each <code>MenuListItem</code> has <code>role="menuitem"</code>.</li>
    <li>Disabled items get <code>aria-disabled="true"</code> and <code>tabIndex="-1"</code> — they cannot receive focus.</li>
    <li>Selected items get <code>aria-selected="true"</code> for screen reader announcement.</li>
    <li>The Switch trail element has <code>role="switch"</code> and <code>aria-checked</code> for state announcement.</li>
    <li>Focus is managed via <code>tabIndex={0}</code> on each enabled item. Keyboard users can press <code>Enter</code> or <code>Space</code> to activate an item.</li>
    <li>The <code>rtl</code> prop sets <code>dir="rtl"</code> on the item for correct screen reader reading order.</li>
  </ul>
</div>

</div>
