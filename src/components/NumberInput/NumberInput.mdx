{/* NumberInput.mdx — Masterteam Design System */}
import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as NumberInputStories from './NumberInput.stories';

<Meta of={NumberInputStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif" }}>

{/* ── Header ───────────────────────────────────────────────── */}
<div style={{ marginBottom: 40 }}>
  <h1 style={{ fontSize: 28, fontWeight: 700, marginBottom: 12, color: '#000b36' }}>Number Input</h1>
  <p style={{ margin: '0 0 16px', fontSize: 15, color: '#374151', lineHeight: 1.6, maxWidth: 700 }}>
    A numeric stepper composed of two components: a standalone <strong>Input Prefix-Suffix</strong> button
    (Plus or Minus, in Solid or Subtle style) and a full <strong>Number Input</strong> field that combines
    the buttons with a text field, label, and helper text. Supports 6 states, 3 input styles, 2 sizes,
    error feedback, and RTL layout.
  </p>
  <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginTop: 16 }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6, background: '#ecfdf5', color: '#065f46', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#059669', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ display: 'inline-flex', alignItems: 'center', background: '#eff3fb', color: '#1849a9', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>6 States</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', background: '#eff3fb', color: '#1849a9', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>3 Input Styles</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', background: '#eff3fb', color: '#1849a9', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>2 Sizes</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', background: '#eff3fb', color: '#1849a9', padding: '3px 10px', borderRadius: 100, fontSize: 12, fontWeight: 500 }}>RTL ✓</span>
  </div>
</div>

{/* ── Table of Contents ────────────────────────────────────── */}
<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '16px 20px', marginBottom: 40, display: 'inline-block', minWidth: 220 }}>
  <p style={{ margin: '0 0 8px', fontSize: 12, fontWeight: 700, color: '#000b36', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Contents</p>
  <ol style={{ margin: 0, padding: '0 0 0 16px', fontSize: 13, color: '#1849a9', lineHeight: 2 }}>
    <li><a href="#overview" style={{ color: '#1849a9', textDecoration: 'none' }}>Overview</a></li>
    <li><a href="#states" style={{ color: '#1849a9', textDecoration: 'none' }}>All States</a></li>
    <li><a href="#error" style={{ color: '#1849a9', textDecoration: 'none' }}>Error States</a></li>
    <li><a href="#sizes" style={{ color: '#1849a9', textDecoration: 'none' }}>Sizes</a></li>
    <li><a href="#input-styles" style={{ color: '#1849a9', textDecoration: 'none' }}>Input Styles</a></li>
    <li><a href="#button-styles" style={{ color: '#1849a9', textDecoration: 'none' }}>Button Styles</a></li>
    <li><a href="#labels" style={{ color: '#1849a9', textDecoration: 'none' }}>Label Variants</a></li>
    <li><a href="#rtl" style={{ color: '#1849a9', textDecoration: 'none' }}>RTL</a></li>
    <li><a href="#prefix-suffix-states" style={{ color: '#1849a9', textDecoration: 'none' }}>Input Prefix-Suffix — States</a></li>
    <li><a href="#prefix-suffix-sizes" style={{ color: '#1849a9', textDecoration: 'none' }}>Input Prefix-Suffix — Sizes</a></li>
    <li><a href="#props" style={{ color: '#1849a9', textDecoration: 'none' }}>Props</a></li>
    <li><a href="#tokens" style={{ color: '#1849a9', textDecoration: 'none' }}>Design Tokens</a></li>
    <li><a href="#accessibility" style={{ color: '#1849a9', textDecoration: 'none' }}>Accessibility</a></li>
  </ol>
</div>

{/* ── Overview ─────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="overview">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 16 }}>Overview</h2>
  <Canvas of={NumberInputStories.Playground} />
  <Controls of={NumberInputStories.Playground} />
</div>

{/* ── All States ────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="states">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>All States</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    6 interactive states. Focused replaces the bottom border with a 2px <code>#1849A9</code> underline.
    Disabled and Read-only remove the fill and mute the border.
  </p>
  <Canvas of={NumberInputStories.AllStates} />
</div>

{/* ── Error States ──────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="error">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Error States</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    When <code>error=true</code>, the border turns <code>#A30000</code> and the helper text turns red
    (<code>#B42318</code>) with a warning icon.
  </p>
  <Canvas of={NumberInputStories.ErrorStates} />
</div>

{/* ── Sizes ─────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="sizes">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Sizes</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    Large (40px field, 56px buttons) and Medium (32px field, 48px buttons).
  </p>
  <Canvas of={NumberInputStories.Sizes} />
</div>

{/* ── Input Styles ──────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="input-styles">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Input Styles</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    Three fill styles for the field background. <em>Filled lighter</em> and <em>Filled darker</em> remove the border.
  </p>
  <Canvas of={NumberInputStories.InputStyles} />
</div>

{/* ── Button Styles ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="button-styles">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Button Styles</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    <strong>Solid</strong> buttons have a <code>#F3F4F6</code> background. <strong>Subtle</strong> buttons are transparent and only show a background on interaction.
  </p>
  <Canvas of={NumberInputStories.ButtonStyles} />
</div>

{/* ── Label Variants ────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="labels">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Label Variants</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    Labels support Regular (400) and Semibold (600) weight, a Required asterisk, Disabled state, and two sizes (Medium 14px, Large 16px).
  </p>
  <Canvas of={NumberInputStories.LabelVariants} />
</div>

{/* ── RTL ───────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="rtl">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>RTL</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    In RTL mode, the layout mirrors: the Minus button appears on the right and Plus on the left. Text alignment and label follow the RTL direction.
  </p>
  <Canvas of={NumberInputStories.RTLNumberInput} />
</div>

{/* ── Input Prefix-Suffix — States ─────────────────────────── */}
<div style={{ marginBottom: 48 }} id="prefix-suffix-states">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Input Prefix-Suffix — States</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    The standalone Plus and Minus buttons in both Solid and Subtle styles across all 6 states.
  </p>
  <Canvas of={NumberInputStories.PrefixSuffixAllStates} />
</div>

{/* ── Input Prefix-Suffix — Sizes ───────────────────────────── */}
<div style={{ marginBottom: 48 }} id="prefix-suffix-sizes">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Input Prefix-Suffix — Sizes</h2>
  <p style={{ margin: '0 0 20px', fontSize: 15, color: '#374151', lineHeight: 1.6 }}>
    Large (56×40px) and Medium (48×32px) in both Solid and Subtle styles.
  </p>
  <Canvas of={NumberInputStories.PrefixSuffixSizes} />
</div>

{/* ── Props ─────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="props">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 20 }}>Props</h2>

  <h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 12 }}>NumberInput</h3>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr 1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13, marginBottom: 32 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Prop</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Type</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Default</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Description</div>

    {[
      ['value',          'number | string',                                                                   '—',             'Controlled value'],
      ['onChange',       'function(n: number)',                                                               '—',             'Called with new numeric value'],
      ['label',          'string',                                                                            "'Label'",       'Label text shown above the field'],
      ['showLabel',      'boolean',                                                                           'true',          'Show or hide the label row'],
      ['helperText',     'string',                                                                            "'Help Text'",   'Helper text below the field'],
      ['showHelperText', 'boolean',                                                                           'false',         'Show or hide helper text'],
      ['placeholder',    'string',                                                                            "'00000'",       'Placeholder shown when empty'],
      ['state',          "'default'|'hovered'|'pressed'|'focused'|'disabled'|'read-only'",                  "'default'",     'Visual/interactive state'],
      ['error',          'boolean',                                                                           'false',         'Error state — red border, red helper text'],
      ['size',           "'large'|'medium'",                                                                  "'large'",       'Field and button height'],
      ['inputStyle',     "'default'|'filled-lighter'|'filled-darker'",                                       "'default'",     'Background fill style'],
      ['btnStyle',       "'solid'|'subtle'",                                                                  "'solid'",       'Plus/Minus button background style'],
      ['labelType',      "'regular'|'semibold'",                                                              "'regular'",     'Label font weight'],
      ['required',       'boolean',                                                                           'false',         'Show required asterisk on label'],
      ['rtl',            'boolean',                                                                           'false',         'RTL layout'],
      ['min',            'number',                                                                            '—',             'Minimum allowed value'],
      ['max',            'number',                                                                            '—',             'Maximum allowed value'],
      ['step',           'number',                                                                            '1',             'Increment/decrement amount'],
    ].map(([prop, type, def, desc]) => (
      <>
        <div key={`${prop}-p`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#1849a9', fontSize: 12 }}>{prop}</code></div>
        <div key={`${prop}-t`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#374151', fontSize: 12 }}>{type}</code></div>
        <div key={`${prop}-d`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#374151', fontSize: 12 }}>{def}</code></div>
        <div key={`${prop}-desc`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>{desc}</div>
      </>
    ))}
  </div>

  <h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 12 }}>InputPrefixSuffix</h3>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr 1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Prop</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Type</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Default</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Description</div>

    {[
      ['type',     "'plus'|'minus'",                                                                 "'plus'",    'Plus (+) or Minus (−) icon'],
      ['state',    "'default'|'hovered'|'pressed'|'focused'|'selected'|'disabled'",                 "'default'", 'Visual state'],
      ['btnStyle', "'solid'|'subtle'",                                                               "'solid'",   'Solid has a bg fill; subtle is transparent'],
      ['size',     "'large'|'medium'",                                                               "'large'",   'Large 56×40px or Medium 48×32px'],
      ['onClick',  'function',                                                                       '—',         'Click handler'],
    ].map(([prop, type, def, desc]) => (
      <>
        <div key={`${prop}-p`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#1849a9', fontSize: 12 }}>{prop}</code></div>
        <div key={`${prop}-t`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#374151', fontSize: 12 }}>{type}</code></div>
        <div key={`${prop}-d`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb' }}><code style={{ color: '#374151', fontSize: 12 }}>{def}</code></div>
        <div key={`${prop}-desc`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>{desc}</div>
      </>
    ))}
  </div>
</div>

{/* ── Design Tokens ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="tokens">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 20 }}>Design Tokens</h2>

  <div style={{ display: 'grid', gridTemplateColumns: '2fr 1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Token</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Value</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Usage</div>

    {[
      { token: 'Default border',       value: '#D2D6DB', usage: 'Input field border in default state' },
      { token: 'Hovered border',       value: '#1F2A37', usage: 'Input field border on hover' },
      { token: 'Pressed bg',           value: '#F3F4F6', usage: 'Input field background on press' },
      { token: 'Focused underline',    value: '#1849A9', usage: '2px bottom border in focused state' },
      { token: 'Disabled border',      value: '#E5E7EB', usage: 'Input field border when disabled' },
      { token: 'Read-only border',     value: '#CED3DC', usage: 'Input field border when read-only' },
      { token: 'Error border',         value: '#A30000', usage: 'Input field border in error state' },
      { token: 'Filled lighter bg',    value: '#FCFCFD', usage: 'Background for filled-lighter style' },
      { token: 'Filled darker bg',     value: '#F3F4F6', usage: 'Background for filled-darker style' },
      { token: 'Label text',           value: '#000B36', usage: 'Label colour' },
      { token: 'Required asterisk',    value: '#A30000', usage: 'Required * marker' },
      { token: 'Placeholder',          value: '#B6BDCA', usage: 'Placeholder text colour' },
      { token: 'Value text',           value: '#000B36', usage: 'Entered value colour (16px/400)' },
      { token: 'Helper text',          value: '#384250', usage: 'Helper text in normal state' },
      { token: 'Helper error',         value: '#B42318', usage: 'Helper text in error state' },
      { token: 'Button solid default', value: '#F3F4F6', usage: 'Solid button background' },
      { token: 'Button hovered',       value: '#E5E7EB', usage: 'Button background on hover/selected' },
      { token: 'Button pressed',       value: '#D2D6DB', usage: 'Button background on press' },
      { token: 'Button focus ring',    value: '#00001C', usage: 'Focus outline for keyboard navigation' },
    ].map(({ token, value, usage }) => (
      <>
        <div key={`${token}-t`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>{token}</div>
        <div key={`${token}-v`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', display: 'flex', alignItems: 'center', gap: 8 }}>
          <span style={{ width: 14, height: 14, borderRadius: 3, background: value, border: '1px solid #e5e7eb', display: 'inline-block', flexShrink: 0 }} />
          <code style={{ color: '#374151', fontSize: 12 }}>{value}</code>
        </div>
        <div key={`${token}-u`} style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#6b7280' }}>{usage}</div>
      </>
    ))}
  </div>
</div>

{/* ── Accessibility ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }} id="accessibility">
  <h2 style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 20 }}>Accessibility</h2>

  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', borderRadius: 8, overflow: 'hidden', border: '1px solid #e5e7eb', fontSize: 13 }}>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Concern</div>
    <div style={{ background: '#f9fafb', padding: '8px 12px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Implementation</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Input role & label</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>The field uses <code style={{ color: '#1849a9', fontSize: 12 }}>type="number"</code>. The visible <code style={{ color: '#1849a9', fontSize: 12 }}>label</code> is associated via <code style={{ color: '#1849a9', fontSize: 12 }}>htmlFor</code> / <code style={{ color: '#1849a9', fontSize: 12 }}>id</code> so screen readers announce it on focus.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Value constraints</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>The <code style={{ color: '#1849a9', fontSize: 12 }}>min</code>, <code style={{ color: '#1849a9', fontSize: 12 }}>max</code>, and <code style={{ color: '#1849a9', fontSize: 12 }}>step</code> props map to native <code style={{ color: '#1849a9', fontSize: 12 }}>aria-valuemin</code>, <code style={{ color: '#1849a9', fontSize: 12 }}>aria-valuemax</code>, and the input's native number constraints.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Error feedback</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>When <code style={{ color: '#1849a9', fontSize: 12 }}>error=true</code>, the field receives <code style={{ color: '#1849a9', fontSize: 12 }}>aria-invalid="true"</code> and the helper text is linked via <code style={{ color: '#1849a9', fontSize: 12 }}>aria-describedby</code> for screen reader announcement.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Required fields</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>The <code style={{ color: '#1849a9', fontSize: 12 }}>required</code> prop sets <code style={{ color: '#1849a9', fontSize: 12 }}>aria-required="true"</code> on the input in addition to showing the visual asterisk on the label.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Disabled state</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>Disabled inputs and buttons receive the native <code style={{ color: '#1849a9', fontSize: 12 }}>disabled</code> attribute, preventing focus and interaction while remaining readable in the accessibility tree.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>Keyboard operation</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>The Plus and Minus buttons are focusable via <code style={{ color: '#1849a9', fontSize: 12 }}>Tab</code> and activated with <code style={{ color: '#1849a9', fontSize: 12 }}>Enter</code> or <code style={{ color: '#1849a9', fontSize: 12 }}>Space</code>. Arrow keys on the native number input increment and decrement by the <code style={{ color: '#1849a9', fontSize: 12 }}>step</code> value.</div>

    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151', fontWeight: 500 }}>RTL support</div>
    <div style={{ padding: '10px 12px', borderTop: '1px solid #e5e7eb', color: '#374151' }}>The <code style={{ color: '#1849a9', fontSize: 12 }}>rtl</code> prop sets <code style={{ color: '#1849a9', fontSize: 12 }}>dir="rtl"</code> on the wrapper, reversing button and text layout for Arabic and other RTL languages.</div>
  </div>
</div>

</div>
