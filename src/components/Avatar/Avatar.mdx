import { Meta } from '@storybook/addon-docs/blocks';
import { Avatar, AvatarGroup, AvatarWithLabel, FIGMA_PHOTO, ARAB_PHOTOS, LABEL_PHOTO } from './Avatar';
import * as AvatarStories from './Avatar.stories';

<Meta of={AvatarStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif" }}>

<div style={{ borderBottom: '1px solid #e5e7eb', paddingBottom: 12, marginBottom: 24 }}>
  <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 6 }}>Components / Avatar</div>
  <h1 style={{ margin: 0, fontSize: 28, fontWeight: 700, color: '#000b36' }}>Avatar</h1>
  <p style={{ margin: '8px 0 0', fontSize: 14, color: '#3c5073', lineHeight: 1.6 }}>A visual representation of a user, displayed in profiles, comments, or messaging apps. Supports initials, icon, and image types across 7 sizes, with round and square shapes, optional status indicators, and group layouts.</p>
  <div style={{ display: 'flex', gap: 8, marginTop: 12, flexWrap: 'wrap' }}>
    <span style={{ background: '#d1fae5', color: '#059669', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20 }}>Available</span>
    <span style={{ background: '#f0f4ff', color: '#1849a9', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20 }}>Node ID: 4113:655</span>
    <a href="https://design.dga.gov.sa/guidelines/components/data-display/avatar" target="_blank" rel="noreferrer" style={{ background: '#fdf4ff', color: '#7c3aed', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20, textDecoration: 'none' }}>DGA Guidelines ↗</a>
    <a href="https://www.figma.com/design/WTmRAkJVvw0IvZMA7wBdTC/Masterteam-Ds-For-Experiment-ONLY?node-id=4113-655" target="_blank" rel="noreferrer" style={{ background: '#fff7ed', color: '#c2410c', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20, textDecoration: 'none' }}>Figma ↗</a>
  </div>
</div>

<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '14px 20px', marginBottom: 24 }}>
  <div style={{ fontSize: 12, fontWeight: 700, color: '#1849a9', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.07em' }}>On this page</div>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '6px 0' }}>
    {[['Overview','#overview'],['Anatomy','#anatomy'],['Types','#types'],['Sizes','#sizes'],['Shape','#shape'],['Status Indicators','#status-indicators'],['Avatar Group','#avatar-group'],['Avatar with Label','#avatar-with-label'],['RTL Support','#rtl-support'],['Token Table','#token-table'],['Props Reference','#props-reference'],["Do / Don't",'#do--dont'],['Accessibility','#accessibility']].map(([label, href]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none', lineHeight: 2 }}>{label}</a>
    ))}
  </div>
</div>

---

## Overview

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 12, padding: '24px 20px', marginBottom: 8, overflow: 'auto' }}>

  <div style={{ display: 'grid', gridTemplateColumns: '80px repeat(7, 1fr)', gap: 8, marginBottom: 20, minWidth: 640 }}>
    <div />
    {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
      <div key={s} style={{ textAlign: 'center' }}>
        <div style={{ fontSize: 12, fontWeight: 700, color: '#374151' }}>{s}</div>
        <div style={{ fontSize: 10, color: '#9ca3af', marginTop: 1 }}>{ {xs:24,sm:32,md:40,lg:48,xl:64,'2xl':80,'3xl':120}[s] }px</div>
      </div>
    ))}
  </div>

  <div style={{ fontSize: 10, fontWeight: 700, letterSpacing: '.09em', color: '#9ca3af', textTransform: 'uppercase', marginBottom: 10, paddingLeft: 4 }}>Round</div>

  <div style={{ display: 'grid', gridTemplateColumns: '80px repeat(7, 1fr)', gap: 8, alignItems: 'flex-end', marginBottom: 16, minWidth: 640 }}>
    <div style={{ fontSize: 11, color: '#6b7280', textAlign: 'right', paddingRight: 12, paddingBottom: 4 }}>Initials</div>
    {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
      <div key={s} style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end' }}>
        <Avatar type="initials" size={s} initials="AB" />
      </div>
    ))}
  </div>

  <div style={{ display: 'grid', gridTemplateColumns: '80px repeat(7, 1fr)', gap: 8, alignItems: 'flex-end', marginBottom: 16, minWidth: 640 }}>
    <div style={{ fontSize: 11, color: '#6b7280', textAlign: 'right', paddingRight: 12, paddingBottom: 4 }}>Icon</div>
    {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
      <div key={s} style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end' }}>
        <Avatar type="icon" size={s} />
      </div>
    ))}
  </div>

  <div style={{ display: 'grid', gridTemplateColumns: '80px repeat(7, 1fr)', gap: 8, alignItems: 'flex-end', marginBottom: 20, minWidth: 640 }}>
    <div style={{ fontSize: 11, color: '#6b7280', textAlign: 'right', paddingRight: 12, paddingBottom: 4 }}>Image</div>
    {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
      <div key={s} style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end' }}>
        <Avatar type="image" size={s} src={FIGMA_PHOTO} alt="User" />
      </div>
    ))}
  </div>

  <div style={{ height: 1, background: '#e5e7eb', margin: '4px 0 16px' }} />
  <div style={{ fontSize: 10, fontWeight: 700, letterSpacing: '.09em', color: '#9ca3af', textTransform: 'uppercase', marginBottom: 10, paddingLeft: 4 }}>Square</div>

  <div style={{ display: 'grid', gridTemplateColumns: '80px repeat(7, 1fr)', gap: 8, alignItems: 'flex-end', marginBottom: 16, minWidth: 640 }}>
    <div style={{ fontSize: 11, color: '#6b7280', textAlign: 'right', paddingRight: 12, paddingBottom: 4 }}>Initials</div>
    {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
      <div key={s} style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end' }}>
        <Avatar type="initials" size={s} square initials="AB" />
      </div>
    ))}
  </div>

  <div style={{ display: 'grid', gridTemplateColumns: '80px repeat(7, 1fr)', gap: 8, alignItems: 'flex-end', marginBottom: 16, minWidth: 640 }}>
    <div style={{ fontSize: 11, color: '#6b7280', textAlign: 'right', paddingRight: 12, paddingBottom: 4 }}>Icon</div>
    {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
      <div key={s} style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end' }}>
        <Avatar type="icon" size={s} square />
      </div>
    ))}
  </div>

  <div style={{ display: 'grid', gridTemplateColumns: '80px repeat(7, 1fr)', gap: 8, alignItems: 'flex-end', minWidth: 640 }}>
    <div style={{ fontSize: 11, color: '#6b7280', textAlign: 'right', paddingRight: 12, paddingBottom: 4 }}>Image</div>
    {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
      <div key={s} style={{ display: 'flex', justifyContent: 'center', alignItems: 'flex-end' }}>
        <Avatar type="image" size={s} square src={FIGMA_PHOTO} alt="User" />
      </div>
    ))}
  </div>

</div>

<div style={{ fontSize: 12, color: '#6b7280', lineHeight: 1.6, marginBottom: 8 }}>
  3 types × 7 sizes × 2 shapes = 42 variants. All sourced from Figma node <code style={{ fontSize: 11 }}>4113:655</code>. Text color token: <code style={{ fontSize: 11 }}>--avatar/text/default (#233a61)</code>.
</div>

---

## Anatomy

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: 12, marginBottom: 24 }}>
{[
  ['1','Container',       'Circular or square frame. Size controls width & height via Figma sizing tokens.'],
  ['2','Background',      'Transparent root + rgba(255,255,255,0.2) alpha-white overlay from Figma "bg" layer.'],
  ['3','White ring',      '2px solid white border — always present. Creates visual stacking separation in groups.'],
  ['4','Content',         'Initials text (up to 2 chars), person icon from Figma assets, or photo image.'],
  ['5','Status dot',      'Scaled indicator at bottom-end corner. Optional; 6 states available.'],
  ['6','Overflow badge',  '"+N" bubble in AvatarGroup when visible children exceed the max prop.'],
].map(([n, title, desc]) => (
  <div key={n} style={{ border: '1px solid #e5e7eb', borderRadius: 10, padding: 16, background: '#fff' }}>
    <div style={{ width: 24, height: 24, borderRadius: 6, background: '#1849a9', color: '#fff', fontSize: 12, fontWeight: 700, display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: 10 }}>{n}</div>
    <div style={{ fontWeight: 600, fontSize: 13, color: '#111827', marginBottom: 4 }}>{title}</div>
    <div style={{ fontSize: 12, color: '#6b7280', lineHeight: 1.5 }}>{desc}</div>
  </div>
))}
</div>

---

## Types

<div style={{ display: 'flex', gap: 40, flexWrap: 'wrap', alignItems: 'flex-end', padding: '24px 20px', background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10, marginBottom: 16 }}>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
    <Avatar type="initials" size="xl" initials="AB" />
    <div style={{ textAlign: 'center' }}>
      <div style={{ fontSize: 13, fontWeight: 600, color: '#111827' }}>Initials</div>
      <code style={{ fontSize: 11, color: '#6b7280' }}>type="initials"</code>
    </div>
  </div>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
    <Avatar type="icon" size="xl" />
    <div style={{ textAlign: 'center' }}>
      <div style={{ fontSize: 13, fontWeight: 600, color: '#111827' }}>Icon</div>
      <code style={{ fontSize: 11, color: '#6b7280' }}>type="icon"</code>
    </div>
  </div>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
    <Avatar type="image" size="xl" src={FIGMA_PHOTO} alt="User photo" />
    <div style={{ textAlign: 'center' }}>
      <div style={{ fontSize: 13, fontWeight: 600, color: '#111827' }}>Image</div>
      <code style={{ fontSize: 11, color: '#6b7280' }}>type="image"</code>
    </div>
  </div>
</div>

| Type | When to use |
|---|---|
| `initials` | No photo available; show first 2 characters of the user's name |
| `icon` | Anonymous user or generic placeholder |
| `image` | Full profile photo is available |

---

## Sizes

Seven sizes from Figma sizing tokens. All sizes support every type × shape combination.

<div style={{ padding: '20px 20px 16px', background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10, marginBottom: 16 }}>
  <div style={{ fontSize: 11, fontWeight: 600, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.07em', marginBottom: 12 }}>Round — Initials</div>
  <div style={{ display: 'flex', alignItems: 'flex-end', gap: 20, flexWrap: 'wrap', marginBottom: 24 }}>
    {[
      { size:'xs',  px:24  },
      { size:'sm',  px:32  },
      { size:'md',  px:40  },
      { size:'lg',  px:48  },
      { size:'xl',  px:64  },
      { size:'2xl', px:80  },
      { size:'3xl', px:120 },
    ].map(({ size, px }) => (
      <div key={size} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
        <Avatar type="initials" size={size} initials="AB" />
        <div style={{ fontSize: 11, fontWeight: 700, color: '#374151', textAlign: 'center' }}>{size}</div>
        <div style={{ fontSize: 10, color: '#9ca3af', textAlign: 'center' }}>{px}px</div>
      </div>
    ))}
  </div>
  <div style={{ fontSize: 11, fontWeight: 600, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.07em', marginBottom: 12 }}>Square — Initials</div>
  <div style={{ display: 'flex', alignItems: 'flex-end', gap: 20, flexWrap: 'wrap' }}>
    {[
      { size:'xs',  px:24  },
      { size:'sm',  px:32  },
      { size:'md',  px:40  },
      { size:'lg',  px:48  },
      { size:'xl',  px:64  },
      { size:'2xl', px:80  },
      { size:'3xl', px:120 },
    ].map(({ size, px }) => (
      <div key={size} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
        <Avatar type="initials" size={size} square initials="AB" />
        <div style={{ fontSize: 11, fontWeight: 700, color: '#374151', textAlign: 'center' }}>{size}</div>
        <div style={{ fontSize: 10, color: '#9ca3af', textAlign: 'center' }}>{px}px</div>
      </div>
    ))}
  </div>
</div>

<div style={{ display: 'grid', gridTemplateColumns: '70px 52px 80px 1fr', gap: 0, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24, fontSize: 12 }}>
  <div style={{ padding: '8px 12px', background: '#f8fafc', fontWeight: 700, fontSize: 11, color: '#374151', borderBottom: '2px solid #e5e7eb' }}>Size</div>
  <div style={{ padding: '8px 12px', background: '#f8fafc', fontWeight: 700, fontSize: 11, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>px</div>
  <div style={{ padding: '8px 12px', background: '#f8fafc', fontWeight: 700, fontSize: 11, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb' }}>Square radius</div>
  <div style={{ padding: '8px 12px', background: '#f8fafc', fontWeight: 700, fontSize: 11, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb' }}>Figma token</div>
  {[
    ['xs',  '24',  '4px',  '--avatar/sizing/avatar-sm'],
    ['sm',  '32',  '4px',  '--avatar/sizing/avatar-sm2'],
    ['md',  '40',  '4px',  '--avatar/sizing/avatar-md'],
    ['lg',  '48',  '4px',  '--avatar/sizing/avatar-md2'],
    ['xl',  '64',  '4px',  '--avatar/sizing/avatar-lg'],
    ['2xl', '80',  '4px',  '--avatar/sizing/avatar-lg2'],
    ['3xl', '120', '8px',  '--avatar/sizing/avatar-xl'],
  ].map(([s, px, r, tok]) => (
    <div key={s} style={{ display: 'contents' }}>
      <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', fontFamily: 'monospace', color: '#1849a9', fontWeight: 600 }}>{s}</div>
      <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', fontFamily: 'monospace', color: '#374151', textAlign: 'center' }}>{px}</div>
      <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', fontFamily: 'monospace', color: '#6b7280' }}>{r}</div>
      <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 11, color: '#6b7280' }}>{tok}</div>
    </div>
  ))}
</div>

---

## Shape

The `square` prop switches from circular (`border-radius: 9999px`) to a rounded square. Corner radius is 4px for xs–2xl and 8px for 3xl, matching Figma's `--radius/4` and `--radius/8` tokens.

<div style={{ display: 'flex', gap: 40, flexWrap: 'wrap', padding: '20px', background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10, marginBottom: 24 }}>
  <div>
    <div style={{ fontSize: 12, fontWeight: 600, color: '#374151', marginBottom: 14 }}>Round <code style={{ fontSize: 10, background: '#f3f4f6', padding: '1px 5px', borderRadius: 4, fontWeight: 400 }}>square=false</code></div>
    <div style={{ display: 'flex', alignItems: 'flex-end', gap: 12 }}>
      {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
        <Avatar key={s} type="initials" size={s} initials="AB" />
      ))}
    </div>
  </div>
  <div>
    <div style={{ fontSize: 12, fontWeight: 600, color: '#374151', marginBottom: 14 }}>Square <code style={{ fontSize: 10, background: '#f3f4f6', padding: '1px 5px', borderRadius: 4, fontWeight: 400 }}>square=true</code></div>
    <div style={{ display: 'flex', alignItems: 'flex-end', gap: 12 }}>
      {['xs','sm','md','lg','xl','2xl','3xl'].map(s => (
        <Avatar key={s} type="initials" size={s} square initials="AB" />
      ))}
    </div>
  </div>
</div>

Shape applies uniformly to all three types — initials, icon, and image:

<div style={{ display: 'flex', gap: 24, flexWrap: 'wrap', padding: '20px', background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 10, marginBottom: 24 }}>
  {['initials','icon','image'].map(t => (
    <div key={t} style={{ display: 'flex', flexDirection: 'column', gap: 12, alignItems: 'center' }}>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.07em', fontWeight: 600 }}>{t}</div>
      <div style={{ display: 'flex', gap: 12, alignItems: 'center' }}>
        <div style={{ textAlign: 'center' }}>
          <Avatar type={t} size="lg" initials="AB" src={FIGMA_PHOTO} alt="User" />
          <div style={{ fontSize: 10, color: '#9ca3af', marginTop: 4 }}>round</div>
        </div>
        <div style={{ textAlign: 'center' }}>
          <Avatar type={t} size="lg" square initials="AB" src={FIGMA_PHOTO} alt="User" />
          <div style={{ fontSize: 10, color: '#9ca3af', marginTop: 4 }}>square</div>
        </div>
      </div>
    </div>
  ))}
</div>

---

## Status Indicators

Six status types positioned at the bottom-end corner of the avatar. Dot size and border width scale with avatar size.

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10, padding: '20px', marginBottom: 16 }}>
  <div style={{ display: 'flex', gap: 28, flexWrap: 'wrap', alignItems: 'flex-start' }}>
    {[
      { status: 'online',  label: 'Online',  hex: '#12b76a', desc: 'Active / connected' },
      { status: 'offline', label: 'Offline', hex: '#98a2b3', desc: 'Not connected' },
      { status: 'away',    label: 'Away',    hex: '#f79009', desc: 'Idle / stepped away' },
      { status: 'notify',  label: 'Notify',  hex: '#f04438', desc: 'Unread notifications' },
      { status: 'primary', label: 'Primary', hex: '#1849a9', desc: 'Brand presence' },
      { status: 'badge',   label: 'Badge',   hex: '#175cd3', desc: 'Verified / role' },
    ].map(({ status, label, hex, desc }) => (
      <div key={status} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8, width: 80 }}>
        <Avatar type="initials" size="lg" initials="AB" status={status} notifyCount={status === 'notify' ? 2 : undefined} />
        <div style={{ textAlign: 'center' }}>
          <div style={{ fontSize: 12, fontWeight: 600, color: '#111827' }}>{label}</div>
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 4, marginTop: 3 }}>
            <div style={{ width: 8, height: 8, borderRadius: status === 'badge' ? 2 : '50%', background: hex, flexShrink: 0 }} />
            <code style={{ fontSize: 10, color: '#6b7280' }}>{hex}</code>
          </div>
          <div style={{ fontSize: 10, color: '#9ca3af', marginTop: 3, lineHeight: 1.4 }}>{desc}</div>
        </div>
      </div>
    ))}
  </div>
</div>

<div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 10, padding: '16px 20px', marginBottom: 24 }}>
  <div style={{ fontSize: 11, fontWeight: 600, color: '#6b7280', marginBottom: 12 }}>Dot size scale — Online status across all sizes</div>
  <div style={{ display: 'flex', alignItems: 'flex-end', gap: 16, flexWrap: 'wrap' }}>
    {[
      { size:'xs', dot:8 },  { size:'sm', dot:8 },  { size:'md', dot:10 },
      { size:'lg', dot:12 }, { size:'xl', dot:14 }, { size:'2xl', dot:16 }, { size:'3xl', dot:20 },
    ].map(({ size, dot }) => (
      <div key={size} style={{ textAlign: 'center' }}>
        <Avatar type="initials" size={size} initials="AB" status="online" />
        <div style={{ fontSize: 10, color: '#9ca3af', marginTop: 6 }}>{size}</div>
        <div style={{ fontSize: 10, color: '#6b7280' }}>{dot}px dot</div>
      </div>
    ))}
  </div>
</div>

---

## Avatar Group

`AvatarGroup` renders multiple avatars in a compact layout. Avatars beyond `max` collapse into an overflow "+N" badge.

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10, padding: '20px', marginBottom: 24 }}>
  <div style={{ marginBottom: 20 }}>
    <div style={{ fontSize: 12, fontWeight: 600, color: '#374151', marginBottom: 10 }}>Stacked <code style={{ fontSize: 10, background: '#f3f4f6', padding: '1px 5px', borderRadius: 4, fontWeight: 400 }}>stacked=true</code></div>
    <AvatarGroup stacked size="sm" max={4} overflowLabel="+1">
      {ARAB_PHOTOS.map((src, i) => (
        <Avatar key={i} type="image" size="sm" src={src} alt={`Person ${i + 1}`} />
      ))}
    </AvatarGroup>
    <div style={{ marginTop: 8, fontSize: 11, color: '#9ca3af' }}>5 photos, max=4 — overflowLabel controls the badge text</div>
  </div>
  <div style={{ height: 1, background: '#f3f4f6', marginBottom: 20 }} />
  <div>
    <div style={{ fontSize: 12, fontWeight: 600, color: '#374151', marginBottom: 10 }}>Spaced <code style={{ fontSize: 10, background: '#f3f4f6', padding: '1px 5px', borderRadius: 4, fontWeight: 400 }}>stacked=false</code></div>
    <AvatarGroup stacked={false} size="sm">
      {ARAB_PHOTOS.map((src, i) => (
        <Avatar key={i} type="image" size="sm" src={src} alt={`Person ${i + 1}`} />
      ))}
    </AvatarGroup>
    <div style={{ marginTop: 8, fontSize: 11, color: '#9ca3af' }}>4px gap between each avatar</div>
  </div>
</div>

---

## Avatar with Label

Pairs an avatar with a name and optional caption. Font sizes scale with the avatar `size` prop.

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10, padding: '20px', marginBottom: 24 }}>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
    {['sm','md','lg'].map(size => (
      <AvatarWithLabel
        key={size}
        size={size}
        type="image"
        src={LABEL_PHOTO}
        name="Ahmad Alwarda"
        caption="ahmad@email.com"
        dir="ltr"
      />
    ))}
  </div>
</div>

---

## RTL Support

Set `dir="rtl"` on `AvatarWithLabel` to flip label placement and move the status dot to the bottom-left corner.

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginBottom: 24 }}>
  <div style={{ padding: '20px', background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10 }}>
    <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.07em', marginBottom: 14 }}>LTR</div>
    <AvatarWithLabel size="md" type="image" src={LABEL_PHOTO} name="Ahmad Alwarda" caption="ahmad@email.com" dir="ltr" />
  </div>
  <div style={{ padding: '20px', background: '#fff', border: '1px solid #e5e7eb', borderRadius: 10 }}>
    <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.07em', marginBottom: 14 }}>RTL</div>
    <AvatarWithLabel size="md" type="image" src={LABEL_PHOTO} name="أحمد الوردة" caption="ahmad@email.com" dir="rtl" />
  </div>
</div>

---

## Token Table

<div style={{ display: 'flex', flexDirection: 'column', gap: 20, marginBottom: 24 }}>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
  <div style={{ background: '#f9fafb', padding: '10px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Container &amp; Border</div>
  <div style={{ display: 'grid', gridTemplateColumns: '240px 120px 1fr', background: '#f0f1f3', padding: '7px 16px', gap: 12, fontSize: 11, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb' }}><span>Token</span><span>Value</span><span>Usage</span></div>
  {[
    ['--avatar/border/default',      '#ffffff',  '2px white ring on bg layer; stacked group separation'],
    ['--avatar/sizing/avatar-sm',    '24px',     'xs size'],
    ['--avatar/sizing/avatar-sm2',   '32px',     'sm size'],
    ['--avatar/sizing/avatar-md',    '40px',     'md size'],
    ['--avatar/sizing/avatar-md2',   '48px',     'lg size'],
    ['--avatar/sizing/avatar-lg',    '64px',     'xl size'],
    ['--avatar/sizing/avatar-lg2',   '80px',     '2xl size'],
    ['--avatar/sizing/avatar-xl',    '120px',    '3xl size'],
    ['--radius/round',               '9999px',   'Round shape'],
    ['--radius/4',                   '4px',      'Square shape — xs through 2xl'],
    ['--radius/8',                   '8px',      'Square shape — 3xl only'],
  ].map(([t, v, d], i) => (
    <div key={t} style={{ display: 'grid', gridTemplateColumns: '240px 120px 1fr', padding: '9px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12 }}>
      <code style={{ fontSize: 11, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{t}</code>
      <span style={{ fontSize: 12, color: '#374151', fontFamily: 'monospace' }}>{v}</span>
      <span style={{ fontSize: 12, color: '#6b7280' }}>{d}</span>
    </div>
  ))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
  <div style={{ background: '#f9fafb', padding: '10px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Typography &amp; Color</div>
  <div style={{ display: 'grid', gridTemplateColumns: '240px 120px 1fr', background: '#f0f1f3', padding: '7px 16px', gap: 12, fontSize: 11, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb' }}><span>Token</span><span>Value</span><span>Usage</span></div>
  {[
    ['--avatar/text/default',             '#233a61',              'Initials/icon text color'],
    ['--colors/alpha/alpha-white-20',      'rgba(255,255,255,0.2)','bg layer overlay for initials and icon types'],
    ['--font-family/font-family-text',     'IBM Plex Sans Arabic', 'Initials font family'],
    ['--font-weight/font-weight-bold',     '700',                  'Initials weight at xs'],
    ['--font-weight/font-weight-semibold', '600',                  'Initials weight at sm–md'],
    ['--font-weight/font-weight-medium',   '500',                  'Initials weight at lg–xl'],
    ['--font-weight/font-weight-regular',  '400',                  'Initials weight at 2xl–3xl'],
  ].map(([t, v, d], i) => (
    <div key={t} style={{ display: 'grid', gridTemplateColumns: '240px 120px 1fr', padding: '9px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12 }}>
      <code style={{ fontSize: 11, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{t}</code>
      <span style={{ fontSize: 12, color: '#374151', fontFamily: 'monospace' }}>{v}</span>
      <span style={{ fontSize: 12, color: '#6b7280' }}>{d}</span>
    </div>
  ))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
  <div style={{ background: '#f9fafb', padding: '10px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Status Colors</div>
  <div style={{ display: 'grid', gridTemplateColumns: '16px 120px 90px 1fr', background: '#f0f1f3', padding: '7px 16px', gap: 12, fontSize: 11, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb' }}><span /><span>Status value</span><span>Hex</span><span>Meaning</span></div>
  {[
    ['#12b76a','online', '#12b76a','Active / connected'],
    ['#98a2b3','offline','#98a2b3','Disconnected'],
    ['#f79009','away',   '#f79009','Idle or stepped away'],
    ['#f04438','notify', '#f04438','Has unread notifications'],
    ['#1849a9','primary','#1849a9','Brand / primary presence'],
    ['#175cd3','badge',  '#175cd3','Verified or role badge'],
  ].map(([swatch, label, hex, desc]) => (
    <div key={label} style={{ display: 'grid', gridTemplateColumns: '16px 120px 90px 1fr', padding: '9px 16px', background: '#fff', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
      <div style={{ width: 12, height: 12, borderRadius: label === 'badge' ? 3 : '50%', background: swatch, border: '1px solid #e5e7eb', flexShrink: 0 }} />
      <code style={{ fontSize: 11, color: '#374151' }}>{label}</code>
      <code style={{ fontSize: 11, color: '#6b7280' }}>{hex}</code>
      <span style={{ fontSize: 12, color: '#6b7280' }}>{desc}</span>
    </div>
  ))}
</div>

</div>

---

## Props Reference

**Avatar**

<div style={{ display: 'grid', gridTemplateColumns: '130px 200px 120px 80px 1fr', gap: 0, marginBottom: 24, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
<div style={{ display: 'contents' }}>
  {['Prop','Type','Default','Req','Description'].map((h, i) => (
    <div key={h} style={{ padding: '10px 12px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
  ))}
</div>
{[
  ['type',        "'initials'|'icon'|'image'",                             "'initials'",    'No','Content type',                                     '#f0f4ff','#1849a9'],
  ['size',        "'xs'|'sm'|'md'|'lg'|'xl'|'2xl'|'3xl'",                 "'md'",          'No','Avatar diameter (24–120px)',                        '#f0fdf4','#059669'],
  ['square',      'boolean',                                                'false',         'No','Rounded-square shape instead of circular',          '#f0fdf4','#059669'],
  ['initials',    'string',                                                 "'AB'",          'No','Up to 2 chars shown when type="initials"',          '#f0f4ff','#1849a9'],
  ['src',         'string',                                                 '—',             'No','Image URL for type="image"',                        '#fdf4ff','#7c3aed'],
  ['alt',         'string',                                                 "''",            'No','aria-label / img alt text',                         '#f0f4ff','#1849a9'],
  ['color',       'string',                                                 "'transparent'", 'No','Root background fill (initials / icon)',            '#fff7ed','#c2410c'],
  ['textColor',   'string',                                                 "'#233a61'",     'No','Initials text color — --avatar/text/default',       '#fff7ed','#c2410c'],
  ['status',      "'online'|'offline'|'away'|'notify'|'primary'|'badge'",  'null',          'No','Status dot at bottom-end corner',                   '#f0fdf4','#059669'],
  ['notifyCount', 'number',                                                 '—',             'No','Count shown inside the notify dot',                 '#fff7ed','#c2410c'],
  ['border',      'boolean',                                                'false',         'No','Adds 1px rgba(22,22,22,0.2) ring overlay',          '#f0fdf4','#059669'],
  ['dir',         "'ltr'|'rtl'",                                            "'ltr'",         'No','Direction; dot moves to bottom-left in RTL',        '#f8fafc','#374151'],
].map(([prop, type, def, req, desc, bg, color]) => (
  <div key={prop} style={{ display: 'contents' }}>
    <div style={{ padding: '8px 12px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 11, color: '#7c3aed' }}>{type}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 11, color: '#6b7280' }}>{def}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
      <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 6px', borderRadius: 10, background: '#f3f4f6', color: '#6b7280' }}>{req}</span>
    </div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4, fontSize: 12 }}>{desc}</div>
  </div>
))}
</div>

**AvatarGroup**

<div style={{ display: 'grid', gridTemplateColumns: '140px 110px 80px 80px 1fr', gap: 0, marginBottom: 24, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
<div style={{ display: 'contents' }}>
  {['Prop','Type','Default','Req','Description'].map((h, i) => (
    <div key={h} style={{ padding: '10px 12px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
  ))}
</div>
{[
  ['stacked',       'boolean',  'true',  'No','Overlap with negative margin (true) or spaced with 4px gap (false)', '#f0fdf4','#059669'],
  ['size',          'string',   "'sm'",  'No','Size for all children and the overflow badge',                       '#f0fdf4','#059669'],
  ['max',           'number',   '5',     'No','Max visible avatars; remainder becomes the overflow badge',          '#fff7ed','#c2410c'],
  ['overflowLabel', 'string',   '—',     'No','Override overflow badge text (e.g. "+99")',                          '#f0f4ff','#1849a9'],
  ['children',      'Avatar[]', '—',     'No','Avatar elements to render in the group',                             '#fdf4ff','#7c3aed'],
].map(([prop, type, def, req, desc, bg, color]) => (
  <div key={prop} style={{ display: 'contents' }}>
    <div style={{ padding: '8px 12px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#7c3aed' }}>{type}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#6b7280' }}>{def}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
      <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 6px', borderRadius: 10, background: '#f3f4f6', color: '#6b7280' }}>{req}</span>
    </div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4, fontSize: 12 }}>{desc}</div>
  </div>
))}
</div>

**AvatarWithLabel**

<div style={{ display: 'grid', gridTemplateColumns: '130px 110px 120px 80px 1fr', gap: 0, marginBottom: 24, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
<div style={{ display: 'contents' }}>
  {['Prop','Type','Default','Req','Description'].map((h, i) => (
    <div key={h} style={{ padding: '10px 12px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
  ))}
</div>
{[
  ['name',      'string',  "'User Name'",   'No','Primary label displayed next to the avatar',    '#f0f4ff','#1849a9'],
  ['caption',   'string',  '—',             'No','Secondary line — role, email, etc.',             '#f0f4ff','#1849a9'],
  ['size',      'string',  "'md'",          'No','Controls avatar size and label font sizes',      '#f0fdf4','#059669'],
  ['type',      'string',  "'initials'",    'No','Avatar type — initials, icon, or image',        '#f0f4ff','#1849a9'],
  ['src',       'string',  '—',             'No','Image URL when type="image"',                   '#fdf4ff','#7c3aed'],
  ['color',     'string',  "'transparent'", 'No','Avatar background color',                       '#fff7ed','#c2410c'],
  ['textColor', 'string',  "'#233a61'",     'No','Avatar text / icon color',                      '#fff7ed','#c2410c'],
  ['status',    'string',  'null',          'No','Status dot on the avatar',                      '#f0fdf4','#059669'],
  ['dir',       'string',  "'ltr'",         'No','Layout direction — ltr or rtl',                  '#f8fafc','#374151'],
].map(([prop, type, def, req, desc, bg, color]) => (
  <div key={prop} style={{ display: 'contents' }}>
    <div style={{ padding: '8px 12px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#7c3aed' }}>{type}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 11, color: '#6b7280' }}>{def}</div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
      <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 6px', borderRadius: 10, background: '#f3f4f6', color: '#6b7280' }}>{req}</span>
    </div>
    <div style={{ padding: '8px 12px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4, fontSize: 12 }}>{desc}</div>
  </div>
))}
</div>

---

## Do / Don't

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 20, marginBottom: 24 }}>
  <div style={{ border: '2px solid #16a34a', borderRadius: 10, padding: 20 }}>
    <div style={{ fontSize: 12, fontWeight: 700, color: '#16a34a', marginBottom: 14, textTransform: 'uppercase', letterSpacing: '.05em' }}>Do</div>
    <div style={{ display: 'flex', alignItems: 'flex-end', gap: 12, marginBottom: 14 }}>
      <Avatar type="initials" size="md" initials="AB" status="online" />
      <Avatar type="image" size="md" src={FIGMA_PHOTO} alt="User" />
      <Avatar type="initials" size="md" initials="CD" />
      <Avatar type="icon" size="md" />
    </div>
    <div style={{ fontSize: 12, color: '#374151', lineHeight: 1.6 }}>Use consistent sizes within the same context. Use status indicators only when presence is relevant to the task.</div>
  </div>
  <div style={{ border: '2px solid #dc2626', borderRadius: 10, padding: 20 }}>
    <div style={{ fontSize: 12, fontWeight: 700, color: '#dc2626', marginBottom: 14, textTransform: 'uppercase', letterSpacing: '.05em' }}>Don't</div>
    <div style={{ display: 'flex', alignItems: 'flex-end', gap: 12, marginBottom: 14 }}>
      <Avatar type="initials" size="xs" initials="AB" />
      <Avatar type="initials" size="3xl" initials="AB" status="online" />
    </div>
    <div style={{ fontSize: 12, color: '#374151', lineHeight: 1.6 }}>Don't mix drastically different sizes in the same row. Don't add status dots where presence information is irrelevant.</div>
  </div>
</div>

---

## Accessibility

<div style={{ display: 'flex', flexDirection: 'column', gap: 10, marginBottom: 24 }}>
{[
  ['Role & label',    'Every avatar renders with role="img" and an aria-label derived from the alt prop or initials. Screen readers announce the user identity without visual reliance.'],
  ['Color contrast',  'Default text color #233a61 against the white Storybook canvas meets WCAG AA. When overriding the color prop with a dark background, switch textColor to #ffffff.'],
  ['Status meaning',  'Status indicators use both color and shape: dot = online/offline/away/notify/primary; rounded square = badge. Color is never the sole differentiator.'],
  ['RTL layout',      'dir="rtl" on AvatarWithLabel reverses label placement via logical CSS (margin-inline-end). The status dot moves to the bottom-left corner using direction-aware transform.'],
].map(([title, desc]) => (
  <div key={title} style={{ display: 'flex', gap: 14, padding: '12px 16px', border: '1px solid #e5e7eb', borderRadius: 8, background: '#fff' }}>
    <div>
      <div style={{ fontWeight: 600, fontSize: 13, color: '#111827', marginBottom: 3 }}>{title}</div>
      <div style={{ fontSize: 12, color: '#6b7280', lineHeight: 1.55 }}>{desc}</div>
    </div>
  </div>
))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 8 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '170px 60px 1fr 100px', gap: 0 }}>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb' }}>Criterion</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>Level</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb' }}>Requirement</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>Status</div>
    {[
      ['1.1.1 Non-text Content', 'A',  'Every avatar has role="img" with a meaningful aria-label (alt prop or initials).'],
      ['1.4.3 Contrast',         'AA', '#233a61 text on white background meets WCAG AA contrast requirements.'],
      ['1.4.1 Use of Color',     'A',  'Status indicators use color + shape (circle vs. rounded square), not color alone.'],
      ['4.1.2 Name, Role, Value','A',  'Container: role="img" + aria-label. Status dot: aria-label="Status: {value}".'],
      ['1.4.10 Reflow',          'AA', 'Avatar elements reflow at 320px viewport without horizontal scroll.'],
      ['1.3.1 Info & Relations', 'A',  'Status meaning conveyed via aria-label on dot element, available to assistive tech.'],
    ].map(([criterion, level, req]) => (
      <div key={criterion} style={{ display: 'contents' }}>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', fontFamily: 'monospace', fontSize: 11, color: '#1849a9', fontWeight: 600 }}>{criterion}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 7px', borderRadius: 10, background: '#dbeafe', color: '#1849a9' }}>{level}</span>
        </div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', fontSize: 12, color: '#374151', lineHeight: 1.4 }}>{req}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 7px', borderRadius: 10, background: '#d1fae5', color: '#059669' }}>Pass</span>
        </div>
      </div>
    ))}
  </div>
</div>

</div>
