import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as LinkStories from './Link.stories';

<Meta of={LinkStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif", padding: '32px 0', maxWidth: 860 }}>

{/* ── Header ── */}
<div style={{ marginBottom: 32 }}>
  <p style={{ margin: '0 0 4px', fontSize: 13, color: '#6b7280' }}>Components</p>
  <h1 style={{ margin: '0 0 10px', fontSize: 28, fontWeight: 700, color: '#000b36', lineHeight: 1.25 }}>
    Link
  </h1>
  <p style={{ margin: '0 0 16px', fontSize: 15, color: '#374151', lineHeight: 1.6, maxWidth: 700 }}>
    Navigational element that connects content within or across pages. Supports three styles
    (Primary, Neutral, On-color), two sizes, six interaction states, inline underline mode,
    optional trailing icon, and full RTL support.
  </p>
  <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6, background: '#d1fae5', color: '#065f46', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#10b981', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>3 Styles</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>2 Sizes</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>6 States</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', borderRadius: 20, padding: '4px 12px', fontSize: 12, fontWeight: 600 }}>RTL ✓</span>
  </div>
</div>

{/* ── Table of Contents ── */}
<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 10, padding: '20px 24px', marginBottom: 40 }}>
  <p style={{ margin: '0 0 10px', fontSize: 13, fontWeight: 700, color: '#000b36' }}>On this page</p>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
    {[
      ['#overview',      'Overview'],
      ['#states',        'States'],
      ['#properties',    'Properties'],
      ['#props',         'Props'],
      ['#tokens',        'Design Tokens'],
      ['#accessibility', 'Accessibility'],
    ].map(([href, label]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none' }}>{label}</a>
    ))}
  </div>
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* OVERVIEW                                                       */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="overview" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Overview</h2>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  Toggle <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>style</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>size</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>inline</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>icon</code>, and <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>rtl</code> in the controls panel to explore all configurations.
</p>

<Canvas of={LinkStories.Playground} />
<Controls of={LinkStories.Playground} />

<Canvas of={LinkStories.StyleMatrix} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* STATES                                                         */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="states" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>States</h2>
<p style={{ margin: '0 0 24px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  All six states apply to every style + size combination. The table below shows exact color values per state.
</p>

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>Primary</h3>

<Canvas of={LinkStories.PrimaryStates} />

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 32 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 0.8fr 1.2fr 0.8fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['State', 'Text Color', 'Underline', 'Focus Outline', 'Preview'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    { state: 'Default',  color: '#1849A9', underline: '—',   outline: '—'          },
    { state: 'Hovered',  color: '#194185', underline: '✓',   outline: '—'          },
    { state: 'Pressed',  color: '#102A56', underline: '✓',   outline: '—'          },
    { state: 'Focused',  color: '#1849A9', underline: '—',   outline: '2px #00001C'},
    { state: 'Visited',  color: '#1849A9', underline: '—',   outline: '—'          },
    { state: 'Disabled', color: '#D2D6DB', underline: '—',   outline: '—'          },
  ].map(({ state, color, underline, outline }) => (
    <div key={state} style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 0.8fr 1.2fr 0.8fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{state}</div>
      <div style={{ padding: '12px 16px', display: 'flex', alignItems: 'center', gap: 8 }}>
        <span style={{ display: 'inline-block', width: 12, height: 12, borderRadius: 3, background: color, border: '1px solid #e5e7eb', flexShrink: 0 }} />
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{color}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{underline}</div>
      <div style={{ padding: '12px 16px' }}>
        {outline !== '—' ? <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{outline}</code> : <span style={{ fontSize: 13, color: '#374151' }}>—</span>}
      </div>
      <div style={{ padding: '12px 16px' }}>
        <span style={{ fontSize: 14, fontWeight: 400, color, textDecoration: underline === '✓' ? 'underline' : 'none' }}>Link</span>
      </div>
    </div>
  ))}
</div>

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>Neutral</h3>

<Canvas of={LinkStories.NeutralStates} />

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 32 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 0.8fr 1.2fr 0.8fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['State', 'Text Color', 'Underline', 'Focus Outline', 'Preview'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    { state: 'Default',  color: '#1F2A37', underline: '—', outline: '—'          },
    { state: 'Hovered',  color: '#0D121C', underline: '✓', outline: '—'          },
    { state: 'Pressed',  color: '#4D5761', underline: '✓', outline: '—'          },
    { state: 'Focused',  color: '#1F2A37', underline: '—', outline: '2px #00001C'},
    { state: 'Visited',  color: '#1F2A37', underline: '—', outline: '—'          },
    { state: 'Disabled', color: '#D2D6DB', underline: '—', outline: '—'          },
  ].map(({ state, color, underline, outline }) => (
    <div key={state} style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 0.8fr 1.2fr 0.8fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{state}</div>
      <div style={{ padding: '12px 16px', display: 'flex', alignItems: 'center', gap: 8 }}>
        <span style={{ display: 'inline-block', width: 12, height: 12, borderRadius: 3, background: color, border: '1px solid #e5e7eb', flexShrink: 0 }} />
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{color}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{underline}</div>
      <div style={{ padding: '12px 16px' }}>
        {outline !== '—' ? <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{outline}</code> : <span style={{ fontSize: 13, color: '#374151' }}>—</span>}
      </div>
      <div style={{ padding: '12px 16px' }}>
        <span style={{ fontSize: 14, fontWeight: 400, color, textDecoration: underline === '✓' ? 'underline' : 'none' }}>Link</span>
      </div>
    </div>
  ))}
</div>

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 10, marginTop: 28 }}>On-color</h3>

<Canvas of={LinkStories.OnColorStates} />

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 0.8fr 1.2fr 1fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['State', 'Text Color', 'Underline', 'Focus Outline', 'Note'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    { state: 'Default',  color: '#FFFFFF', underline: '—', outline: '—',           note: '—'          },
    { state: 'Hovered',  color: '#FFFFFF', underline: '✓', outline: '—',           note: '—'          },
    { state: 'Pressed',  color: '#FFFFFF', underline: '✓', outline: '—',           note: '—'          },
    { state: 'Focused',  color: '#FFFFFF', underline: '—', outline: '2px #FFFFFF', note: '—'          },
    { state: 'Visited',  color: '#FFFFFF', underline: '—', outline: '—',           note: '—'          },
    { state: 'Disabled', color: '#FFFFFF', underline: '—', outline: '—',           note: 'opacity: 0.4'},
  ].map(({ state, color, underline, outline, note }) => (
    <div key={state} style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 0.8fr 1.2fr 1fr', background: '#000b36', borderBottom: '1px solid #1f2937' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#9ca3af' }}>{state}</div>
      <div style={{ padding: '12px 16px', display: 'flex', alignItems: 'center', gap: 8 }}>
        <span style={{ display: 'inline-block', width: 12, height: 12, borderRadius: 3, background: color, border: '1px solid #374151', flexShrink: 0 }} />
        <code style={{ fontSize: 12, background: '#1f2937', padding: '2px 6px', borderRadius: 4, color: '#9ca3af' }}>{color}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#6b7280' }}>{underline}</div>
      <div style={{ padding: '12px 16px' }}>
        {outline !== '—' ? <code style={{ fontSize: 12, background: '#1f2937', padding: '2px 6px', borderRadius: 4, color: '#9ca3af' }}>{outline}</code> : <span style={{ fontSize: 13, color: '#6b7280' }}>—</span>}
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#6b7280' }}>{note}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* PROPERTIES                                                     */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="properties" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Properties</h2>

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 24 }}>Inline</h3>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  When <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>inline</code> is <strong>false</strong> (default), the link has no underline until hovered.
  When <strong>true</strong>, the link is always underlined — use this when the link appears within a body text paragraph.
</p>

<Canvas of={LinkStories.InlineVariants} />

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 32 }}>Icon</h3>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  When <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>icon</code> is <strong>true</strong>, a trailing external-link icon (20px for Medium, 16px for Small)
  appears after the text with an 8px gap. The icon inherits the link color.
</p>

<Canvas of={LinkStories.WithIcon} />

<h3 style={{ fontSize: 15, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 32 }}>RTL</h3>
<p style={{ margin: '0 0 20px', fontSize: 14, color: '#374151', lineHeight: 1.7, maxWidth: 700 }}>
  When <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>rtl</code> is <strong>true</strong>, the component switches to right-to-left layout.
  The icon position flips to the trailing side in the RTL context.
</p>

<Canvas of={LinkStories.RTLVariants} />

{/* ══════════════════════════════════════════════════════════════ */}
{/* PROPS                                                          */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="props" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Props</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 0.8fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Prop', 'Type', 'Default', 'Description'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['href',       'string',                                     '—',         'Destination URL passed to the <a> element'],
    ['children',   'ReactNode',                                  '—',         'Link label text'],
    ['style',      "'primary' | 'neutral' | 'on-color'",        "'primary'", 'Visual color style'],
    ['size',       "'medium' | 'small'",                        "'medium'",  'Medium = 16px, Small = 14px'],
    ['inline',     'boolean',                                    'false',     'Always underlines the link — use within body text'],
    ['icon',       'boolean',                                    'false',     'Shows a trailing external-link icon'],
    ['disabled',   'boolean',                                    'false',     'Disables interaction; removes href, sets aria-disabled'],
    ['rtl',        'boolean',                                    'false',     'Right-to-left layout'],
    ['target',     'string',                                     '—',         'Forwarded to <a> target (_blank etc.)'],
    ['rel',        'string',                                     '—',         'Forwarded to <a>. Auto-set to "noopener noreferrer" for target="_blank"'],
    ['onClick',    '(e) => void',                                '—',         'Click handler. Not called when disabled.'],
    ['className',  'string',                                     '—',         'Additional CSS class'],
    ['aria-label', 'string',                                     '—',         'Accessible label (use when link text is ambiguous)'],
  ].map(([prop, type, def, desc]) => (
    <div key={prop} style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr 0.8fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{prop}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      </div>
      <div style={{ padding: '12px 16px' }}>
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{def}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* DESIGN TOKENS                                                  */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="tokens" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Design Tokens</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1.4fr 1.2fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    {['Token', 'Value', 'Usage'].map((h) => (
      <div key={h} style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>{h}</div>
    ))}
  </div>
  {[
    ['Primary default',         '#1849A9', 'Primary link text — Default, Focused, Visited'],
    ['Primary hovered',         '#194185', 'Primary link — Hovered state'],
    ['Primary pressed',         '#102A56', 'Primary link — Pressed state'],
    ['Neutral default',         '#1F2A37', 'Neutral link — Default, Focused, Visited'],
    ['Neutral hovered',         '#0D121C', 'Neutral link — Hovered state'],
    ['Neutral pressed',         '#4D5761', 'Neutral link — Pressed state'],
    ['On-color text',           '#FFFFFF', 'On-color link — all active states'],
    ['Disabled text',           '#D2D6DB', 'Disabled state for Primary and Neutral'],
    ['Focus outline (LTR)',     '#00001C', 'Focus ring on Primary and Neutral (2px)'],
    ['Focus outline (On-color)','#FFFFFF', 'Focus ring on On-color (2px)'],
    ['Medium font size',        '16px',    'Size=Medium link text'],
    ['Small font size',         '14px',    'Size=Small link text'],
    ['Icon gap',                '8px',     'Gap between link text and trailing icon'],
    ['Icon size (medium)',       '20px',    'Trailing icon at Size=Medium'],
    ['Icon size (small)',        '16px',    'Trailing icon at Size=Small'],
  ].map(([token, value, usage]) => (
    <div key={token} style={{ display: 'grid', gridTemplateColumns: '1.4fr 1.2fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{token}</div>
      <div style={{ padding: '12px 16px', display: 'flex', alignItems: 'center', gap: 8 }}>
        {value.startsWith('#') && (
          <span style={{ display: 'inline-block', width: 14, height: 14, borderRadius: 3, background: value, border: '1px solid #e5e7eb', flexShrink: 0 }} />
        )}
        <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{value}</code>
      </div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151' }}>{usage}</div>
    </div>
  ))}
</div>

{/* ══════════════════════════════════════════════════════════════ */}
{/* ACCESSIBILITY                                                  */}
{/* ══════════════════════════════════════════════════════════════ */}
<h2 id="accessibility" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8, marginTop: 48 }}>Accessibility</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 40 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb' }}>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Concern</div>
    <div style={{ padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Implementation</div>
  </div>
  {[
    ['Native element',    'Renders as a native <a> element — fully keyboard navigable via Tab.'],
    ['Focus ring',        'Focus ring (:focus-visible) is always visible: #00001C for Primary/Neutral, #FFFFFF for On-color.'],
    ['Disabled state',    'Disabled links use aria-disabled="true" and have href removed to prevent navigation. They remain in the DOM but are not keyboard focusable.'],
    ['External links',    'When target="_blank", rel="noopener noreferrer" is automatically applied.'],
    ['Link label',        'Use aria-label when the link text alone is ambiguous (e.g., multiple "Click here" links on a page).'],
    ['Trailing icon',     'The trailing icon is marked aria-hidden="true" and does not affect screen reader output.'],
    ['Visited state',     'Visited state styling uses CSS :visited — the design explicitly overrides browser defaults to maintain brand colors.'],
  ].map(([concern, impl]) => (
    <div key={concern} style={{ display: 'grid', gridTemplateColumns: '1fr 2fr', background: '#fff', borderBottom: '1px solid #f3f4f6' }}>
      <div style={{ padding: '12px 16px', fontSize: 13, fontWeight: 600, color: '#111827' }}>{concern}</div>
      <div style={{ padding: '12px 16px', fontSize: 13, color: '#374151', lineHeight: 1.6 }}>{impl}</div>
    </div>
  ))}
</div>

</div>
