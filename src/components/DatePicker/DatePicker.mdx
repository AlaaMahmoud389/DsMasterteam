{/* DatePicker.mdx — Masterteam Design System */}
import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import { DatePicker } from './DatePicker';
import * as DatePickerStories from './DatePicker.stories';

<Meta of={DatePickerStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif" }}>

{/* ── Header ───────────────────────────────────────────────────── */}
<div style={{ borderBottom: '1px solid #e5e7eb', paddingBottom: 16, marginBottom: 28 }}>
  <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 8 }}>Components / Form / Date Picker</div>
  <h1 style={{ margin: '0 0 10px', fontSize: 28, fontWeight: 700, color: '#000b36', lineHeight: 1.25 }}>Date Picker</h1>
  <p style={{ margin: '0 0 16px', fontSize: 15, color: '#374151', lineHeight: 1.6, maxWidth: 700 }}>
    A calendar panel for selecting a single date or a date range. Supports five independent feature flags — Input Field, Dual-Month View, Quick Options, Submit Button, and Range — which can be combined freely. Available in LTR and RTL layouts.
  </p>
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8 }}>
    <span style={{ background: '#d1fae5', color: '#065f46', fontSize: 12, fontWeight: 600, padding: '3px 10px', borderRadius: 9999 }}>Available</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', fontSize: 12, fontWeight: 600, padding: '3px 10px', borderRadius: 9999 }}>2 Modes</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', fontSize: 12, fontWeight: 600, padding: '3px 10px', borderRadius: 9999 }}>5 Feature Flags</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', fontSize: 12, fontWeight: 600, padding: '3px 10px', borderRadius: 9999 }}>Dual-Month</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', fontSize: 12, fontWeight: 600, padding: '3px 10px', borderRadius: 9999 }}>Quick Options</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', fontSize: 12, fontWeight: 600, padding: '3px 10px', borderRadius: 9999 }}>RTL Support</span>
    <span style={{ background: '#dbeafe', color: '#1e40af', fontSize: 12, fontWeight: 600, padding: '3px 10px', borderRadius: 9999 }}>Node 4250:1943</span>
  </div>
</div>

{/* ── On this page ── */}
<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '16px 20px', marginBottom: 32 }}>
  <div style={{ fontSize: 13, fontWeight: 600, color: '#1e40af', marginBottom: 10 }}>On this page</div>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: '4px 24px' }}>
    {[
      ['#overview',      'Overview'],
      ['#anatomy',       'Anatomy'],
      ['#range',         'Property: Range'],
      ['#input-field',   'Property: Input Field'],
      ['#dual-month',    'Property: Dual-Month'],
      ['#quick-options', 'Property: Quick Options'],
      ['#submit-button', 'Property: Submit Button'],
      ['#full-featured', 'Full-Featured Combination'],
      ['#rtl',           'RTL Support'],
      ['#states',        'States'],
      ['#tokens',        'Token Table'],
      ['#properties',    'Properties'],
      ['#accessibility', 'Accessibility'],
      ['#usage',         'Usage Guidelines'],
      ['#mismatch',      'Mismatch Report'],
    ].map(([href, text]) => (
      <a key={href} href={href} style={{ color: '#1849a9', fontSize: 13, textDecoration: 'none', lineHeight: '22px' }}>{text}</a>
    ))}
  </div>
</div>

---

{/* ── Overview ── */}
<h2 id="overview" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Overview</h2>
<p style={{ fontSize: 14, color: '#374151', marginBottom: 16, lineHeight: 1.6 }}>
  Use the Playground to explore all props interactively. Toggle <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>inline</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>mode</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showInputField</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>dualMonth</code>, <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showQuickOptions</code>, and <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showSubmitButton</code> in the controls panel below.
</p>

<Canvas of={DatePickerStories.Playground} />
<Controls of={DatePickerStories.Playground} />

---

{/* ── Anatomy ── */}
<h2 id="anatomy" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Anatomy</h2>

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '28px 24px', marginBottom: 24 }}>
  <DatePicker inline />
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '10px 28px', marginTop: 28 }}>
    {[
      ['1', 'Panel — white bg, radius 8px, shadow-xl'],
      ['2', 'Month label — 16px Bold'],
      ['3', 'Year dropdown button — click to open year list'],
      ['4', 'Nav arrows (← →) — 32×32px, inline-end of header in LTR'],
      ['5', 'Weekday row — 16px Regular, color #b6bdca, 40px tall'],
      ['6', 'Date cell — 56×56px outer hit area, 48×48px inner circle'],
      ['7', 'Today — 2px border #1849a9, blue text'],
      ['8', 'Selected — solid #1849a9 fill, white text'],
      ['9', 'Adjacent-month days — muted #3c5073'],
    ].map(([n, l]) => (
      <div key={n} style={{ display: 'flex', alignItems: 'center', gap: 7, fontSize: 13, color: '#111827' }}>
        <span style={{ width: 20, height: 20, borderRadius: '50%', background: '#1849a9', color: '#fff', fontSize: 10, fontWeight: 700, display: 'inline-flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>{n}</span>
        {l}
      </div>
    ))}
  </div>
</div>

<Canvas of={DatePickerStories.SingleInline} />

---

{/* ── Property: Range ── */}
<h2 id="range" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Property: Range</h2>

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px 24px', marginBottom: 16 }}>
  <p style={{ margin: '0 0 12px', fontSize: 12, fontWeight: 600, color: '#6C7C96' }}>mode="range" — click start date then end date</p>
  <DatePicker inline mode="range" />
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 16 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '120px 1fr 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>mode</span><span>Behaviour</span><span>Visual</span>
  </div>
  {[
    ['"single"', 'One date selected. Closes picker on click (no submit btn).', 'Filled circle on selected date.'],
    ['"range"',  'First click = start. Second click = end (or restart if both set).', 'Filled circles on start/end. #eff3fb strip between them.'],
  ].map(([m, b, v], i) => (
    <div key={m} style={{ display: 'grid', gridTemplateColumns: '120px 1fr 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'start' }}>
      <code style={{ fontSize: 12, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9', fontWeight: 600 }}>{m}</code>
      <span style={{ fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{b}</span>
      <span style={{ fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{v}</span>
    </div>
  ))}
</div>

<p style={{ fontSize: 14, color: '#374151', marginBottom: 8, fontWeight: 600 }}>Range cell states:</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '140px 130px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Cell type</span><span>Background</span><span>Notes</span>
  </div>
  {[
    ['Range start',   '#1849a9 fill',  'Fully filled circle. Right half of cell wrapper gets range-strip bg.'],
    ['Range end',     '#1849a9 fill',  'Fully filled circle. Left half of cell wrapper gets range-strip bg.'],
    ['In-range',      '#eff3fb strip', 'Full-width strip behind cell, no circle fill. Number in default color.'],
    ['Hover preview', '#eff3fb strip', 'Shows tentative range while hovering before second click.'],
  ].map(([t, bg, n], i) => (
    <div key={t} style={{ display: 'grid', gridTemplateColumns: '140px 130px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'start' }}>
      <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{t}</span>
      <code style={{ fontSize: 11, color: '#374151' }}>{bg}</code>
      <span style={{ fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{n}</span>
    </div>
  ))}
</div>

<Canvas of={DatePickerStories.RangeInline} />

---

{/* ── Property: Input Field ── */}
<h2 id="input-field" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Property: Input Field</h2>
<p style={{ fontSize: 14, color: '#374151', marginBottom: 16, lineHeight: 1.6 }}>
  <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showInputField</code> adds a labeled date-display field above the calendar. In <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>mode="single"</code> one field appears; in <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>mode="range"</code> two fields appear (From / To).
</p>

<div style={{ display: 'flex', gap: 24, flexWrap: 'wrap', marginBottom: 16 }}>
  <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px' }}>
    <p style={{ margin: '0 0 12px', fontSize: 12, fontWeight: 600, color: '#6C7C96' }}>Single — showInputField</p>
    <DatePicker inline showInputField />
  </div>
  <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px' }}>
    <p style={{ margin: '0 0 12px', fontSize: 12, fontWeight: 600, color: '#6C7C96' }}>Range — showInputField</p>
    <DatePicker inline mode="range" showInputField />
  </div>
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '200px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Detail</span><span>Value</span>
  </div>
  {[
    ['Field height',    '40px'],
    ['Border (empty)',  '#9da4ae'],
    ['Border (filled)', '#d2d6db'],
    ['Placeholder',     'Month DD, YYYY (LTR) · يوم/شهر/سنة (RTL)'],
    ['Labels (range)',  '"From" / "To" (LTR) · "من" / "إلى" (RTL)'],
    ['Format',          'MMMM D, YYYY — e.g. "January 19, 2024"'],
    ['Editable',        'Read-only display — date is set by clicking cells'],
  ].map(([d, v], i) => (
    <div key={d} style={{ display: 'grid', gridTemplateColumns: '200px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'start' }}>
      <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{d}</span>
      <span style={{ fontSize: 13, color: '#374151' }}>{v}</span>
    </div>
  ))}
</div>

<Canvas of={DatePickerStories.SingleWithInputField} />

---

{/* ── Property: Dual-Month View ── */}
<h2 id="dual-month" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Property: Dual-Month View</h2>
<p style={{ fontSize: 14, color: '#374151', marginBottom: 16, lineHeight: 1.6 }}>
  <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>dualMonth</code> renders two consecutive calendar months side by side. The main calendar shows <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>viewMonth</code>; the second always shows <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>viewMonth + 1</code>. Navigation arrows move both calendars simultaneously.
</p>

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px 24px', marginBottom: 16 }}>
  <p style={{ margin: '0 0 12px', fontSize: 12, fontWeight: 600, color: '#6C7C96' }}>dualMonth + mode="range"</p>
  <DatePicker inline dualMonth mode="range" />
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '220px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Detail</span><span>Value</span>
  </div>
  {[
    ['Main calendar width',     '424px (392px grid + 16px padding each side)'],
    ['Second calendar width',   '424px'],
    ['Gap between calendars',   'Divided by 1px solid #e5e7eb border'],
    ['Total panel width (dual)','848px'],
    ['Navigation',              'Prev arrow on main calendar only · Next arrow on second calendar only'],
    ['Month sync',              'Second calendar always shows main + 1 month'],
    ['Year dropdown',           'Independent on each calendar'],
    ['Range across months',     'Range strip carries across from one calendar into the next'],
  ].map(([d, v], i) => (
    <div key={d} style={{ display: 'grid', gridTemplateColumns: '220px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'start' }}>
      <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{d}</span>
      <span style={{ fontSize: 13, color: '#374151' }}>{v}</span>
    </div>
  ))}
</div>

<Canvas of={DatePickerStories.DualMonthRange} />

---

{/* ── Property: Quick Options ── */}
<h2 id="quick-options" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Property: Quick Options</h2>
<p style={{ fontSize: 14, color: '#374151', marginBottom: 16, lineHeight: 1.6 }}>
  <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showQuickOptions</code> adds a 160px sidebar of preset date-range shortcuts. Clicking a shortcut immediately sets the range and navigates the calendar to the start date. Works with both <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>mode="range"</code> (recommended) and <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>mode="single"</code>.
</p>

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px 24px', marginBottom: 16 }}>
  <p style={{ margin: '0 0 12px', fontSize: 12, fontWeight: 600, color: '#6C7C96' }}>mode="range" + showQuickOptions</p>
  <DatePicker inline mode="range" showQuickOptions />
</div>

<p style={{ fontSize: 14, fontWeight: 600, color: '#000b36', margin: '16px 0 8px' }}>Available shortcuts (LTR / RTL):</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 16 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '32px 140px 1fr 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>#</span><span>Key</span><span>LTR label</span><span>RTL label (Arabic)</span>
  </div>
  {[
    ['today',       'Today',         'اليوم'],
    ['thisWeek',    'This Week',     'هذا الأسبوع'],
    ['lastWeek',    'Last Week',     'الأسبوع الماضي'],
    ['thisMonth',   'This Month',    'هذا الشهر'],
    ['lastMonth',   'Last Month',    'الشهر الماضي'],
    ['last3Months', 'Last 3 Months', 'آخر 3 أشهر'],
    ['last7Days',   'Last 7 Days',   'آخر 7 أيام'],
    ['last30Days',  'Last 30 Days',  'آخر 30 يوم'],
    ['last90Days',  'Last 90 Days',  'آخر 90 يوم'],
  ].map(([k, en, ar], i) => (
    <div key={k} style={{ display: 'grid', gridTemplateColumns: '32px 140px 1fr 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
      <span style={{ fontSize: 12, color: '#9ca3af' }}>{i + 1}</span>
      <code style={{ fontSize: 11, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{k}</code>
      <span style={{ fontSize: 13, color: '#374151' }}>{en}</span>
      <span style={{ fontSize: 13, color: '#374151', direction: 'rtl' }}>{ar}</span>
    </div>
  ))}
</div>

<p style={{ fontSize: 14, fontWeight: 600, color: '#000b36', margin: '16px 0 8px' }}>Sidebar anatomy:</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '200px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Element</span><span>Value</span>
  </div>
  {[
    ['Sidebar width',   '160px'],
    ['Sidebar border',  '1px solid #e5e7eb on the inline-start edge'],
    ['Section label',   '"Shortcuts" / "اختصارات" — 12px Bold #6c737f'],
    ['Item font',       '14px Regular #000b36'],
    ['Item padding',    '8px vertical, 12px horizontal'],
    ['Active item',     'background: #eff3fb · color: #1849a9 · font-weight: 500'],
    ['Hover',           'background: #f3f4f6'],
  ].map(([el, v], i) => (
    <div key={el} style={{ display: 'grid', gridTemplateColumns: '200px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'start' }}>
      <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{el}</span>
      <span style={{ fontSize: 13, color: '#374151' }}>{v}</span>
    </div>
  ))}
</div>

<Canvas of={DatePickerStories.QuickOptionsDualMonth} />

---

{/* ── Property: Submit Button ── */}
<h2 id="submit-button" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Property: Submit Button</h2>
<p style={{ fontSize: 14, color: '#374151', marginBottom: 16, lineHeight: 1.6 }}>
  <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showSubmitButton</code> adds an <strong>Apply / Cancel</strong> action bar at the bottom of the panel. With this flag enabled, date/range selections become <strong>pending</strong> — <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>onChange</code> is only fired when Apply is clicked. Clicking Cancel dismisses without confirming.
</p>

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px 24px', marginBottom: 16 }}>
  <p style={{ margin: '0 0 12px', fontSize: 12, fontWeight: 600, color: '#6C7C96' }}>mode="range" + showSubmitButton</p>
  <DatePicker inline mode="range" showSubmitButton />
</div>

<p style={{ fontSize: 14, fontWeight: 600, color: '#000b36', margin: '16px 0 8px' }}>Action bar anatomy:</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 16 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '100px 1fr 1fr 1fr 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Button</span><span>Label (LTR)</span><span>Label (RTL)</span><span>Background</span><span>Text</span>
  </div>
  {[
    ['Apply',  'Apply',  'تأكيد', '#0d121c (dark)', '#ffffff'],
    ['Cancel', 'Cancel', 'إلغاء', 'transparent',    '#000b36'],
  ].map(([btn, ltr, rtl, bg, text], i) => (
    <div key={btn} style={{ display: 'grid', gridTemplateColumns: '100px 1fr 1fr 1fr 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
      <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{btn}</span>
      <span style={{ fontSize: 13, color: '#374151' }}>{ltr}</span>
      <span style={{ fontSize: 13, color: '#374151', direction: 'rtl' }}>{rtl}</span>
      <code style={{ fontSize: 11, color: '#374151' }}>{bg}</code>
      <code style={{ fontSize: 11, color: '#374151' }}>{text}</code>
    </div>
  ))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '200px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Detail</span><span>Value</span>
  </div>
  {[
    ['Bar top border',      '1px solid #cbd5e1'],
    ['Bar padding',         '12px top/bottom, 16px inline'],
    ['Button height',       '40px'],
    ['Button border-radius','4px'],
    ['Cancel border',       '1px solid #d2d6db'],
    ['Icon',                '16×16 SVG leading icon in each button'],
    ['Behaviour',           'Selection stays "pending" until Apply; Cancel reverts'],
  ].map(([d, v], i) => (
    <div key={d} style={{ display: 'grid', gridTemplateColumns: '200px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'start' }}>
      <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{d}</span>
      <span style={{ fontSize: 13, color: '#374151' }}>{v}</span>
    </div>
  ))}
</div>

<Canvas of={DatePickerStories.SubmitButtonRange} />

---

{/* ── Full-Featured Combination ── */}
<h2 id="full-featured" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Full-Featured Combination</h2>
<p style={{ fontSize: 14, color: '#374151', marginBottom: 16, lineHeight: 1.6 }}>
  All five properties enabled together: <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>dualMonth</code> + <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showInputField</code> + <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showQuickOptions</code> + <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>showSubmitButton</code> + <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>mode="range"</code>:
</p>

<div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px 24px', marginBottom: 16, overflowX: 'auto' }}>
  <DatePicker inline mode="range" dualMonth showInputField showQuickOptions showSubmitButton />
</div>

<p style={{ fontSize: 14, fontWeight: 600, color: '#000b36', margin: '16px 0 8px' }}>Combined panel dimensions:</p>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '260px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Configuration</span><span>Panel width</span>
  </div>
  {[
    ['Single calendar only',         '424px'],
    ['Single + Quick Options',        '424 + 160 = 584px'],
    ['Dual-Month only',               '848px'],
    ['Dual-Month + Quick Options',    '848 + 160 = 1008px'],
  ].map(([cfg, w], i) => (
    <div key={cfg} style={{ display: 'grid', gridTemplateColumns: '260px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
      <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{cfg}</span>
      <code style={{ fontSize: 13, color: '#374151' }}>{w}</code>
    </div>
  ))}
</div>

<Canvas of={DatePickerStories.FullFeatured} />

---

{/* ── RTL Support ── */}
<h2 id="rtl" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>RTL Support</h2>

<div style={{ display: 'flex', gap: 24, flexWrap: 'wrap', marginBottom: 16 }}>
  <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px' }}>
    <p style={{ margin: '0 0 12px', fontSize: 12, fontWeight: 600, color: '#6C7C96' }}>LTR</p>
    <DatePicker inline dir="ltr" />
  </div>
  <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: '20px' }}>
    <p style={{ margin: '0 0 12px', fontSize: 12, fontWeight: 600, color: '#6C7C96' }}>RTL</p>
    <DatePicker inline dir="rtl" />
  </div>
</div>

<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '12px 16px', fontSize: 13, color: '#1849a9', marginBottom: 24 }}>
  <strong>RTL changes:</strong> Month+year moves to inline-end · Nav arrows move to inline-start · Day headers switch to Arabic (أحد–سبت) · Year dropdown aligns to inline-end · Range strip start/end halves mirror · Quick option sidebar border mirrors · Apply/Cancel labels switch to Arabic (تأكيد / إلغاء) · Input field labels switch (من / إلى).
</div>

<Canvas of={DatePickerStories.LTRvsRTL} />

---

{/* ── States ── */}
<h2 id="states" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>States</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '180px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>State</span><span>Description</span>
  </div>
  {[
    { key: 'default',  label: 'Default cell',     desc: 'No bg · #000b36 · 16px Regular' },
    { key: 'today',    label: 'Today',             desc: '2px border #1849a9 · #1849a9 Bold · no fill' },
    { key: 'selected', label: 'Selected',          desc: 'bg #1849a9 · text #f9fafb · Bold' },
    { key: 'hover',    label: 'Hover',             desc: 'bg #e5e7eb (default) · #1849a933 (today)' },
    { key: 'selHover', label: 'Selected hover',    desc: 'bg #194185' },
    { key: 'selPress', label: 'Selected pressed',  desc: 'bg #102a56' },
    { key: 'adjacent', label: 'Adjacent month',    desc: 'color #3c5073' },
    { key: 'disabled', label: 'Disabled',          desc: 'color #d2d6db · no pointer-events' },
    { key: 'focused',  label: 'Focus visible',     desc: 'box-shadow ring 2px #1849a9 + 4px white on the outer 56px cell' },
  ].map(({ key, label, desc }, i) => (
    <div key={key} style={{ display: 'grid', gridTemplateColumns: '180px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'start' }}>
      <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{label}</span>
      <span style={{ fontSize: 13, color: '#374151' }}>{desc}</span>
    </div>
  ))}
</div>

<Canvas of={DatePickerStories.TriggerStates} />

---

{/* ── Token Table ── */}
<h2 id="tokens" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Token Table</h2>

<h3 style={{ fontSize: 15, fontWeight: 600, color: '#000b36', marginBottom: 12 }}>Sizing Tokens</h3>
<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '210px 72px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Token</span><span>Value</span><span>Usage</span>
  </div>
  {[
    { t: '--dp-cell-size',         v: '56px',   u: 'Outer hit area of each date cell' },
    { t: '--dp-cell-radius',       v: '9999px', u: 'Full-round cell shape' },
    { t: '--dp-panel-radius',      v: '8px',    u: 'Panel corner radius' },
    { t: '--dp-trigger-height',    v: '40px',   u: 'Trigger field + date input height' },
    { t: '--dp-trigger-radius',    v: '4px',    u: 'Trigger field border radius' },
    { t: '--dp-trigger-label-gap', v: '4px',    u: 'Gap between label and trigger' },
  ].map(({ t, v, u }, i) => (
    <div key={t} style={{ display: 'grid', gridTemplateColumns: '210px 72px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
      <code style={{ fontSize: 11, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{t}</code>
      <span style={{ fontSize: 13, fontWeight: 700, color: '#000b36' }}>{v}</span>
      <span style={{ fontSize: 13, color: '#374151' }}>{u}</span>
    </div>
  ))}
</div>

<h3 style={{ fontSize: 15, fontWeight: 600, color: '#000b36', marginBottom: 12 }}>Colour Tokens</h3>
<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '240px 130px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Token</span><span>Value</span><span>Usage</span>
  </div>
  {[
    { t: '--dp-cell-today-border',        h: '#1849a9', u: 'Today cell border ring' },
    { t: '--dp-cell-selected-bg',         h: '#1849a9', u: 'Selected cell fill' },
    { t: '--dp-cell-selected-bg-hover',   h: '#194185', u: 'Selected + hover' },
    { t: '--dp-cell-selected-bg-pressed', h: '#102a56', u: 'Selected + pressed' },
    { t: '--dp-cell-hover-bg',            h: '#e5e7eb', u: 'Default cell hover' },
    { t: '--dp-cell-pressed-bg',          h: '#d2d6db', u: 'Default cell pressed' },
    { t: '--dp-cell-focus-ring',          h: '#1849a9', u: 'Focus ring color' },
    { t: '--dp-cell-disabled-text',       h: '#d2d6db', u: 'Disabled cell text' },
    { t: '--dp-range-bg',                 h: '#eff3fb', u: 'Range strip + active quick option bg' },
    { t: '--dp-text-default',             h: '#000b36', u: 'Default cell text' },
    { t: '--dp-text-primary',             h: '#1849a9', u: 'Today number + year dropdown active' },
    { t: '--dp-text-secondary',           h: '#3c5073', u: 'Adjacent month dates' },
    { t: '--dp-text-oncolor',             h: '#f9fafb', u: 'Selected cell text' },
    { t: '--dp-text-disabled',            h: '#6c7c96', u: 'Disabled trigger text' },
    { t: '--dp-text-helper',              h: '#233a61', u: 'Helper text below trigger' },
    { t: '--dp-text-placeholder',         h: '#b6bdca', u: 'Weekday headers + trigger placeholder' },
    { t: '--dp-panel-bg',                 h: '#ffffff', u: 'Panel background' },
    { t: '--dp-panel-border',             h: '#e5e7eb', u: 'Panel border + calendar divider' },
    { t: '--dp-trigger-border',           h: '#d2d6db', u: 'Trigger + Cancel button border' },
    { t: '--dp-year-dd-border',           h: '#d2d6db', u: 'Year dropdown border' },
  ].map(({ t, h, u }, i) => (
    <div key={t} style={{ display: 'grid', gridTemplateColumns: '240px 130px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
      <code style={{ fontSize: 11, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{t}</code>
      <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6 }}>
        <span style={{ display: 'inline-block', width: 14, height: 14, borderRadius: 3, background: h, border: '1px solid rgba(0,0,0,0.12)', flexShrink: 0 }} />
        <code style={{ fontSize: 11, color: '#374151' }}>{h}</code>
      </span>
      <span style={{ fontSize: 13, color: '#374151' }}>{u}</span>
    </div>
  ))}
</div>

---

{/* ── Properties ── */}
<h2 id="properties" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Properties</h2>

<h3 style={{ fontSize: 15, fontWeight: 600, color: '#000b36', marginBottom: 12 }}>DatePicker</h3>
<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 24 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '160px 100px 130px 1fr', background: '#f9fafb', padding: '10px 16px', gap: 12, fontSize: 12, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Prop</span><span>Type</span><span>Default</span><span>Description</span>
  </div>
  {[
    { n: 'value',            type: 'Date | Date[]', def: '—',          d: 'Controlled selected date (single) or [start, end] tuple (range).' },
    { n: 'defaultValue',     type: 'Date',          def: 'null',        d: 'Default date for uncontrolled single mode.' },
    { n: 'onChange',         type: 'function',      def: '—',          d: 'Fires with Date (single) or [Date, Date] (range). With showSubmitButton, only fires on Apply.' },
    { n: 'mode',             type: 'enum',          def: '"single"',    d: '"single" — one date. "range" — start + end date pair.' },
    { n: 'inline',           type: 'boolean',       def: 'false',       d: 'Render the panel directly without a trigger input field.' },
    { n: 'showInputField',   type: 'boolean',       def: 'false',       d: 'Show date display input(s) above the calendar grid.' },
    { n: 'dualMonth',        type: 'boolean',       def: 'false',       d: 'Show two consecutive calendar months side-by-side (848px total).' },
    { n: 'showQuickOptions', type: 'boolean',       def: 'false',       d: 'Show 160px shortcuts sidebar with 9 preset date-range options.' },
    { n: 'showSubmitButton', type: 'boolean',       def: 'false',       d: 'Show Apply / Cancel bar. onChange fires only when Apply is clicked.' },
    { n: 'onApply',          type: 'function',      def: '—',          d: 'Called when the Apply button is clicked.' },
    { n: 'onCancel',         type: 'function',      def: '—',          d: 'Called when Cancel is clicked. Pending selection is cleared.' },
    { n: 'label',            type: 'string',        def: '—',          d: 'Label above the trigger field (ignored when inline).' },
    { n: 'helperText',       type: 'string',        def: '—',          d: 'Helper text below trigger. Hidden when errorMessage is set.' },
    { n: 'errorMessage',     type: 'string',        def: '—',          d: 'Error text. Also triggers red border on trigger field.' },
    { n: 'placeholder',      type: 'string',        def: '"DD/MM/YY"',  d: 'Trigger placeholder. Auto-switches to "يوم/شهر/سنة" in RTL.' },
    { n: 'dir',              type: 'enum',          def: '"ltr"',       d: '"ltr" or "rtl". Flips header, arrows, day labels, sidebar, action labels.' },
    { n: 'disabled',         type: 'boolean',       def: 'false',       d: 'Disables trigger and all date cells.' },
    { n: 'readOnly',         type: 'boolean',       def: 'false',       d: 'Trigger is non-interactive. Panel does not open.' },
    { n: 'minDate',          type: 'Date',          def: '—',          d: 'Dates before this are disabled and unselectable.' },
    { n: 'maxDate',          type: 'Date',          def: '—',          d: 'Dates after this are disabled and unselectable.' },
  ].map(({ n, type, def, d }, i) => (
    <div key={n} style={{ display: 'grid', gridTemplateColumns: '160px 100px 130px 1fr', padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'start' }}>
      <code style={{ fontSize: 12, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9', fontWeight: 600 }}>{n}</code>
      <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4, color: '#374151' }}>{type}</code>
      <code style={{ fontSize: 11, color: '#6b7280' }}>{def}</code>
      <span style={{ fontSize: 13, color: '#374151', lineHeight: 1.5 }}>{d}</span>
    </div>
  ))}
</div>

---

{/* ── Accessibility ── */}
<h2 id="accessibility" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Accessibility</h2>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 8, overflow: 'hidden', marginBottom: 28 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '220px 1fr', background: '#f9fafb', borderBottom: '1px solid #e5e7eb', padding: '10px 16px', fontSize: 12, fontWeight: 600, color: '#6b7280', textTransform: 'uppercase', letterSpacing: '0.06em' }}>
    <span>Concern</span><span>Implementation</span>
  </div>
  {[
    ['Enter / Space opens picker',        'Trigger has aria-haspopup="dialog" and aria-expanded. Enter or Space toggle the panel.'],
    ['Escape closes panel',               'Panel closes on Escape; focus returns to the trigger button.'],
    ['Tab between all elements',          'Date cells, nav arrows, year button, quick options, Apply/Cancel are all reachable by Tab. Disabled cells have tabIndex -1.'],
    ['Click outside closes panel',        'mousedown outside trigger and panel closes the dropdown.'],
    ['role="dialog" on panel',            'Panel has aria-label="Date picker" (LTR) or "منتقي التاريخ" (RTL).'],
    ['role="grid" on date grid',          'Each date row is in a grid; each cell has role="gridcell". aria-selected reflects selection.'],
    ['aria-label on every date button',   'Each button has a full locale date string label for screen reader announcement.'],
    ['role="option" on year list',        'Year dropdown uses role="listbox" + role="option" for proper AT enumeration.'],
    ['Double box-shadow focus ring',      'All interactive elements use 2px #1849a9 + 4px white box-shadow on :focus-visible. No outline.'],
    ['Today is not only color-coded',     'Today cell uses a circle border AND bold text — meaning is not conveyed through color alone.'],
    ['Disabled uses dedicated tokens',    '#d2d6db for disabled text color — no opacity shortcut. pointer-events: none.'],
    ['Range strip preserves text',        'In-range cells keep full number text visible with default color. No overlay or opacity on text.'],
  ].map(([concern, impl]) => (
    <div key={concern} style={{ display: 'grid', gridTemplateColumns: '220px 1fr', padding: '10px 16px', borderBottom: '1px solid #f3f4f6', fontSize: 13, alignItems: 'start', background: '#fff' }}>
      <span style={{ fontWeight: 600, color: '#000b36' }}>{concern}</span>
      <span style={{ color: '#374151' }}>{impl}</span>
    </div>
  ))}
</div>

---

{/* ── Usage Guidelines ── */}
<h2 id="usage" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Usage Guidelines</h2>

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 20, marginBottom: 28 }}>
  <div style={{ border: '1.5px solid #d1fae5', borderRadius: 8, padding: '16px 18px', background: '#f0fdf4' }}>
    <div style={{ fontSize: 13, fontWeight: 700, color: '#065f46', marginBottom: 10 }}>Do</div>
    <ul style={{ margin: 0, paddingLeft: 18, fontSize: 13, color: '#374151', lineHeight: 1.8 }}>
      <li>Use <code style={{ fontSize: 12 }}>mode="range"</code> + <code style={{ fontSize: 12 }}>showQuickOptions</code> for reporting/analytics date filters.</li>
      <li>Use <code style={{ fontSize: 12 }}>dualMonth</code> + <code style={{ fontSize: 12 }}>mode="range"</code> when selecting multi-week ranges.</li>
      <li>Use <code style={{ fontSize: 12 }}>showSubmitButton</code> when the selection must be explicitly confirmed before applying.</li>
      <li>Always provide a <code style={{ fontSize: 12 }}>label</code> on trigger fields.</li>
      <li>Use <code style={{ fontSize: 12 }}>showInputField</code> to give users a clear text summary of their selection.</li>
    </ul>
  </div>
  <div style={{ border: '1.5px solid #fee2e2', borderRadius: 8, padding: '16px 18px', background: '#fff5f5' }}>
    <div style={{ fontSize: 13, fontWeight: 700, color: '#991b1b', marginBottom: 10 }}>Don't</div>
    <ul style={{ margin: 0, paddingLeft: 18, fontSize: 13, color: '#374151', lineHeight: 1.8 }}>
      <li>Don't use <code style={{ fontSize: 12 }}>showQuickOptions</code> with <code style={{ fontSize: 12 }}>mode="single"</code> — shortcuts set ranges.</li>
      <li>Don't enable <code style={{ fontSize: 12 }}>dualMonth</code> without <code style={{ fontSize: 12 }}>mode="range"</code> — context is unclear.</li>
      <li>Don't place in <code style={{ fontSize: 12 }}>overflow:hidden</code> containers — dropdown panel will clip.</li>
      <li>Don't enable <code style={{ fontSize: 12 }}>showSubmitButton</code> without wiring <code style={{ fontSize: 12 }}>onCancel</code> to reset state.</li>
      <li>Don't use without a <code style={{ fontSize: 12 }}>label</code> on trigger fields.</li>
    </ul>
  </div>
</div>

---

{/* ── Mismatch Report ── */}
<h2 id="mismatch" style={{ fontSize: 20, fontWeight: 700, color: '#000b36', marginBottom: 8 }}>Mismatch Report</h2>
<p style={{ fontSize: 14, color: '#374151', marginBottom: 16, lineHeight: 1.6 }}>Audit against Figma node <code style={{ fontSize: 12, background: '#f3f4f6', padding: '2px 6px', borderRadius: 4 }}>4250:1943</code>.</p>

<div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', marginBottom: 16 }}>
  <span style={{ background: '#ecfdf5', color: '#059669', fontSize: 12, fontWeight: 700, padding: '4px 12px', borderRadius: 100, border: '1px solid #a7f3d0' }}>22 Match</span>
  <span style={{ background: '#fffbeb', color: '#d97706', fontSize: 12, fontWeight: 700, padding: '4px 12px', borderRadius: 100, border: '1px solid #fde68a' }}>2 Deviation</span>
  <span style={{ background: '#eff3fb', color: '#1849a9', fontSize: 12, fontWeight: 700, padding: '4px 12px', borderRadius: 100, border: '1px solid #bfdbfe' }}>2 New token</span>
  <span style={{ background: '#f3f4f6', color: '#6b7280', fontSize: 12, fontWeight: 700, padding: '4px 12px', borderRadius: 100, border: '1px solid #e5e7eb' }}>1 Enhancement</span>
</div>

<div style={{ border: '1px solid #fde68a', borderRadius: 8, overflow: 'hidden', marginBottom: 16 }}>
  <div style={{ background: '#fffbeb', padding: '10px 16px', borderBottom: '1px solid #fde68a', fontSize: 12, fontWeight: 600, color: '#d97706', textTransform: 'uppercase', letterSpacing: '0.06em' }}>Deviations</div>
  {[
    {
      a: 'Range strip color',
      fig: 'Token --form/datecell-background-100: #dff6e7 (green tint — Saudi Arabia brand color)',
      impl: 'Using #eff3fb (blue tint). The green is from the RTL/Arabic brand variant; the LTR screenshots show a blue-tinted strip. Configurable via --dp-range-bg token.',
    },
    {
      a: 'Apply button color',
      fig: 'background: #0d121c (dark/black) per Figma design — uses global button-background-black-default token',
      impl: 'Implemented as #0d121c exactly matching Figma. Note this differs from the standard primary button color (#1849a9) used in other components.',
    },
  ].map(({ a, fig, impl }, i) => (
    <div key={a} style={{ padding: '12px 16px', background: i % 2 === 0 ? '#fff' : '#fffef5', borderBottom: '1px solid #fef9c3' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 6 }}>
        <span style={{ background: '#fffbeb', color: '#d97706', fontSize: 11, padding: '2px 8px', borderRadius: 4, fontWeight: 700, border: '1px solid #fde68a' }}>Deviation</span>
        <span style={{ fontSize: 13, fontWeight: 600, color: '#000b36' }}>{a}</span>
      </div>
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10 }}>
        <div>
          <div style={{ fontSize: 10, fontWeight: 700, textTransform: 'uppercase', color: '#6b7280', marginBottom: 3 }}>Figma</div>
          <div style={{ fontSize: 12, color: '#374151' }}>{fig}</div>
        </div>
        <div>
          <div style={{ fontSize: 10, fontWeight: 700, textTransform: 'uppercase', color: '#6b7280', marginBottom: 3 }}>Implementation</div>
          <div style={{ fontSize: 12, color: '#374151' }}>{impl}</div>
        </div>
      </div>
    </div>
  ))}
</div>

<div style={{ border: '1px solid #bfdbfe', borderRadius: 8, overflow: 'hidden', marginBottom: 16 }}>
  <div style={{ background: '#eff3fb', padding: '10px 16px', borderBottom: '1px solid #bfdbfe', fontSize: 12, fontWeight: 600, color: '#1849a9', textTransform: 'uppercase', letterSpacing: '0.06em' }}>New tokens &amp; enhancements</div>
  {[
    { s: 'New token',   sb: '#eff3fb', st: '#1849a9', a: '--dp-range-bg: #eff3fb',        n: 'Range strip color not present as a named Figma token. Derived from primary-light tint. Override with --dp-range-bg: #dff6e7 for the Arabic green variant.' },
    { s: 'New token',   sb: '#eff3fb', st: '#1849a9', a: '--dp-panel-border: #e5e7eb',    n: 'Panel border not explicitly named in Figma variable set. Added for visual clarity on white backgrounds.' },
    { s: 'Enhancement', sb: '#f3f4f6', st: '#6b7280', a: 'minDate / maxDate props',        n: 'Date range constraints not in Figma (static data). Standard production date picker pattern.' },
  ].map(({ s, sb, st, a, n }, i) => (
    <div key={a} style={{ display: 'flex', alignItems: 'flex-start', gap: 12, padding: '10px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6' }}>
      <span style={{ background: sb, color: st, fontSize: 11, padding: '2px 8px', borderRadius: 4, fontWeight: 700, whiteSpace: 'nowrap', flexShrink: 0, border: `1px solid ${sb === '#eff3fb' ? '#bfdbfe' : '#e5e7eb'}` }}>{s}</span>
      <div>
        <div style={{ fontSize: 13, fontWeight: 600, color: '#000b36', marginBottom: 2 }}>{a}</div>
        <div style={{ fontSize: 12, color: '#6b7280', lineHeight: 1.5 }}>{n}</div>
      </div>
    </div>
  ))}
</div>

<div style={{ background: '#f9fafb', border: '1px solid #e5e7eb', borderRadius: 8, padding: '12px 16px', fontSize: 13, color: '#6b7280' }}>
  <span style={{ fontWeight: 700, color: '#374151' }}>Missing design decisions not in Figma variable set:</span> Year dropdown max-height · Panel drop-shadow exact spread values · Dual-month calendar divider width · Weekday row exact height · Quick options sidebar item line-height · Input field placeholder text format
</div>

</div>
