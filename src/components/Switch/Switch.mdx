{/* Switch.mdx — Masterteam Design System */}
import { Meta } from '@storybook/addon-docs/blocks';
import { Switch, SwitchLabel } from './Switch';
import * as SwitchStories from './Switch.stories';

<Meta of={SwitchStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif" }}>

{/* ── Header ───────────────────────────────────────────────── */}
<div style={{ marginBottom: 40, paddingBottom: 28, borderBottom: '1px solid #e5e7eb' }}>
  <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 10 }}>Components / Switch</div>
  <h1 style={{ fontSize: 34, fontWeight: 600, marginBottom: 10, color: '#111827' }}>Switch</h1>
  <div style={{ fontSize: 15, color: '#6b7280', maxWidth: 680, lineHeight: 1.7 }}>
    A toggle control with two components: a standalone <strong>Switch</strong> (48 × 24 px track with sliding thumb)
    and a <strong>Switch Label</strong> that adds a label, optional helper text, and optional alert message.
    Supports 5 states, RTL layout, an optional tick icon in the thumb, and flexible switch positioning (leading or trailing).
  </div>
  <div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', marginTop: 16 }}>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>
      <span style={{ width: 7, height: 7, borderRadius: '50%', background: '#059669', display: 'inline-block' }} />
      Available
    </span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>5 states</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>RTL</span>
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#f3f4f6', padding: '3px 10px', borderRadius: 100, fontSize: 12, color: '#6b7280' }}>Accessible</span>
    <a href="https://www.figma.com/design/WTmRAkJVvw0IvZMA7wBdTC/Masterteam-Ds-For-Experiment-ONLY?node-id=4250-1937" target="_blank" rel="noopener" style={{ display: 'inline-flex', alignItems: 'center', gap: 5, background: '#eff3fb', color: '#1849a9', padding: '3px 12px', borderRadius: 100, fontSize: 12, textDecoration: 'none', fontWeight: 500 }}>Figma ↗</a>
  </div>
</div>

{/* ── Switch — On / Off ────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>Switch — Off / On / On with Icon</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    The thumb slides from left (off) to right (on). When <code>showIcon</code> is true and the switch is on,
    a tick icon appears inside the white thumb in the track colour.
  </p>
  <div style={{ display: 'flex', gap: 40, flexWrap: 'wrap', alignItems: 'flex-end' }}>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>Off</div>
      <Switch checked={false} />
    </div>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>On</div>
      <Switch checked={true} />
    </div>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>On + Icon</div>
      <Switch checked={true} showIcon />
    </div>
  </div>
</div>

{/* ── All States — Off ─────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>All States — Off</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    5 interactive states for the off position. The border and thumb darken on Hover/Press and lighten when Disabled.
  </p>
  <div style={{ display: 'flex', gap: 32, flexWrap: 'wrap', alignItems: 'flex-end' }}>
    {['default', 'hovered', 'pressed', 'focused', 'disabled'].map((state) => (
      <div key={state}>
        <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>{state}</div>
        <Switch checked={false} state={state} />
      </div>
    ))}
  </div>
</div>

{/* ── All States — On ──────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>All States — On</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    Track fill darkens on Hover, turns light grey on Focus, remains blue when Disabled (with reduced opacity).
  </p>
  <div style={{ display: 'flex', gap: 32, flexWrap: 'wrap', alignItems: 'flex-end' }}>
    {['default', 'hovered', 'pressed', 'focused', 'disabled'].map((state) => (
      <div key={state}>
        <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>{state}</div>
        <Switch checked={true} state={state} />
      </div>
    ))}
  </div>
</div>

{/* ── All States — On with Icon ────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>All States — On with Icon</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    Same states as On, with a tick icon inside the thumb. The icon is coloured to match the track.
  </p>
  <div style={{ display: 'flex', gap: 32, flexWrap: 'wrap', alignItems: 'flex-end' }}>
    {['default', 'hovered', 'pressed', 'focused', 'disabled'].map((state) => (
      <div key={state}>
        <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>{state}</div>
        <Switch checked={true} showIcon state={state} />
      </div>
    ))}
  </div>
</div>

{/* ── RTL ──────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>RTL</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    In RTL mode, the thumb starts on the right side (off) and slides to the left (on) — mirroring the LTR behaviour.
  </p>
  <div style={{ display: 'flex', gap: 40, flexWrap: 'wrap', alignItems: 'flex-end' }}>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>RTL — Off</div>
      <Switch checked={false} rtl />
    </div>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>RTL — On</div>
      <Switch checked={true} rtl />
    </div>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>RTL — On + Icon</div>
      <Switch checked={true} showIcon rtl />
    </div>
  </div>
</div>

{/* ── Switch Label — Trail positions ───────────────────────── */}
<div style={{ marginBottom: 48, borderTop: '1px solid #e5e7eb', paddingTop: 40 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>Switch Label — Switch Position</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    <code>trailSwitch=false</code> (default) places the switch on the leading side (left in LTR).
    <code>trailSwitch=true</code> places it on the trailing side (right in LTR).
  </p>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 24, maxWidth: 420 }}>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>Trail Switch = False</div>
      <SwitchLabel label="Switch Label" checked={false} trailSwitch={false} />
    </div>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>Trail Switch = True</div>
      <SwitchLabel label="Switch Label" checked={true} showIcon trailSwitch={true} />
    </div>
  </div>
</div>

{/* ── Switch Label — Helper Text ───────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>Switch Label — Helper Text</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    Pass a string to <code>helperText</code> to show a secondary description below the label row.
  </p>
  <div style={{ maxWidth: 420 }}>
    <SwitchLabel
      label="Switch Label"
      checked={false}
      helperText="When a selection needs a further detailed explanation, it goes here."
    />
  </div>
</div>

{/* ── Switch Label — Alert Message ─────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>Switch Label — Alert Message</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    Pass a string to <code>alertMessage</code> to show an error row (icon + red text) beneath the helper text.
    Helper text and alert message are independent — either or both can be shown.
  </p>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 24, maxWidth: 420 }}>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>Alert only</div>
      <SwitchLabel label="Switch Label" checked={false} alertMessage="Error/Warning message" />
    </div>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>Helper + Alert</div>
      <SwitchLabel
        label="Switch Label"
        checked={false}
        helperText="When a selection needs a further detailed explanation, it goes here."
        alertMessage="Error/Warning message"
      />
    </div>
  </div>
</div>

{/* ── Switch Label — RTL ───────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>Switch Label — RTL</h2>
  <p style={{ fontSize: 14, color: '#6b7280', marginBottom: 24, lineHeight: 1.7 }}>
    RTL layout with Arabic text. The switch and label positions mirror naturally.
  </p>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 24, maxWidth: 420 }}>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>RTL — Trail Switch = False</div>
      <SwitchLabel label="عنوان المفتاح" checked={false} rtl trailSwitch={false} />
    </div>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>RTL — Trail Switch = True</div>
      <SwitchLabel label="عنوان المفتاح" checked={true} showIcon rtl trailSwitch={true} />
    </div>
    <div>
      <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>RTL — Helper + Alert</div>
      <SwitchLabel
        label="عنوان المفتاح"
        checked={false}
        rtl
        helperText="يكتب المحتوى الإضافي هنا في حال ان عنوان الاختيار يحتاج الى شرح أو تفصيل."
        alertMessage="رسالة خطأ أو تحذير"
      />
    </div>
  </div>
</div>

{/* ── Switch Label — All States ────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 6 }}>Switch Label — All States</h2>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 20, maxWidth: 420 }}>
    {['default', 'hovered', 'pressed', 'focused', 'disabled'].map((state) => (
      <div key={state}>
        <div style={{ fontSize: 11, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 10 }}>{state}</div>
        <SwitchLabel
          label="Switch Label"
          checked={state !== 'disabled' && state !== 'default'}
          state={state}
          helperText="Helper text appears here."
        />
      </div>
    ))}
  </div>
</div>

{/* ── Props: Switch ────────────────────────────────────────── */}
<div style={{ marginBottom: 48, borderTop: '1px solid #e5e7eb', paddingTop: 40 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 20 }}>Props — Switch</h2>
  <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Prop</th>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Type</th>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Default</th>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Description</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['checked', 'boolean', 'false', 'On (true) or Off (false)'],
        ['showIcon', 'boolean', 'false', 'Show tick icon inside thumb when on'],
        ['state', "'default'|'hovered'|'pressed'|'focused'|'disabled'", "'default'", 'Visual state'],
        ['rtl', 'boolean', 'false', 'RTL layout — thumb starts on right when off'],
        ['onChange', 'function', '—', 'Called with new boolean value on click'],
      ].map(([prop, type, def, desc]) => (
        <tr key={prop} style={{ borderBottom: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 14px', fontFamily: 'monospace', color: '#1849a9' }}>{prop}</td>
          <td style={{ padding: '10px 14px', fontFamily: 'monospace', color: '#6b7280', fontSize: 13 }}>{type}</td>
          <td style={{ padding: '10px 14px', fontFamily: 'monospace', color: '#6b7280' }}>{def}</td>
          <td style={{ padding: '10px 14px', color: '#374151' }}>{desc}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

{/* ── Props: SwitchLabel ───────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 20 }}>Props — SwitchLabel</h2>
  <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Prop</th>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Type</th>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Default</th>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Description</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['label', 'string', "'Switch Label'", 'Label text'],
        ['checked', 'boolean', 'false', 'Switch on/off state'],
        ['showIcon', 'boolean', 'false', 'Show tick icon in thumb when on'],
        ['state', "'default'|'hovered'|'pressed'|'focused'|'disabled'", "'default'", 'Visual state of the switch'],
        ['trailSwitch', 'boolean', 'false', 'Move switch to trailing side (right in LTR, left in RTL)'],
        ['helperText', 'string | null', 'null', 'Optional helper text below the label row'],
        ['alertMessage', 'string | null', 'null', 'Optional error/alert message with alert-circle icon'],
        ['rtl', 'boolean', 'false', 'RTL layout'],
        ['onChange', 'function', '—', 'Called with new boolean value on toggle'],
      ].map(([prop, type, def, desc]) => (
        <tr key={prop} style={{ borderBottom: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 14px', fontFamily: 'monospace', color: '#1849a9' }}>{prop}</td>
          <td style={{ padding: '10px 14px', fontFamily: 'monospace', color: '#6b7280', fontSize: 13 }}>{type}</td>
          <td style={{ padding: '10px 14px', fontFamily: 'monospace', color: '#6b7280' }}>{def}</td>
          <td style={{ padding: '10px 14px', color: '#374151' }}>{desc}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

{/* ── Design Tokens ────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 20 }}>Design Tokens</h2>
  <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
    <thead>
      <tr style={{ background: '#f9fafb' }}>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Token</th>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Value</th>
        <th style={{ textAlign: 'left', padding: '10px 14px', borderBottom: '1px solid #e5e7eb', color: '#374151', fontWeight: 600 }}>Usage</th>
      </tr>
    </thead>
    <tbody>
      {[
        ['Track OFF default', '#1F2A37', 'Border colour, thumb fill'],
        ['Track OFF hovered', '#0D121C', 'Darker border + thumb on hover'],
        ['Track OFF pressed', '#4D5761', 'Lighter on press'],
        ['Track OFF disabled', '#6C7C96', 'Border only when disabled'],
        ['Thumb OFF disabled', '#D2D6DB', 'Muted thumb when disabled'],
        ['Track ON default', '#1849A9', 'Blue fill for on state'],
        ['Track ON hovered', '#102A56', 'Darker blue on hover'],
        ['Track ON focused', '#D2D6DB', 'Light grey on keyboard focus'],
        ['Thumb ON all', '#F9FAFB', 'White thumb in on state'],
        ['Focus ring hover', '#F3F4F6', 'Outer glow on hover/press'],
        ['Focus ring focus', '#00001C', 'Keyboard focus outline'],
        ['Label text', '#000B36', 'SwitchLabel label'],
        ['Helper text', '#233A61', 'SwitchLabel helper text'],
        ['Alert text / icon', '#A30000', 'SwitchLabel alert message'],
      ].map(([token, value, usage]) => (
        <tr key={token} style={{ borderBottom: '1px solid #f3f4f6' }}>
          <td style={{ padding: '10px 14px', color: '#374151' }}>{token}</td>
          <td style={{ padding: '10px 14px' }}>
            <span style={{ display: 'inline-flex', alignItems: 'center', gap: 8 }}>
              <span style={{ width: 16, height: 16, borderRadius: 4, background: value, border: '1px solid #e5e7eb', display: 'inline-block', flexShrink: 0 }} />
              <code style={{ fontFamily: 'monospace', color: '#6b7280' }}>{value}</code>
            </span>
          </td>
          <td style={{ padding: '10px 14px', color: '#6b7280' }}>{usage}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

{/* ── Usage ────────────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 16 }}>Usage</h2>
  <pre style={{ background: '#f9fafb', border: '1px solid #e5e7eb', borderRadius: 8, padding: 20, fontSize: 13, lineHeight: 1.7, overflowX: 'auto', color: '#374151' }}>{`import { Switch, SwitchLabel } from './Switch';

// Standalone switch
<Switch checked={on} onChange={setOn} />

// With tick icon
<Switch checked={on} showIcon onChange={setOn} />

// RTL standalone
<Switch checked={on} rtl onChange={setOn} />

// Switch with label (switch leads)
<SwitchLabel
  label="Dark Mode"
  checked={on}
  showIcon
  onChange={setOn}
/>

// Switch trails (right side in LTR)
<SwitchLabel
  label="Notifications"
  checked={on}
  trailSwitch
  onChange={setOn}
/>

// With helper text and alert
<SwitchLabel
  label="Enable feature"
  checked={on}
  helperText="This setting affects all users."
  alertMessage="Permission required to change this setting."
  onChange={setOn}
/>

// RTL with all options
<SwitchLabel
  label="عنوان المفتاح"
  checked={on}
  showIcon
  rtl
  trailSwitch
  helperText="نص مساعد"
  onChange={setOn}
/>`}</pre>
</div>

{/* ── Accessibility ─────────────────────────────────────────── */}
<div style={{ marginBottom: 48 }}>
  <h2 style={{ fontSize: 20, fontWeight: 600, color: '#111827', marginBottom: 12 }}>Accessibility</h2>
  <ul style={{ fontSize: 14, color: '#374151', lineHeight: 2, paddingLeft: 20 }}>
    <li>The track renders as <code>&lt;button role="switch"&gt;</code> with <code>aria-checked</code> reflecting on/off state.</li>
    <li>Keyboard operable: Space and Enter toggle the switch.</li>
    <li>Disabled switches receive <code>disabled</code> attribute and <code>pointer-events: none</code>.</li>
    <li><code>:focus-visible</code> shows a <code>#00001C</code> outline for keyboard navigation.</li>
    <li>RTL support via <code>dir="rtl"</code> and explicit thumb mirroring.</li>
  </ul>
</div>

</div>
