import { Meta } from '@storybook/addon-docs/blocks';
import { MapChart } from './MapChart';
import * as MapStories from './MapChart.stories';

<Meta of={MapStories} />

<div style={{ fontFamily: "'IBM Plex Sans Arabic', system-ui, sans-serif" }}>
<div style={{ marginBottom: 40, paddingBottom: 28, borderBottom: '1px solid #e5e7eb' }}>
  <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 10 }}>Components / Charts / Map Chart</div>
  <h1 style={{ fontSize: 34, fontWeight: 600, marginBottom: 10, color: '#111827' }}>Map Chart</h1>
  <div style={{ fontSize: 15, color: '#6b7280', maxWidth: 640, lineHeight: 1.7 }}>SVG choropleth of Saudi Arabia's 13 administrative regions. Three render modes: interactive choropleth (data-driven colour), solid fill, and outline-only — matching Figma nodes 4560:5175–5177.</div>
  <div style={{ display: 'flex', gap: 8, marginTop: 12, flexWrap: 'wrap' }}>
    <span style={{ background: '#d1fae5', color: '#059669', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20 }}>Available</span>
    <span style={{ background: '#f0f4ff', color: '#1849a9', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20 }}>Node ID: 4560:5177</span>
    <a href="https://www.figma.com/design/WTmRAkJVvw0IvZMA7wBdTC/Masterteam-Ds-For-Experiment-ONLY?node-id=4560-5177" target="_blank" rel="noreferrer" style={{ background: '#fff7ed', color: '#c2410c', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20, textDecoration: 'none' }}>Figma ↗</a>
  </div>
</div>

<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '14px 20px', marginBottom: 24 }}>
  <div style={{ fontSize: 12, fontWeight: 700, color: '#1849a9', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.07em' }}>On this page</div>
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '4px 16px' }}>
    {[['Figma Variants','#figma-variants'],['Choropleth','#choropleth'],['Colour Ramp','#colour-ramp'],['Regions','#regions'],['Tooltip','#tooltip'],['RTL','#rtl'],['States','#states'],['Props Reference','#props-reference'],['Token Table','#token-table'],["Do / Don't",'#do--dont'],['Accessibility','#accessibility']].map(([label, href]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none', lineHeight: 2 }}>{label}</a>
    ))}
  </div>
</div>

---

## Figma Variants

Figma exposes two boolean controls on the map component: `filled` and `rtl`. These map to `variant` and `dir` props.

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 24 }}>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Figma: filled=true (node 4560:5176)</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 16 }}>
    <MapChart title="" data={[]} variant="filled" showColorScale={false} showRegionLabels={false} />
  </div>
</div>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Figma: filled=false / outline (node 4560:5175)</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 16 }}>
    <MapChart title="" data={[]} variant="outline" showColorScale={false} showRegionLabels={false} />
  </div>
</div>
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24 }}>
<div style={{ background: '#f9fafb', padding: '8px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Figma → prop mapping</div>
{[
['filled=true',  'variant="filled"',     'Uniform #1849a9 fill — use for country outline illustrations'],
['filled=false', 'variant="outline"',    'White fill, #1849a9 stroke — use for outline map backgrounds'],
['(interactive)','variant="choropleth"', 'Data-driven colour per region — default for data visualisation'],
['rtl',          'dir="rtl"',            'Mirrors region label text direction; map geometry unchanged'],
].map(([figma, prop, desc]) => (
  <div key={figma} style={{ display: 'grid', gridTemplateColumns: '160px 200px 1fr', borderBottom: '1px solid #f3f4f6', fontSize: 13 }}>
    <div style={{ padding: '8px 12px', fontFamily: 'monospace', fontSize: 11, color: '#7c3aed', background: '#fafafa' }}>{figma}</div>
    <div style={{ padding: '8px 12px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9' }}>{prop}</div>
    <div style={{ padding: '8px 12px', color: '#374151' }}>{desc}</div>
  </div>
))}
</div>

---

## Choropleth

The default `variant="choropleth"` colours each region by its data value using the 6-step design system blue ramp.

<div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 10, padding: 20, marginBottom: 16 }}>
  <MapChart
    title="Saudi Arabia — Population by Region"
    data={[
      { id: 'riyadh', value: 8420 }, { id: 'makkah', value: 8560 },
      { id: 'madinah', value: 2100 }, { id: 'eastern', value: 4910 },
      { id: 'asir', value: 2210 }, { id: 'tabuk', value: 910 },
      { id: 'hail', value: 720 }, { id: 'northern-borders', value: 380 },
      { id: 'jizan', value: 1560 }, { id: 'najran', value: 620 },
      { id: 'bahah', value: 480 }, { id: 'jawf', value: 560 },
      { id: 'qassim', value: 1420 },
    ]}
    variant="choropleth"
    showColorScale={true}
    showRegionLabels={true}
  />
</div>

```jsx
<MapChart
  title="Saudi Arabia — Population by Region"
  data={[
    { id: 'riyadh',           value: 8420 },
    { id: 'makkah',           value: 8560 },
    { id: 'eastern',          value: 4910 },
    // ...all 13 regions
  ]}
  variant="choropleth"
  showColorScale={true}
  showRegionLabels={true}
/>
```

---

## Colour Ramp

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24 }}>
<div style={{ background: '#f9fafb', padding: '8px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>6-step blue ramp (low → high)</div>
{[
['Step 1 — lowest',  '#d1e9ff', '--chart-series-6'],
['Step 2',           '#b2ddff', '--chart-series-5'],
['Step 3',           '#53b1fd', '--chart-series-4'],
['Step 4',           '#2e90fa', '--chart-series-3'],
['Step 5',           '#175cd3', '--chart-series-2'],
['Step 6 — highest', '#1849a9', '--chart-series-1'],
].map(([label, hex, token]) => (
  <div key={hex} style={{ display: 'grid', gridTemplateColumns: '180px 44px 200px 1fr', alignItems: 'center', borderBottom: '1px solid #f3f4f6', fontSize: 13 }}>
    <div style={{ padding: '8px 12px', fontWeight: 600, color: '#111827', background: '#fafafa' }}>{label}</div>
    <div style={{ padding: '6px 10px' }}>
      <div style={{ width: 28, height: 28, borderRadius: 4, background: hex, border: '1px solid #e5e7eb' }} />
    </div>
    <div style={{ padding: '8px 6px', fontFamily: 'monospace', fontSize: 11, color: '#6b7280' }}>{hex}</div>
    <div style={{ padding: '8px 10px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9' }}>{token}</div>
  </div>
))}
</div>

Regions with no matching data entry use `var(--chart-border, #f3f4f6)` (light grey — null fill).

---

## Regions

All 13 Saudi Arabia administrative regions supported out-of-the-box. Pass `id` strings in `data` to colour a region.

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24 }}>
<div style={{ background: '#f9fafb', padding: '8px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Region IDs</div>
{[
['riyadh',           'Riyadh',           'الرياض',           'Central — largest by area and population'],
['makkah',           'Makkah',           'مكة المكرمة',        'Western coast — includes Jeddah, Taif'],
['madinah',          'Madinah',          'المدينة المنورة',     'Northwest — Al-Madinah Al-Munawwarah'],
['eastern',          'Eastern Region',   'المنطقة الشرقية',    'Northeast — Persian Gulf coast, oil region'],
['asir',             'Asir',             'عسير',             'Southwest highlands'],
['tabuk',            'Tabuk',            'تبوك',             'Far northwest — Red Sea / Gulf of Aqaba'],
['hail',             'Hail',             'حائل',             'North-central'],
['northern-borders', 'N. Borders',       'الحدود الشمالية',    'Far north — borders Iraq & Jordan'],
['jizan',            'Jizan',            'جازان',            'Southwestern coast — borders Yemen'],
['najran',           'Najran',           'نجران',            'Southern — borders Yemen & Oman'],
['bahah',            'Al-Bahah',         'الباحة',           'Small western region (Sarawat Mountains)'],
['jawf',             'Al-Jawf',          'الجوف',            'Northwest interior'],
['qassim',           'Qassim',           'القصيم',           'Central-north — agriculture region'],
].map(([id, en, ar, desc]) => (
  <div key={id} style={{ display: 'grid', gridTemplateColumns: '155px 140px 180px 1fr', borderBottom: '1px solid #f3f4f6', fontSize: 13 }}>
    <div style={{ padding: '7px 12px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9', background: '#fafafa' }}>{id}</div>
    <div style={{ padding: '7px 12px', fontWeight: 600, color: '#111827' }}>{en}</div>
    <div style={{ padding: '7px 12px', color: '#6b7280', direction: 'rtl' }}>{ar}</div>
    <div style={{ padding: '7px 12px', color: '#374151' }}>{desc}</div>
  </div>
))}
</div>

---

## Tooltip

Hover or Tab to any region to reveal its value in a tooltip card.

<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '12px 16px', marginBottom: 16, fontSize: 13, color: '#374151' }}>
  <strong style={{ color: '#1849a9' }}>Try it:</strong> Hover over any region in the choropleth above — a tooltip shows the region name and value.
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 24 }}>
<div style={{ background: '#f9fafb', padding: '8px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Tooltip behaviour</div>
{[
['Trigger',   'mouseenter / focus on any region path'],
['Dismiss',   'mouseleave / blur'],
['Content',   'Region name + formatted value (value.toLocaleString())'],
['Keyboard',  'tabIndex={0} on every region — Tab cycles through all 13'],
['No data',   'Shows "N/A" for regions not present in data array'],
['Position',  'Absolute inside card; offset from cursor by 14px'],
].map(([label, desc]) => (
  <div key={label} style={{ display: 'grid', gridTemplateColumns: '100px 1fr', borderBottom: '1px solid #f3f4f6', fontSize: 13 }}>
    <div style={{ padding: '8px 12px', fontWeight: 600, color: '#111827', background: '#fafafa' }}>{label}</div>
    <div style={{ padding: '8px 12px', color: '#374151' }}>{desc}</div>
  </div>
))}
</div>

---

## RTL

Set `dir="rtl"` to display region labels in Arabic and mirror text direction.

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 24 }}>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>dir="ltr" (English labels)</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 12 }}>
    <MapChart title="" data={[{id:'riyadh',value:8420},{id:'makkah',value:8560},{id:'eastern',value:4910},{id:'madinah',value:2100},{id:'asir',value:2210},{id:'tabuk',value:910},{id:'hail',value:720},{id:'northern-borders',value:380},{id:'jizan',value:1560},{id:'najran',value:620},{id:'bahah',value:480},{id:'jawf',value:560},{id:'qassim',value:1420}]} variant="choropleth" showColorScale={false} showRegionLabels={true} dir="ltr" />
  </div>
</div>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>dir="rtl" (Arabic labels)</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 12 }}>
    <MapChart title="المملكة العربية السعودية" data={[{id:'riyadh',value:8420},{id:'makkah',value:8560},{id:'eastern',value:4910},{id:'madinah',value:2100},{id:'asir',value:2210},{id:'tabuk',value:910},{id:'hail',value:720},{id:'northern-borders',value:380},{id:'jizan',value:1560},{id:'najran',value:620},{id:'bahah',value:480},{id:'jawf',value:560},{id:'qassim',value:1420}]} variant="choropleth" showColorScale={false} showRegionLabels={true} dir="rtl" />
  </div>
</div>
</div>

---

## States

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: 16, marginBottom: 24 }}>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Choropleth</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 12 }}>
    <MapChart title="Choropleth" data={[{id:'riyadh',value:8420},{id:'makkah',value:8560},{id:'eastern',value:4910},{id:'madinah',value:2100},{id:'asir',value:2210},{id:'tabuk',value:910},{id:'hail',value:720},{id:'northern-borders',value:380},{id:'jizan',value:1560},{id:'najran',value:620},{id:'bahah',value:480},{id:'jawf',value:560},{id:'qassim',value:1420}]} variant="choropleth" showColorScale={false} showRegionLabels={false} />
  </div>
</div>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Filled</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 12 }}>
    <MapChart title="Filled" data={[]} variant="filled" showColorScale={false} showRegionLabels={false} />
  </div>
</div>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Outline</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 12 }}>
    <MapChart title="Outline" data={[]} variant="outline" showColorScale={false} showRegionLabels={false} />
  </div>
</div>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Partial data</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 12 }}>
    <MapChart title="Partial" data={[{id:'riyadh',value:8420},{id:'makkah',value:8560},{id:'eastern',value:4910}]} variant="choropleth" showColorScale={false} showRegionLabels={false} />
  </div>
</div>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Loading</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 12 }}>
    <MapChart title="Loading" data={[]} loading={true} />
  </div>
</div>
<div>
  <div style={{ fontSize: 12, fontWeight: 600, color: '#6b7280', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.06em' }}>Empty</div>
  <div style={{ background: '#f8fafc', border: '1px solid #e5e7eb', borderRadius: 8, padding: 12 }}>
    <MapChart title="Empty" data={[]} empty={true} />
  </div>
</div>
</div>

---

## Props Reference

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 16 }}>
<div style={{ display: 'grid', gridTemplateColumns: '150px 130px 110px 1fr 160px', background: '#f0f1f3', padding: '7px 16px', gap: 12, fontSize: 11, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb' }}><span>Prop</span><span>Type</span><span>Default</span><span>Description</span><span>Visual effect</span></div>
{[
{n:'title',type:'string',req:true,tb:'#eff3fb',tc:'#1849a9',def:'—',d:'Card title displayed at top of card',v:'Card heading + SVG aria-label'},
{n:'data',type:'DataItem[]',req:true,tb:'#eff3fb',tc:'#1849a9',def:'[]',d:'{ id, value } per region. Unmatched IDs show null colour.',v:'Region colour intensity (choropleth)'},
{n:'variant',type:'enum',req:false,tb:'#fff7ed',tc:'#c2410c',def:"'choropleth'",d:"'choropleth' | 'filled' | 'outline' — render mode",v:'Data-driven vs decorative fill'},
{n:'regions',type:'RegionDef[]',req:false,tb:'#f3f0ff',tc:'#7c3aed',def:'SAUDI_REGIONS',d:'Override built-in region shapes with custom SVG paths',v:'Custom map geography'},
{n:'showColorScale',type:'boolean',req:false,tb:'#f3f0ff',tc:'#7c3aed',def:'true',d:'Show low→high gradient bar below map (choropleth only)',v:'Colour legend bar'},
{n:'showRegionLabels',type:'boolean',req:false,tb:'#f3f0ff',tc:'#7c3aed',def:'true',d:'Render region name text inside each polygon',v:'Label text in each region'},
{n:'loading',type:'boolean',req:false,tb:'#f3f0ff',tc:'#7c3aed',def:'false',d:'Animated skeleton loading state',v:'Pulsing grey regions'},
{n:'empty',type:'boolean',req:false,tb:'#f3f0ff',tc:'#7c3aed',def:'false',d:'"No data available" empty state',v:'All null-colour regions + message'},
{n:'dir',type:'enum',req:false,tb:'#fff7ed',tc:'#c2410c',def:"'ltr'",d:"'ltr' | 'rtl' — switches labels to Arabic (labelAr field)",v:'Region labels in Arabic'},
].map(({n,type,req,tb,tc,def,d,v},i)=>(
<div key={n} style={{display:'grid',gridTemplateColumns:'150px 130px 110px 1fr 160px',padding:'10px 16px',background:i%2===0?'#fff':'#fafafa',borderBottom:'1px solid #f3f4f6',gap:12,alignItems:'start'}}>
<code style={{fontSize:12,background:tb,padding:'2px 6px',borderRadius:4,color:tc,fontWeight:600}}>{n}{req&&<span style={{color:'#ef4444'}}> *</span>}</code>
<span style={{background:'#f3f4f6',color:'#374151',fontSize:11,padding:'2px 7px',borderRadius:4,fontWeight:500,display:'inline-block',alignSelf:'center'}}>{type}</span>
<code style={{fontSize:12,color:'#6b7280',alignSelf:'center'}}>{def}</code>
<span style={{fontSize:13,color:'#374151',lineHeight:1.5}}>{d}</span>
<span style={{fontSize:12,color:'#6b7280',lineHeight:1.4}}>{v}</span>
</div>
))}
</div>

---

## Token Table

<div style={{ display: 'flex', flexDirection: 'column', gap: 20, marginBottom: 16 }}>
<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
<div style={{ background: '#f9fafb', padding: '10px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Container</div>
<div style={{ display: 'grid', gridTemplateColumns: '260px 130px 1fr', background: '#f0f1f3', padding: '7px 16px', gap: 12, fontSize: 11, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb' }}><span>CSS Token</span><span>Figma resolved value</span><span>Figma path</span></div>
{[
['--chart-container-bg','#ffffff','charts/container/background/default'],
['--chart-container-padding','24 px','charts/container/chart-card-2xl-padding'],
['--chart-container-radius','8 px','charts/container/chart-card-radius-sm'],
['--chart-border','#f3f4f6','charts/container/border/default + null region fill'],
].map(([t, v, f], i) => (
<div key={t} style={{ display: 'grid', gridTemplateColumns: '260px 130px 1fr', padding: '9px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
<code style={{ fontSize: 12, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{t}</code>
<span style={{ fontSize: 13, color: '#111827' }}>{v}</span>
<code style={{ fontSize: 11, color: '#6b7280' }}>{f}</code>
</div>
))}
</div>
<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
<div style={{ background: '#f9fafb', padding: '10px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Typography &amp; Colors</div>
<div style={{ display: 'grid', gridTemplateColumns: '260px 150px 1fr', background: '#f0f1f3', padding: '7px 16px', gap: 12, fontSize: 11, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb' }}><span>CSS Token</span><span>Value</span><span>Usage</span></div>
{[
['--chart-title','#000b36','Card title text'],
['--chart-axis','#6c7c96','Colour-scale min/max labels'],
].map(([t, h, f], i) => (
<div key={t} style={{ display: 'grid', gridTemplateColumns: '260px 150px 1fr', padding: '9px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
<code style={{ fontSize: 12, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{t}</code>
<span style={{ display: 'inline-flex', alignItems: 'center', gap: 6 }}>
<span style={{ display: 'inline-block', width: 14, height: 14, borderRadius: 3, background: h, border: '1px solid rgba(0,0,0,0.12)', flexShrink: 0 }} />
<code style={{ fontSize: 11, color: '#111827' }}>{h}</code>
</span>
<span style={{ fontSize: 12, color: '#6b7280' }}>{f}</span>
</div>
))}
</div>
<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
<div style={{ background: '#f9fafb', padding: '10px 16px', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '.08em', color: '#6b7280' }}>Choropleth Colour Ramp</div>
<div style={{ display: 'grid', gridTemplateColumns: '260px 150px 1fr', background: '#f0f1f3', padding: '7px 16px', gap: 12, fontSize: 11, fontWeight: 600, color: '#6b7280', borderBottom: '1px solid #e5e7eb' }}><span>CSS Token</span><span>Value</span><span>Ramp step</span></div>
{[
['--chart-series-6','#d1e9ff','Step 1 — lowest values (lightest)'],
['--chart-series-5','#b2ddff','Step 2'],
['--chart-series-4','#53b1fd','Step 3'],
['--chart-series-3','#2e90fa','Step 4'],
['--chart-series-2','#175cd3','Step 5'],
['--chart-series-1','#1849a9','Step 6 — highest values + filled variant fill'],
].map(([t, h, f], i) => (
<div key={t} style={{ display: 'grid', gridTemplateColumns: '260px 150px 1fr', padding: '9px 16px', background: i % 2 === 0 ? '#fff' : '#fafafa', borderBottom: '1px solid #f3f4f6', gap: 12, alignItems: 'center' }}>
<code style={{ fontSize: 12, background: '#eff3fb', padding: '2px 6px', borderRadius: 4, color: '#1849a9' }}>{t}</code>
<span style={{ display: 'inline-flex', alignItems: 'center', gap: 6 }}>
<span style={{ display: 'inline-block', width: 14, height: 14, borderRadius: 3, background: h, border: '1px solid rgba(0,0,0,0.12)', flexShrink: 0 }} />
<code style={{ fontSize: 11, color: '#111827' }}>{h}</code>
</span>
<span style={{ fontSize: 12, color: '#6b7280' }}>{f}</span>
</div>
))}
</div>
</div>

---

## Do / Don't

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 24 }}>

<div style={{ borderRadius: 10, overflow: 'hidden', border: '2px solid #a7f3d0' }}>
<div style={{ padding: '20px 16px', background: '#fff' }}>
<MapChart
  title="Population by Region"
  data={[
    {id:'riyadh',value:8420},{id:'makkah',value:8560},{id:'madinah',value:2100},
    {id:'eastern',value:4910},{id:'asir',value:2210},{id:'qassim',value:1420},
    {id:'tabuk',value:910},{id:'jizan',value:1560},{id:'hail',value:720},
    {id:'najran',value:620},{id:'bahah',value:480},{id:'jawf',value:560},
    {id:'northern-borders',value:380},
  ]}
  variant="choropleth"
  showColorScale={true}
  showRegionLabels={true}
/>
</div>
<div style={{ padding: '10px 14px', background: '#ecfdf5', borderTop: '1px solid #a7f3d0' }}>
<div style={{ display: 'flex', alignItems: 'center', gap: 7, fontSize: 12, fontWeight: 700, color: '#059669', marginBottom: 6 }}><span style={{ width: 16, height: 16, borderRadius: '50%', background: '#059669', color: '#fff', fontSize: 10, fontWeight: 800, display: 'inline-flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>✓</span>Full choropleth + colour scale + labels</div>
{['Use choropleth for quantitative regional data — colour intensity encodes the value.','Always show showColorScale so viewers can interpret colour intensity.','Include data for all 13 regions — gaps show as grey and may mislead.'].map((r,i)=>(
<div key={i} style={{ display: 'flex', gap: 6, fontSize: 12, color: '#065f46', marginTop: 4, lineHeight: 1.4 }}><span style={{ color: '#059669', fontWeight: 700, flexShrink: 0 }}>·</span>{r}</div>
))}
</div>
</div>

<div style={{ borderRadius: 10, overflow: 'hidden', border: '2px solid #fecaca' }}>
<div style={{ padding: '20px 16px', background: '#fff' }}>
<MapChart
  title="Region Data"
  data={[
    {id:'riyadh',value:8420},{id:'makkah',value:8560},{id:'eastern',value:4910},
  ]}
  variant="choropleth"
  showColorScale={false}
  showRegionLabels={false}
/>
</div>
<div style={{ padding: '10px 14px', background: '#fef2f2', borderTop: '1px solid #fecaca' }}>
<div style={{ display: 'flex', alignItems: 'center', gap: 7, fontSize: 12, fontWeight: 700, color: '#dc2626', marginBottom: 6 }}><span style={{ width: 16, height: 16, borderRadius: '50%', background: '#dc2626', color: '#fff', fontSize: 10, fontWeight: 800, display: 'inline-flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>✕</span>Partial data, no scale, no labels</div>
{["Don't omit showColorScale — without it, colour intensity is uninterpretable.","Don't leave most regions as grey — partial data creates a misleading null map.","Don't use a map when a bar chart would show magnitudes more clearly."].map((r,i)=>(
<div key={i} style={{ display: 'flex', gap: 6, fontSize: 12, color: '#991b1b', marginTop: 4, lineHeight: 1.4 }}><span style={{ color: '#dc2626', fontWeight: 700, flexShrink: 0 }}>·</span>{r}</div>
))}
</div>
</div>

</div>

---

## Accessibility

<div style={{ display: 'flex', flexDirection: 'column', gap: 16, marginBottom: 16 }}>
{[
{cat:'SVG Role & Labels',catBg:'#eff3fb',catBorder:'#dbeafe',catIconBg:'#1849a9',catIcon:'SVG',items:[
{title:'role="img" + aria-label on SVG',desc:'The SVG receives role="img" and aria-label={title} so screen readers announce it as a named geographic image.'},
{title:'Each region path has aria-label',desc:'Every clickable <path> carries aria-label="RegionName: value" so keyboard and AT users get the data for each region without relying on colour.'},
{title:'Hidden data table',desc:'A visually-hidden <table> (.srOnly) mirrors all region IDs and values. Screen readers expose it as a named table; sighted users see only the SVG.'},
]},
{cat:'Colour Independence',catBg:'#fdf4ff',catBorder:'#e9d5ff',catIconBg:'#9333ea',catIcon:'◑',items:[
{title:'Do not rely on colour alone',desc:'Region values are exposed via tooltip and the hidden accessible table. The colour ramp is a visual redundancy, not the sole indicator.'},
{title:'Colour scale bar',desc:'The gradient scale bar shows min and max values as text labels, providing a colour-independent reference for the intensity encoding.'},
{title:'Null regions use distinct fill',desc:'Regions without data use --chart-border (#f3f4f6) — visually distinct from even the lightest ramp colour (#d1e9ff). Tooltip shows "N/A".'},
]},
{cat:'RTL Accessibility',catBg:'#ecfdf5',catBorder:'#a7f3d0',catIconBg:'#059669',catIcon:'RTL',items:[
{title:'dir="rtl" switches to Arabic labels',desc:'Setting dir="rtl" on the card uses labelAr strings for all region labels — both in SVG text nodes and in tooltip rows.'},
{title:'Region aria-labels remain data-based',desc:'aria-label values reference the region name and numeric value — not their map position. RTL rendering does not change accessible names.'},
]},
].map(({cat,catBg,catBorder,catIconBg,catIcon,items})=>(
<div key={cat} style={{border:'1px solid #e5e7eb',borderRadius:10,overflow:'hidden'}}>
<div style={{display:'flex',alignItems:'center',gap:10,padding:'11px 16px',background:catBg,borderBottom:`1px solid ${catBorder}`}}>
<span style={{width:28,height:28,borderRadius:6,background:catIconBg,color:'#fff',fontSize:10,fontWeight:800,display:'inline-flex',alignItems:'center',justifyContent:'center',flexShrink:0}}>{catIcon}</span>
<span style={{fontSize:13,fontWeight:700,color:'#111827'}}>{cat}</span>
</div>
{items.map(({title,desc},j)=>(
<div key={j} style={{display:'flex',alignItems:'flex-start',gap:10,padding:'11px 16px',borderBottom:j<items.length-1?'1px solid #f3f4f6':'none',background:'#fff'}}>
<span style={{width:17,height:17,borderRadius:'50%',background:'#059669',color:'#fff',fontSize:10,fontWeight:800,display:'inline-flex',alignItems:'center',justifyContent:'center',flexShrink:0,marginTop:2}}>✓</span>
<div>
<div style={{fontSize:13,fontWeight:600,color:'#111827',marginBottom:2}}>{title}</div>
<div style={{fontSize:12,color:'#6b7280',lineHeight:1.5}}>{desc}</div>
</div>
</div>
))}
</div>
))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 8 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '160px 60px 1fr 100px', gap: 0 }}>
    {[['Criterion','Level','Requirement','Status']].map(([a,b,c,d]) => (
      <div key="hdr" style={{ display: 'contents' }}>
        <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb' }}>{a}</div>
        <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>{b}</div>
        <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb' }}>{c}</div>
        <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>{d}</div>
      </div>
    ))}
    {[
      ['1.1.1 Non-text Content', 'A', 'SVG role="img" + aria-label; each region path has descriptive aria-label; hidden <table> mirrors all data'],
      ['1.3.1 Info & Relationships', 'A', 'Data table with region names and values for screen readers'],
      ['1.4.1 Use of Colour', 'A', 'Values exposed via tooltip and accessible table — colour is redundant, not sole indicator'],
      ['1.4.3 Contrast (text)', 'AA', 'Title #000b36 on white: 18:1; scale labels #6c7c96: 4.6:1'],
      ['1.4.10 Reflow', 'AA', 'SVG uses width="100%" — scales to container'],
      ['2.1.1 Keyboard', 'A', 'Region paths are tabIndex={0}; tooltip opens on focus'],
    ].map(([criterion, level, req]) => (
      <div key={criterion} style={{ display: 'contents' }}>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', fontFamily: 'monospace', fontSize: 12, color: '#1849a9', fontWeight: 600 }}>{criterion}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: level === 'AAA' ? '#ede9fe' : '#dbeafe', color: level === 'AAA' ? '#6d28d9' : '#1849a9' }}>{level}</span>
        </div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', fontSize: 12, color: '#374151', lineHeight: 1.4 }}>{req}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: '#d1fae5', color: '#059669' }}>✓ Pass</span>
        </div>
      </div>
    ))}
  </div>
</div>

</div>
