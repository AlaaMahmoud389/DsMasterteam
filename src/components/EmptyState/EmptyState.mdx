import { Meta } from '@storybook/addon-docs/blocks';
import * as EmptyStateStories from './EmptyState.stories';
import { EmptyState } from './EmptyState';

<Meta of={EmptyStateStories} />

{/* ─── Header ─────────────────────────────────────────────── */}

<div style={{ marginBottom: 8, fontSize: 12, color: '#6b7280' }}>Components / Empty State</div>

# Empty State

A full-featured empty-state panel used when content is absent — after a failed search, when a section has no data yet, or when a file cannot be found. Supports three indicator types (Featured Icon, Illustration, File Type Icon), three sizes, dual-button actions, and RTL layout.

<div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginTop: 12, marginBottom: 32 }}>
  <span style={{ background: '#d1fae5', color: '#059669', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20 }}>Available</span>
  <span style={{ background: '#f0f4ff', color: '#1849a9', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20 }}>Node ID: 4770:11151</span>
  <a href="https://www.figma.com/design/WTmRAkJVvw0IvZMA7wBdTC/Masterteam-Ds-For-Experiment-ONLY?node-id=4770-11151" target="_blank" rel="noreferrer" style={{ background: '#fff7ed', color: '#c2410c', fontSize: 11, fontWeight: 700, padding: '2px 10px', borderRadius: 20, textDecoration: 'none' }}>Figma ↗</a>
</div>

{/* ─── On this page ─────────────────────────────────────── */}

<div style={{ background: '#f0f4ff', border: '1px solid #dce3f7', borderRadius: 8, padding: '14px 20px', marginBottom: 24 }}>
  <div style={{ fontSize: 12, fontWeight: 700, color: '#1849a9', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '.07em' }}>On this page</div>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '6px 0' }}>
    {[['Overview','#overview'],['Anatomy','#anatomy'],['Icon Types','#icon-types'],['Sizes','#sizes'],['RTL Support','#rtl-support'],['Button Variants','#button-variants'],['Props Reference','#props-reference'],['Token Table','#token-table'],['Do / Don\'t','#do--dont'],['Accessibility','#accessibility']].map(([label, href]) => (
      <a key={href} href={href} style={{ fontSize: 13, color: '#1849a9', textDecoration: 'none', lineHeight: 2 }}>{label}</a>
    ))}
  </div>
</div>

---

## Overview

<div id="overview" style={{ border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden', padding: 24, background: '#f8fafc', marginBottom: 32 }}>
  <EmptyState
    iconType="featured-icon"
    size="sm"
    title="No projects found"
    description='Your search "Landing page design" did not match any projects. Please try again.'
    primaryLabel="Button"
    secondaryLabel="Button"
  />
</div>

## Anatomy

<div id="anatomy" style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 12, marginBottom: 32 }}>
  {[
    { n: '1', title: 'Background Pattern', desc: 'Decorative concentric-circles (icon types) or grid-lines (illustration) pattern' },
    { n: '2', title: 'Indicator',          desc: 'Featured icon box, cloud illustration, or file type icon — varies by iconType prop' },
    { n: '3', title: 'Title',              desc: '18px semibold heading — primary message (e.g. "No projects found")' },
    { n: '4', title: 'Description',        desc: '14px regular supporting text — explains the empty state and next steps' },
    { n: '5', title: 'Secondary Button',   desc: 'Neutral action (e.g. "Learn more", "Browse all")' },
    { n: '6', title: 'Primary Button',     desc: 'Primary action with optional → arrow icon (e.g. "Get started", "Create project")' },
  ].map(({ n, title, desc }) => (
    <div key={n} style={{ border: '1px solid #e5e7eb', borderRadius: 10, padding: 16, background: '#fff' }}>
      <div style={{ width: 28, height: 28, borderRadius: '50%', background: '#1849a9', color: '#fff', fontSize: 13, fontWeight: 700, display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: 10 }}>{n}</div>
      <div style={{ fontSize: 13, fontWeight: 700, color: '#111827', marginBottom: 4 }}>{title}</div>
      <div style={{ fontSize: 12, color: '#6b7280', lineHeight: 1.5 }}>{desc}</div>
    </div>
  ))}
</div>

## Icon Types

<div id="icon-types" style={{ display: 'flex', flexDirection: 'column', gap: 24, marginBottom: 32 }}>

<div style={{ fontSize: 13, color: '#6b7280', marginBottom: 8, lineHeight: 1.6 }}>Three indicator types are available, each with a distinct visual treatment and background pattern.</div>

<div style={{ display: 'flex', gap: 24, flexWrap: 'wrap' }}>
  {[
    { iconType: 'featured-icon',  label: 'Featured Icon',   note: 'White box with icon + concentric-circles bg — use for search/filter empty states' },
    { iconType: 'illustration',   label: 'Illustration',    note: 'Cloud illustration with frosted overlay + grid bg — use for onboarding / first-time empty' },
    { iconType: 'file-type-icon', label: 'File Type Icon',  note: 'Gradient circle with file icon + concentric-circles bg — use for document/file contexts' },
  ].map(({ iconType, label, note }) => (
    <div key={iconType} style={{ flex: '1 1 300px', minWidth: 280 }}>
      <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 4 }}>{label}</div>
      <div style={{ fontSize: 12, color: '#6b7280', marginBottom: 10, lineHeight: 1.5 }}>{note}</div>
      <div style={{ border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden', background: '#fff' }}>
        <EmptyState iconType={iconType} size="sm" />
      </div>
    </div>
  ))}
</div>

</div>

## Sizes

<div id="sizes" style={{ marginBottom: 32 }}>

<div style={{ fontSize: 13, color: '#6b7280', marginBottom: 16, lineHeight: 1.6 }}>Three sizes control the indicator dimensions. Text sizes and button dimensions remain constant across all sizes.</div>

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 12, marginBottom: 20 }}>
  {[
    { size: 'sm', note: 'Featured icon 48×48 / Illustration 152×118' },
    { size: 'md', note: 'Featured icon 48×48 / Illustration 172×128' },
    { size: 'lg', note: 'Featured icon 56×56 / Illustration 220×160' },
  ].map(({ size, note }) => (
    <div key={size} style={{ border: '1px solid #e5e7eb', borderRadius: 10, padding: '10px 14px', background: '#f8fafc', fontSize: 12 }}>
      <div style={{ fontWeight: 700, color: '#1849a9', marginBottom: 4 }}>{size.toUpperCase()}</div>
      <div style={{ color: '#6b7280', lineHeight: 1.5 }}>{note}</div>
    </div>
  ))}
</div>

<div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
  {['sm', 'md', 'lg'].map(size => (
    <div key={size}>
      <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 8 }}>Featured Icon — {size}</div>
      <div style={{ border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden', background: '#fff' }}>
        <EmptyState iconType="featured-icon" size={size} />
      </div>
    </div>
  ))}
</div>

</div>

## RTL Support

<div id="rtl-support" style={{ display: 'flex', gap: 24, flexWrap: 'wrap', marginBottom: 32 }}>
  <div style={{ flex: '1 1 360px' }}>
    <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 8 }}>LTR (Left-to-Right)</div>
    <div style={{ border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden', background: '#fff' }}>
      <EmptyState iconType="featured-icon" size="sm" />
    </div>
  </div>
  <div style={{ flex: '1 1 360px' }}>
    <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 8 }}>RTL (Right-to-Left)</div>
    <div style={{ border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden', background: '#fff' }}>
      <EmptyState
        iconType="featured-icon"
        size="sm"
        rtl
        title="لا توجد مشاريع"
        description='بحثك عن "تصميم صفحة الهبوط" لم يطابق أي مشاريع. يرجى المحاولة مرة أخرى.'
        primaryLabel="زر"
        secondaryLabel="زر"
      />
    </div>
  </div>
</div>

## Button Variants

<div id="button-variants" style={{ display: 'flex', flexDirection: 'column', gap: 20, marginBottom: 32 }}>
  {[
    { label: 'Both buttons (secondary + primary with arrow)', primaryLabel: 'Get started', secondaryLabel: 'Learn more', showArrow: true },
    { label: 'Primary only — with arrow',   primaryLabel: 'Get started', secondaryLabel: null,       showArrow: true },
    { label: 'Primary only — no arrow',     primaryLabel: 'Get started', secondaryLabel: null,       showArrow: false },
    { label: 'No buttons',                  primaryLabel: null,          secondaryLabel: null,       showArrow: false },
  ].map(({ label, primaryLabel, secondaryLabel, showArrow }) => (
    <div key={label}>
      <div style={{ fontSize: 11, fontWeight: 700, color: '#9ca3af', textTransform: 'uppercase', letterSpacing: '.06em', marginBottom: 8 }}>{label}</div>
      <div style={{ border: '1px solid #e5e7eb', borderRadius: 12, overflow: 'hidden', background: '#fff' }}>
        <EmptyState iconType="featured-icon" size="sm" primaryLabel={primaryLabel} secondaryLabel={secondaryLabel} showArrow={showArrow} />
      </div>
    </div>
  ))}
</div>

## Props Reference

<div id="props-reference" style={{ display: 'grid', gridTemplateColumns: '180px 160px 130px 100px 1fr', gap: 0, marginBottom: 32, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', fontSize: 13 }}>
<div style={{ display: 'contents' }}>
  {['Prop','Type','Default','Required','Description'].map((h, i) => (
    <div key={h} style={{ padding: '10px 14px', background: '#f8fafc', fontWeight: 700, color: '#374151', fontSize: 12, borderBottom: '2px solid #e5e7eb', borderRight: i < 4 ? '1px solid #e5e7eb' : 'none' }}>{h}</div>
  ))}
</div>
{[
  ['iconType',       '"featured-icon" | "illustration" | "file-type-icon"', '"featured-icon"', 'No',  'Determines the indicator and background pattern',                 '#fff7ed','#c2410c'],
  ['size',           '"sm" | "md" | "lg"',                                  '"sm"',            'No',  'Controls indicator dimensions (text/buttons stay constant)',       '#fff7ed','#c2410c'],
  ['rtl',            'boolean',                                             'false',           'No',  'Right-to-left layout — mirrors buttons and text direction',       '#f0fdf4','#059669'],
  ['title',          'string',                                              '"No projects found"','No','Primary heading text',                                           '#f0f4ff','#1849a9'],
  ['description',    'string',                                              '"Your search…"',  'No',  'Supporting description text (set null/empty to hide)',            '#f0f4ff','#1849a9'],
  ['primaryLabel',   'string | null',                                       '"Button"',        'No',  'Primary action button text; null hides the button',              '#f8fafc','#374151'],
  ['secondaryLabel', 'string | null',                                       '"Button"',        'No',  'Secondary action button text; null hides the button',            '#f8fafc','#374151'],
  ['showArrow',      'boolean',                                             'true',            'No',  'Show → arrow icon inside the primary button',                    '#f0fdf4','#059669'],
  ['onPrimary',      'function',                                            '—',               'No',  'Click handler for the primary button',                           '#f8fafc','#374151'],
  ['onSecondary',    'function',                                            '—',               'No',  'Click handler for the secondary button',                         '#f8fafc','#374151'],
  ['icon',           'string (img src)',                                    '—',               'No',  'Custom icon src for the featured-icon box or illustration overlay','#f8fafc','#374151'],
  ['className',      'string',                                              '—',               'No',  'Extra class applied to the outer wrapper',                       '#f8fafc','#374151'],
  ['style',          'object',                                              '—',               'No',  'Inline styles on the outer wrapper',                             '#f8fafc','#374151'],
].map(([prop, type, def, req, desc, bg, color]) => (
  <div key={prop} style={{ display: 'contents' }}>
    <div style={{ padding: '9px 14px', background: bg, borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', color, fontWeight: 600, fontSize: 12 }}>{prop}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 11, color: '#7c3aed', wordBreak: 'break-word' }}>{type}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', fontFamily: 'monospace', fontSize: 12, color: '#6b7280' }}>{def}</div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderRight: '1px solid #e5e7eb', textAlign: 'center' }}>
      <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: req === 'Yes' ? '#fee2e2' : '#f3f4f6', color: req === 'Yes' ? '#dc2626' : '#6b7280' }}>{req}</span>
    </div>
    <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', color: '#374151', lineHeight: 1.4 }}>{desc}</div>
  </div>
))}
</div>

## Token Table

<div id="token-table" style={{ display: 'flex', flexDirection: 'column', gap: 0, border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 32 }}>

<div style={{ padding: '10px 16px', background: '#f8fafc', borderBottom: '2px solid #e5e7eb', fontSize: 12, fontWeight: 700, color: '#374151', display: 'grid', gridTemplateColumns: '280px 140px 1fr', gap: 0 }}>
  <div>Token</div>
  <div style={{ borderLeft: '1px solid #e5e7eb', paddingLeft: 14 }}>Value</div>
  <div style={{ borderLeft: '1px solid #e5e7eb', paddingLeft: 14 }}>Usage</div>
</div>

<div style={{ padding: '8px 16px', background: '#f0f4ff', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, color: '#1849a9', textTransform: 'uppercase', letterSpacing: '.07em' }}>Container &amp; Layout</div>
{[
  ['--empty-state/background/default',        '#ffffff',              'Featured icon box background'],
  ['--empty-state/illustration/null-2',        '#e5e7eb',              'Featured icon box border color'],
  ['--empty-state/illustration/null-3',        '#f3f4f6',              'File type icon gradient end color'],
  ['--empty-state/illustration/null-opacity',  'rgba(31,42,55,0.4)',   'Illustration frosted-glass overlay background'],
  ['--empty-state/spacing/card-3xl-gap',       '32px',                 'Gap between indicator, text block, and actions'],
  ['--empty-state/spacing/card-sm-gap',        '8px',                  'Gap between heading and description'],
  ['Inner content width',                       '352px',                'Fixed width for the text and button group'],
].map(([token, value, usage]) => (
  <div key={token} style={{ display: 'grid', gridTemplateColumns: '280px 140px 1fr', borderBottom: '1px solid #f3f4f6', alignItems: 'center' }}>
    <div style={{ padding: '9px 16px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9', fontWeight: 600, wordBreak: 'break-all' }}>{token}</div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', display: 'flex', alignItems: 'center', gap: 8 }}>
      {value.startsWith('#') && <div style={{ width: 16, height: 16, borderRadius: 4, background: value, border: '1px solid #e5e7eb', flexShrink: 0 }} />}
      <code style={{ fontSize: 11, color: '#6b7280' }}>{value}</code>
    </div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', fontSize: 12, color: '#374151' }}>{usage}</div>
  </div>
))}

<div style={{ padding: '8px 16px', background: '#f0f4ff', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, color: '#1849a9', textTransform: 'uppercase', letterSpacing: '.07em' }}>Typography &amp; Colors</div>
{[
  ['--empty-state/text/default',   '#000b36',     'Title / heading color'],
  ['--empty-state/text/secondary', '#3c5073',     'Description / supporting text color'],
  ['Title font',                   '18px / 28px / 600', 'IBM Plex Sans Arabic Semibold'],
  ['Description font',             '14px / 20px / 400', 'IBM Plex Sans Arabic Regular'],
].map(([token, value, usage]) => (
  <div key={token} style={{ display: 'grid', gridTemplateColumns: '280px 140px 1fr', borderBottom: '1px solid #f3f4f6', alignItems: 'center' }}>
    <div style={{ padding: '9px 16px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9', fontWeight: 600 }}>{token}</div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', display: 'flex', alignItems: 'center', gap: 8 }}>
      {value.startsWith('#') && <div style={{ width: 16, height: 16, borderRadius: 4, background: value, border: '1px solid #e5e7eb', flexShrink: 0 }} />}
      <code style={{ fontSize: 11, color: '#6b7280' }}>{value}</code>
    </div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', fontSize: 12, color: '#374151' }}>{usage}</div>
  </div>
))}

<div style={{ padding: '8px 16px', background: '#f0f4ff', borderBottom: '1px solid #e5e7eb', fontSize: 11, fontWeight: 700, color: '#1849a9', textTransform: 'uppercase', letterSpacing: '.07em' }}>Indicator Sizing</div>
{[
  ['Featured icon — sm/md', '48 × 48 px / r-10px', 'Icon box size and border radius'],
  ['Featured icon — lg',    '56 × 56 px / r-12px', 'Larger icon box for lg size'],
  ['Illustration — sm',     '152 × 118 px',         'Cloud illustration container'],
  ['Illustration — md',     '172 × 128 px',         'Cloud illustration container'],
  ['Illustration — lg',     '220 × 160 px',         'Cloud illustration container'],
  ['File type icon wrap',   '104 × 104 px',         'Gradient circle (40px icon + 32px padding)'],
  ['Background pattern',    '480 × 480 px',         'Decorative circle/grid behind the indicator'],
].map(([token, value, usage]) => (
  <div key={token} style={{ display: 'grid', gridTemplateColumns: '280px 140px 1fr', borderBottom: '1px solid #f3f4f6', alignItems: 'center' }}>
    <div style={{ padding: '9px 16px', fontFamily: 'monospace', fontSize: 11, color: '#1849a9', fontWeight: 600 }}>{token}</div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6' }}>
      <code style={{ fontSize: 11, color: '#6b7280' }}>{value}</code>
    </div>
    <div style={{ padding: '9px 16px', borderLeft: '1px solid #f3f4f6', fontSize: 12, color: '#374151' }}>{usage}</div>
  </div>
))}

</div>

## Do / Don't

<div id="do--dont" style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 32 }}>

<div>
  <div style={{ fontSize: 13, fontWeight: 700, color: '#059669', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 6 }}>
    <span style={{ background: '#d1fae5', borderRadius: 4, padding: '2px 8px' }}>Do</span>
    Write a helpful title and next-step description
  </div>
  <div style={{ border: '2px solid #d1fae5', borderRadius: 12, overflow: 'hidden', background: '#f0fdf4' }}>
    <EmptyState
      iconType="featured-icon"
      size="sm"
      title="No results found"
      description="Try adjusting your search or filters to find what you're looking for."
      primaryLabel="Clear filters"
      secondaryLabel="Browse all"
      showArrow={false}
    />
  </div>
</div>

<div>
  <div style={{ fontSize: 13, fontWeight: 700, color: '#dc2626', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 6 }}>
    <span style={{ background: '#fee2e2', borderRadius: 4, padding: '2px 8px' }}>Don't</span>
    Leave the user with no guidance or action
  </div>
  <div style={{ border: '2px solid #fecaca', borderRadius: 12, overflow: 'hidden', background: '#fff5f5' }}>
    <EmptyState
      iconType="featured-icon"
      size="sm"
      title="Empty"
      description=""
      primaryLabel={null}
      secondaryLabel={null}
    />
  </div>
</div>

<div>
  <div style={{ fontSize: 13, fontWeight: 700, color: '#059669', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 6 }}>
    <span style={{ background: '#d1fae5', borderRadius: 4, padding: '2px 8px' }}>Do</span>
    Match the icon type to the context
  </div>
  <div style={{ border: '2px solid #d1fae5', borderRadius: 12, overflow: 'hidden', background: '#f0fdf4' }}>
    <EmptyState
      iconType="file-type-icon"
      size="sm"
      title="No files uploaded"
      description="Drag and drop files here or click to upload."
      primaryLabel="Upload file"
      secondaryLabel={null}
    />
  </div>
</div>

<div>
  <div style={{ fontSize: 13, fontWeight: 700, color: '#dc2626', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 6 }}>
    <span style={{ background: '#fee2e2', borderRadius: 4, padding: '2px 8px' }}>Don't</span>
    Use more than two action buttons
  </div>
  <div style={{ border: '2px solid #fecaca', borderRadius: 12, overflow: 'hidden', background: '#fff5f5' }}>
    <EmptyState
      iconType="featured-icon"
      size="sm"
      title="No projects found"
      description="Your search did not match any projects."
      primaryLabel="Create new"
      secondaryLabel="Try again"
      showArrow
    />
  </div>
</div>

</div>

## Accessibility

<div id="accessibility" style={{ marginBottom: 32 }}>

<div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', marginBottom: 20 }}>
  {[
    { label: 'Semantic Structure', color: '#dbeafe', text: '#1849a9' },
    { label: 'Keyboard Navigation', color: '#ede9fe', text: '#6d28d9' },
    { label: 'Color Contrast',      color: '#d1fae5', text: '#059669' },
    { label: 'RTL Support',         color: '#fff7ed', text: '#c2410c' },
  ].map(({ label, color, text }) => (
    <div key={label} style={{ padding: '6px 14px', borderRadius: 20, background: color, color: text, fontSize: 12, fontWeight: 700 }}>{label}</div>
  ))}
</div>

<div style={{ display: 'flex', flexDirection: 'column', gap: 12, marginBottom: 20 }}>
  {[
    { cat: 'Semantic Structure', items: [
      'Title renders as a <p> with 18px semibold — consider wrapping in a <section> with <h2> at the page level',
      'Background pattern images carry alt="" to suppress screen-reader noise',
      'Illustration overlay and decorative images are all aria-hidden via empty alt attributes',
    ]},
    { cat: 'Keyboard Navigation', items: [
      'Both buttons are native <button> elements — fully keyboard-accessible by default',
      'Tab order: secondary button → primary button',
      'Focus ring: 0 0 0 4px #f2f4f7 on both button focus-visible states',
    ]},
    { cat: 'Color Contrast', items: [
      'Title #000b36 on white: > 15:1 — exceeds AAA',
      'Description #3c5073 on white: > 7:1 — exceeds AA',
      'Primary button text #f9fafb on #1849a9: > 5:1 — meets AA',
      'Secondary button text #000b36 on #f3f4f6: > 13:1 — exceeds AAA',
    ]},
    { cat: 'RTL', items: [
      'dir="rtl" on the wrapper is sufficient to flip the layout and button order',
      'Arrow icon in primary button should be flipped via transform: scaleX(-1) in RTL (handled automatically)',
    ]},
  ].map(({ cat, items }) => (
    <div key={cat} style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden' }}>
      <div style={{ padding: '8px 14px', background: '#f8fafc', borderBottom: '1px solid #e5e7eb', fontSize: 12, fontWeight: 700, color: '#374151' }}>{cat}</div>
      <div style={{ padding: '10px 16px', display: 'flex', flexDirection: 'column', gap: 6 }}>
        {items.map(item => (
          <div key={item} style={{ fontSize: 13, color: '#374151', lineHeight: 1.5, display: 'flex', gap: 8 }}>
            <span style={{ color: '#059669', flexShrink: 0 }}>–</span>
            <span>{item}</span>
          </div>
        ))}
      </div>
    </div>
  ))}
</div>

<div style={{ border: '1px solid #e5e7eb', borderRadius: 10, overflow: 'hidden', marginBottom: 8 }}>
  <div style={{ display: 'grid', gridTemplateColumns: '160px 60px 1fr 100px', gap: 0 }}>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb' }}>Criterion</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>Level</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb' }}>Requirement</div>
    <div style={{ padding: '9px 14px', background: '#f8fafc', fontWeight: 700, fontSize: 12, color: '#374151', borderBottom: '2px solid #e5e7eb', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>Status</div>
    {[
      ['1.1.1 Non-text Content','A',  'Decorative images carry alt="" — screen readers skip them'],
      ['1.4.3 Contrast',        'AA', 'All text elements exceed 4.5:1 contrast ratio against their backgrounds'],
      ['2.1.1 Keyboard',        'A',  'Both action buttons are native <button> — keyboard-accessible without custom handling'],
      ['2.4.7 Focus Visible',   'AA', 'Buttons show a visible 4px focus ring on keyboard focus'],
      ['1.3.1 Info & Structure','A',  'Landmark structure communicated through semantic HTML; decorative patterns suppressed'],
    ].map(([criterion, level, req]) => (
      <div key={criterion} style={{ display: 'contents' }}>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', fontFamily: 'monospace', fontSize: 12, color: '#1849a9', fontWeight: 600 }}>{criterion}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: '#dbeafe', color: '#1849a9' }}>{level}</span>
        </div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', fontSize: 12, color: '#374151', lineHeight: 1.4 }}>{req}</div>
        <div style={{ padding: '9px 14px', background: '#fff', borderBottom: '1px solid #f3f4f6', borderLeft: '1px solid #e5e7eb', textAlign: 'center' }}>
          <span style={{ fontSize: 11, fontWeight: 700, padding: '2px 8px', borderRadius: 10, background: '#d1fae5', color: '#059669' }}>✓ Pass</span>
        </div>
      </div>
    ))}
  </div>
</div>

</div>
