# Design System Rules

Use Storybook as the source of truth.

Always search for an existing component before creating a new one.

Reuse all existing components from the design system.

Follow existing:
- Colors
- Typography
- Spacing
- Grid system
- Interaction patterns

If a component exists in Storybook, use it instead of creating a new one.
